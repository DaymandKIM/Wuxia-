# 무협 방치형 — 작업 지침

세로 화면 모바일 방치형 RPG. 자동 전투를 구경하는 게 코어다.
소스는 모듈로 나뉘어 있고, `build.py`가 **단일 HTML 하나**로 묶는다.
브라우저에서 바로 열린다. 서버·번들러·프레임워크 없다.

한국어로 대화한다. 주석도 한국어로 쓴다.

---

## 반드시 지킬 것

**0. 스프라이트를 자르면 눈으로 먼저 검사받는다.**
`python review.py [종류]`로 확대 대조 PNG를 만들어 사용자에게 보이고,
확인을 받은 뒤에 게임에 반영한다. `spritetest.js`가 통과해도
"그림이 이상한 것"은 못 잡는다. 이걸 안 해서 잘린 지팡이·구체·개구리
정수리·앞다리를 네 번 놓쳤다.

**검은 조각은 매번 자동으로 확인한다** (사용자 확정 — "매번 말해야 하니").
옆칸 스프라이트(발·기운 조각)가 얇은 다리로 붙어 머리 위에 검은 막대로
뜨는 사고가 반복됐다("캐릭터에 검은선"). 이건 실루엣이 몸과 (대각·직교)
가늘게 이어져 **기하 검사(spritetest)로는 못 잡는다** — 규칙 0 그대로
"그림이 이상한 것"이라 눈(비전)으로만 잡힌다. 그래서 스프라이트를 자르면
**반드시** `python blackcheck.py [이름...]`으로 흰 배경 확대판을 만들어
비전으로(주 에이전트가 그림을 읽거나 서브에이전트에 위임) 검은 막대·
테두리 잔재·부유 조각이 없는지 확인한 뒤 반영한다. 추출 단계에서
declutter(1px 침식→최대 덩어리→dilate)로 얇게 붙은 조각을 미리 끊는다.
**반영한 뒤에는 구동 검증을 붙인다** (사용자 확정) — 파일 규격만 보는
spritetest와 별개로, 그 그림이 실제로 맞는 상황·맞는 스트립·맞는 폭으로
그려지는지 jsdom에서 확인한다(`fxtest.js`). 새 스프라이트가 게임에
들어가면 반드시 fxtest에 검사를 추가한다.

**1. 숫자는 `src/00-data.js`에만.** 코드에 상수를 박지 않는다.
밸런스·크기·속도·확률 전부 여기 모여 있다.

**2. 빌드 결과를 손으로 고치지 않는다.** `dist/wuxia.html`은 생성물이다.
`src/`를 고치고 `python build.py`를 다시 돌린다.

**3. 한 번에 하나씩.** 광범위한 변경 대신 문제를 하나씩 격리해서 고친다.

**4. 밸런스는 직관이 아니라 `node sim.js` 출력으로 판단한다.**
점검할 축(수련 스텟·무공 계수·숙련 성 등)은 `docs/QA-밸런스.md`에 정리돼 있다.
**새 성장 축이 들어오면 그 표와 `sim.js`의 spend()에 반드시 추가한다.**

---

## 명령

```bash
python build.py        # src/ + assets/ → dist/wuxia.html   (먼저 이걸 돌린다)

node lint.js           # 정의 없는 호출·빠진 필수 함수
node spritetest.js     # 선언 규격 vs 실제 PNG · 캔버스 가장자리 접촉
python review.py       # 눈으로 볼 대조 PNG → review/review-<종류>.png
python blackcheck.py [이름...]  # 검은 막대·부유 조각 눈검사판(흰 배경 확대) → review/blackcheck.png
python skinmatch.py [키...]     # 옛 시트 스트립 살색·옷색을 새 시트 톤으로 (질주·권기·옛 발차기·경공 컷 — 다시 뽑으면 다시 돌린다)
node test.js           # jsdom으로 실제 실행 (오류 0 이어야 한다)
node sim.js            # 24시간 진행 시뮬 (소비 전략 포함, SIM_MIN=분 으로 단축)
node animtest.js       # 공격 동작이 피격에 끊기는지
node hittest.js        # 방향별 명중
node bosstest.js       # 구역별 보스 격파 시간
node sweeptest.js      # 단계 정리 연출
node medtest.js        # 운기조식(회복)
node uitest.js         # 사냥터 이동 UI
node screens.js        # 기기별 캔버스 크기
node frogtest.js       # 개구리 혀 사거리·규격
node shamantest.js     # 주술사 원거리·마법
node savetest.js       # 저장·오프라인 진행 (자동 저장·상한·깨진 저장 복구)
node artstest.js       # 무공 (표 노출·습득 조건·심법 효과·초식 자동 시전·저장)
node fatetest.js       # 기연 (인연 축적·카드·보상·조각 해금·저장)
node equiptest.js      # 장비 (드랍·합성·자동/직접 장착·아이템 강화·보유 효과·저장·패널)
node fxtest.js         # 주인공 연출 구동 (시전 스트립·탄·권기 정권·경지 기운)
node ronintest.js      # 폐촌 (낭인 베기·병 던지기, 들개, 보스 원혼 해골 귀화)
node snowtest.js       # 설산 (설랑 물기, 빙백령 얼음 조각, 보스 백호 숨결 탄)
node bambootest.js     # 죽림 확장 (정령병·추적자 잎날·방울뱀)
node cavetest.js       # 동굴 (독충 물기, 박쥐 음파 고리)
node heaventest.js     # 천산 (수리 급강하, 수호무사 초승달 검기)
python castshot.py     # 주술사 두 동작 미리보기 그림
```

`npm i` 한 번 필요하다 (jsdom).
빌드 경로를 바꾸려면 `WUXIA_OUT=/어디/wuxia.html python build.py`.

---

## 구조

```
src/00-data.js     설계 숫자 전부. 여기만 보면 밸런스가 보인다
src/10-engine.js   캔버스·이미지 로딩·수학 헬퍼
src/15-audio.js    합성음 (파일 없음, WebAudio로 만든다)
src/20-state.js    S(게임) · P(주인공)
src/30-combat.js   공격·피해·쓰러짐
src/40-step.js     매 프레임 갱신
src/50-render.js   그리기
src/60-ui.js       HUD·토스트·사냥터 여정 지도(buildZonePanel — 지도 노드는 선택, 이동은 단계 줄·S.reach 가 본 단계까지, v2.70.6)
src/62-train.js    수련(스텟 구매) 패널·탭 알림점
src/63-arts.js     무공 패널·습득·탭 알림점 (초식 시전은 30-combat)
src/64-fate.js     기연 — 인연 정산 이벤트·카드 (인연 축적은 30-combat)
src/65-save.js     저장(localStorage)·오프라인 진행·돌아온 패널
src/68-equip.js    장비 — 드랍·합성·장착·아이템 강화·보유 효과·패널 (v2.70 표준형)
src/70-main.js     시작·루프

assets/*.png       스프라이트. 파일명이 곧 키다 (frog_atk1.png → 'frog_atk1')
sheets/*.png       AI가 만든 원본 시트. 다시 뽑을 때 여기서 시작한다
raw/shaman/*.png   샤먼 기본 5동작의 절단된 원본 (시트가 없어 보관 중)
shell.html         HTML 껍데기. build.py가 여기에 코드를 끼워 넣는다

dist/              빌드 결과 (생성물)
review/            검사판 PNG (생성물)
```

---

## 스프라이트 파이프라인 — 여기서 계속 사고가 났다

에셋은 AI가 만든 시트에서 파이썬으로 뽑는다. 종류마다 스크립트가 하나씩 있다.

| 스크립트 | 시트 | 상태 |
|---|---|---|
| `frog.py` | `sheets/frog_idle.png` `sheets/frog_atk.png` | 돌아간다 |
| `demonsheet.py` → `demonclean.py` | `sheets/demon.png` | 돌아간다 (순서 지켜야 함) |
| `bandit.py` → `foesheet.py` | `sheets/bandit.png` | 돌아간다 (v2.64, 4×4 공용 추출기) |
| `shaman.py` → `foesheet.py` | `sheets/shaman.png` + `shaman_b.png` | 돌아간다 (v2.64, 두 시트 합침) |
| `panther.py` `wisp.py` → `foesheet.py` | `sheets/panther.png` `sheets/wisp.png` | 돌아간다 (v2.65 — 테두리 줄 검출 격자) |
| `hero_sheet.py` + `hero_*2.py` | `sheets/hero_*2.png` | 돌아간다 (v2.73 — **그림 기준** 공용 추출기. 아래 8개 스크립트는 컷 선택만) |
| `hero_pose2.py` | 주먹4·발차기2 시트 | 돌아간다 (v2.76.5 — 대기·피격은 주먹4(황갈 얼굴 기준 시트), 경공은 발차기2+skinmatch. 운기조식·시전만 옛 시트) |
| `hero_kick3.py` | `sheets/hero_kick3.png` | **현행** (v2.76.1 — 발차기 3종, gridless. 기준 컷은 곧게 선 (0,0)으로 hero_punch4와 배율 일치) |
| `hero_kick2.py` | `sheets/hero_kick2.png` | 옛 발차기(v2.71) — **v2.76.2부터 kickside2·kickround2·kickhigh2 동작으로 되살림**(raw/kick2_old = 원본). hero_pose2 피격·경공 컷도 여기서 |
| `hero_run2.py` | `sheets/hero_run2.png` | 돌아간다 (v2.71.1 질주 6컷 → v2.76.6 scale_mul 0.85 + skinmatch — 머리 큰 시트·파란 옷을 새 시트에 맞춤) |
| `hero_punch4.py` | `sheets/hero_punch4.png` | **현행** (v2.76 — 주먹 3종 정권·연환권·승룡권, gridless. 잔상 컷은 마젠타라 폐기) |
| `hero_punch3.py` | `sheets/hero_fx.png` 2·3줄 | 권기 정권 두 판 → qipunch·qipunchb(순환 맨 끝 마무리 일격). **scale_mul 0.86** — hero_fx는 머리 큰 비율이라 머리 폭 기준으로 줄임(v2.76.3) |
| `hero_punch2.py` | `sheets/hero_punch2.png` | 대기·피격 컷(hero_pose2)에만 쓴다. 정권으로는 안 쓴다 |
| `hero_sword4.py` | `sheets/hero_sword4.png` | **현행** (v2.74.5 2차 재작업 — 하늘색 검기, 다른 무기와 통일) |
| `hero_sword3.py` | `sheets/hero_sword3.png` | 1차 재작업(흰 검기). 보관 |
| `hero_sword2.py` | `sheets/hero_sword2.png` | 옛 시트(액자선에 잘림). 쓰지 않는다 |
| `hero_fan3.py` | `sheets/hero_fan3.png` | **현행** (v2.75 재작업 시트, gridless) |
| `hero_fan2.py` | `sheets/hero_fan2.png` | 옛 시트(액자선에 잘림). 쓰지 않는다 |
| `hero_saber3.py` | `sheets/hero_saber3.png` | **현행** (v2.74.2 재작업 시트, gridless) |
| `hero_saber2.py` | `sheets/hero_saber2.png` | 옛 시트(액자선에 잘림). 쓰지 않는다 |
| `hero_spear3.py` | `sheets/hero_spear3.png` | **현행** (v2.74.3 재작업 시트, gridless) |
| `hero_spear2.py` | `sheets/hero_spear2.png` | 옛 시트(액자선에 잘림). 쓰지 않는다 |
| `hero_staff3.py` | `sheets/hero_staff3.png` | **현행** (v2.74.4 재작업 시트, gridless) |
| `hero_staff2.py` | `sheets/hero_staff2.png` | 옛 시트(액자선에 잘림). 쓰지 않는다 |
| `bossfx.py` | **시트 없음** | 못 돌린다 |
| `shamanmagic.py` `shamanstaff.py` | — | 옛 주술사(추정 지팡이). v2.64에 대체돼 쓰지 않는다 |
| `shaman.py` `demon.py` `shamanorb.py` | — | 옛 버전. 쓰지 않는다 |

시트가 없는 것들은 `assets/`의 결과물만 남아 있다. 다시 뽑으려면 시트가 필요하다.

### 겪은 사고와 원인

- **비율이 다른 시트는 머리 폭으로 맞춘다**(v2.76.3 "머리가 왜 이리 크냐"): hero_fx는 머리 큰 치비라 몸 높이(BODY_PX 47)로
  맞추면 머리가 27px(새 시트 20~23)로 튄다. extract(scale_mul=)로 줄인다 — 몸이 조금 작아져도 머리가 먼저 보인다.
- **시트마다 살색이 다르다**(v2.76.4~5 "피부색이 오묘하게 다르다"·"얼굴색 다시 봐봐"): hero_fx·hero_kick2 주황 (224,152,120) /
  hero_punch2 창백한 베이지 (204,184,156) / **hero_punch4·hero_kick3 황갈 (203,138,125) ← 기준**. 옛 스트립을 쓰면
  `python skinmatch.py`로 맞추고, 대기·피격 같은 단일 컷은 기준 시트(주먹4)에서 다시 뽑는다. **살색은 r-g>40 마스크로만
  재라** — r-g 20 마스크는 도복 베이지(185,165,130)를 살색으로 세서 기준색이 도복색이 됐다(v2.76.4 사고).
  얼굴 확대 대조는 `review/face_compare.png`(스크립트는 VERSION v2.76.5 참조).
  **옷색도 다르다**(v2.76.6 "걷고 뛰는 거 봐봐"): 옛 시트 허리띠·바지 파랑 (80,92,140) vs 새 시트 회청 (88,80,112) —
  skinmatch.py가 옷 단계까지 같이 맞춘다(기운 이펙트는 밝기·g로 제외). 옛 시트 컷을 새로 쓰면 **크기(scale_mul)·살색·
  옷색 세 가지**를 다 맞춰야 순환 중 딴 인물이 안 된다.
- **이펙트 색은 시트 프롬프트에서 못 박는다**: 자주·분홍 잔상은 마젠타 배경 판정에 먹혀 못 뽑는다(v2.76). 파랑·청록·흰·금만.
- **짧은 무기는 같은 시트의 온전한 컷에서 잘라 붙인다**(v2.74.6, 사용자): hero_sheet.extract(patch=...) 훅으로
  원본 해상도에서 손질한다. 예: hero_sword4.graft — 기증 칼날을 자루 끝·축에 맞춰 늘려 투명한 자리와 별 위에 칠함.
- **재작업 시트(순마젠타·선 없음)는 gridless 모드**(v2.74): 균등 분할 칸 + 인물 발끝 땅 + 기준 컷 배율(stand_cell).
  시트마다 배율이 달라(165 vs 172px) STAND_H 고정값을 쓰면 크기가 어긋난다 — 재작업 시트는 꼭 stand_cell을 준다.
- **v2.72 무기 시트 5장은 안쪽 액자선에서 무기가 잘려 있다**(v2.73.3, review/clipped_cells.png) — 복구 불가.
  여백의 파편은 붙이지 않는다(hero_sheet). 재작업 프롬프트 docs/프롬프트-주인공.md "무기 공격 시트 재작업".
  새 시트 프롬프트엔 반드시 "NO frames/borders/brackets, weapon fully inside the cell with padding"을 넣는다.
- **칸은 그림의 경계가 아니다 — 무기가 테두리를 넘어 여백까지 그려진다**(v2.73 감사: 창 끝·큰 원 검기가
  여백에 1000px). 주인공 시트는 hero_sheet.py가 시트 전체 덩어리 라벨링 + 줄 이어붙이기로 **그림 기준**
  크롭하고 캔버스 규격을 자동으로 잰다(review/hero_specs.json → HFX.aw/fh). 칸 단위 크롭 금지.
- **주인공 컷은 한 시트 계열로 통일한다**(v2.73.2 "여전히 이상"): 옛 시트(흰 도복·날씬)와 새 시트(베이지·굵은
  윤곽)가 섞이면 피격 때마다 딴 인물이 깜빡인다. 새 동작이 들어오면 대기·피격·경공까지 같은 계열로 맞춘다.
  남은 옛 컷: medit(운기조식)·cast*(초식 시전) — 새 시트가 오면 교체.
- **옅은 배경 시트에선 도복이 배경으로 지워진다**(v2.73.1 "도복이 배경에 묻힘"): 배경 (171,138,167)과 도복
  베이지 (185,165,130)가 ±40 안이다. 배경 판정은 반드시 **색상(자줏빛)** 조건을 같이 건다. 그리고 **검사판
  배경은 흰색이 아니라 게임 바닥색**으로 — 흰 배경은 도복이 비치는 구멍을 못 보여 준다(hero_sheet 검사판).
- **얇은 칼날은 축소에서 사라진다**(원본 2px → 0.5px): 축소 전 알파 1px 팽창 + 문턱 96(hero_sheet.shrink).
- **테두리 줄이 행마다 1~2px 어긋나면 전체 높이 검출에 빠진다**(부채 시트): 칸 폭이 중앙값보다 15% 넓으면
  빠진 줄을 찾아 쪼갠다(hero_sheet). 검출 문턱을 낮추거나 어두운 픽셀을 넓히면 인물 윤곽까지 줄로 잡힌다.
- **시트마다 배경 농도·테두리 색·모서리 표식이 다르다.** 주먹 시트(v2.71.2)는 배경이 옅어 r-g>50 판정이
  안 잡혔고, 칸 모서리 ㄱ자 표식이 조각으로 남았다. hero_kick2.grid/cell_rgba가 배경 중앙값 ±40·어두운
  얇은 줄 격자·모서리 조각 버리기로 일반화돼 있다 — 주인공 시트는 이걸 쓴다.
- **AI 시트의 칸은 균등하지 않다.** 표범 시트는 3행이 172px, 4~5행이 255px였다. 고정
  격자(H/행수)로 자르면 칸을 걸쳐 두 그림이 섞인다. `foesheet._lines`가 어두운 테두리 줄을
  검출해 격자를 잡는다 — 검출 개수가 행·열과 안 맞을 때만 고정 격자로 떨어진다.
- **AI 시트의 칸은 프레임 경계가 아니다.** 샤먼 마법 시트는 가운데 두 칸이
  한 그림이었다 — 기운이 지팡이에서 나와 옆 칸으로 이어진다. 칸마다 자르면
  그림이 두 동강 난다. 마젠타가 아닌 덩어리를 라벨링하면 이어진 칸이
  하나로 붙어 나오므로 그걸로 판단한다.
- **칸 테두리는 순검정이 아니다.** 마왕 시트는 테두리가 마젠타와 섞인
  어두운 보라(115,11,113)였다. 검정으로 찾으면 한 줄도 안 잡힌다.
- **테두리는 덩어리를 고른 *뒤에* 지운다.** 먼저 지우면 테두리를 가로지르는
  촉수가 두 동강 나서 위쪽이 버려진다.
- **몸에 붙은 검은 외곽선을 지우지 마라.** '어둡고 채도 낮은 것'을 전부
  배경으로 보면 개구리 정수리가 납작해진다. 알파를 만든 뒤 검은 픽셀 쪽으로
  2px 되살린다.
- **얇은 것은 통째로 배경색에 물든다.** 마왕 촉수가 1~2px이라 전체가 분홍이
  됐다. 지우면 촉수가 사라진다. 지우지 말고 마젠타 성분만 걷어낸다
  (r·b를 g 쪽으로). 자주색이 그림인 프레임은 분홍 비율로 갈라낸다.
- **없는 동작을 합성하지 마라.** 개구리 이동 프레임을 혀 달린 그림에서
  혀만 지워 만들었더니 얼굴이 잘렸다. 시트에 있는 칸을 쓴다.
- **꼭 재단해야 하면 대상만 떼어내서 한다.** 혀를 줄이려고 세로로 통째
  잘랐더니 같은 열에 있던 앞다리가 같이 잘렸다. 혀만 분리해서 줄이고
  몸 위에 다시 얹는다.
- **재단 뒤에 딱 맞게 잘라내지 마라.** 프레임마다 잘리는 자리가 달라져
  몸이 1px씩 흔들린다. 공통 캔버스에서 한 번에 정리한다.
- **그림을 도형으로 가정하고 마스크로 깎지 마라.** 건곤이형 원반을
  "원이겠지" 하고 원형 마스크로 깎았다가 바깥 고리가 잘렸다. 칸을 잘못
  잘랐으면 격자를 버리고 **덩어리 라벨링 bbox**로 다시 자른다 — 그게
  그림의 진짜 모양이다.
- **잘림 판별법:** 끝 세 열/행의 픽셀 수가 줄지 않고 끝나면 절단이다.
  자연스러운 형태는 끝으로 갈수록 줄어든다.
- **칸 테두리 잔재는 최종 에셋에서 다시 확인한다.** katka·katkb·방울뱀·
  추적자에 어두운 자주 줄·상자·옅은 워시가 남은 채 v2.23~26이 나갔다
  ("테두리가 검은 현상" 제보, v2.30에서 세정). katka류는 noEdge라 기존
  가장자리 검사를 다 통과했었다 — spritetest의 **줄 잔재 검사**(가장자리
  8px 안 85% 줄 + 안쪽 3줄 급단절)가 이제 이걸 잡는다. 색으로 지울 땐
  주인공 머리카락(어두운 자주갈색)이 같이 먹히니 기하 조건을 꼭 더한다.

### 렌더러 규약

`drawFoe`는 스프라이트를 **가로 중앙 · 바닥 기준**으로 그린다.
그래서 캔버스는 좌우 대칭이어야 한다 — 개구리 혀나 샤먼 기운처럼 한쪽으로만
뻗는 것이 있으면 그만큼 반대쪽에도 여백이 필요하다. 그래서 개구리가 164×43,
샤먼이 198×63이다.

몸보다 캔버스가 넓은 적은 선택값 두 개를 준다.
- `sw` — 그림자 크기와 공격 기울임에 쓰는 실제 몸 폭
- `bh` — 체력바 높이와 탄 발사 위치에 쓰는 실제 몸 높이

없으면 `w`·`h`를 쓴다.

**시트 방향은 눈으로 단정하지 말고 실기로 확인한다.** 강도 시트(v2.64)를 왼쪽 보기로
오판해 반전했더니 게임에서 주인공에게 등을 돌렸다. 뽑은 뒤 반드시 주인공 좌·우에 세운
크로뮴 스크린샷으로 확인한다.
**적 스프라이트 기본 방향은 오른쪽이다.** `f.dir = P.x >= f.x ? 1 : -1`이고
dir이 음수일 때만 좌우 반전하므로, 에셋은 오른쪽을 봐야 양쪽에서 주인공을
향한다. AI 시트가 왼쪽을 보면 추출 때 뒤집는다 — 단, 칸마다 방향이 섞인
시트가 있으니(낭인 사건) **걷기 프레임과 실기 스크린샷으로 칸별 확인**한다.
들개는 반대로 구웠다가 v2.11에서 전 프레임을 되돌렸다.

---

## 지금 상태

죽림 11종(강도·유령불·표범·주술사·개구리 + v2.25~27 확장: 무뢰배·
등딱지벌레·말벌(비행), 엘리트 정령병·추적자, 레어 방울뱀 —
등장표 13칸) + 보스 마왕,
**폐촌 3종**(낭인 v2.9 · 들개 v2.10 · 보스 원혼 v2.11),
**동굴 3종**(독충 v2.12→정밀판 v2.19 · 박쥐 v2.24 — 비행·음파 고리 fly 탄 · 보스 석암거인 v2.13),
**설산 3종**(설랑 v2.14 · 빙백령 v2.16 — 원거리 얼음 조각, shotFly 탄 ·
보스 설산백호 v2.17 — 눈보라 숨결 탄),
**천산 3종**(천산수리 v2.20 — 비행 몹: 캔버스에 띄움 12px, 대기·활공만
왼쪽 보기라 8컷 반전 · 수호무사 v2.21 — 정예 검객: 낭인 투척 틀 +
fly 초승달 검기, hp 1.7 정예 스탯, v2.22에 **돌격**(FOES.dashCd 계열 —
중거리 70~170에서 찌르기 자세로 미끄러져 들어와 꿰뚫는다, 투척은
180~270 담당) · 옥갑충 v2.27 — 玉甲 딱정벌레, 강도 재탕 대체) +
**보스 뇌운신장**(v2.32 — 먹구름 몸+금 갑주 뇌신 장수. 번개 주먹
스매시 4컷[시트 좌향 반전] + 갈래 번개 탄, 원혼·백호와 같은 탄 보스 틀.
시전/죽음 컷은 배율이 달라 92/139로 별도 정규화. 이로써 5구역 보스 완성) —
전부 사용자 제미나이 시트.
v2.17에서 **보스 단계 탄 동결 버그**를 잡았다: step()의 isBoss 분기가
탄 루프보다 먼저 return 해서 보스의 탄이 허공에 얼어 있었다 — 탄 루프를
분기 앞으로 옮겼고, 보스 탄 검증은 fly 폭발(burst) 계수로 한다
(체력 감소는 보스 전천후 근접타와 구분이 안 돼 가짜 통과가 났었다). 낭인은 근접 베기가 기본인데 거리가 뜨면(80~200px) 술병을
던진다 — FOES.throwCd 계열이 근접형+투척 혼합 AI의 틀이다(탄 그림
shotImg·명중 먼지 shotDust, 탄은 빙글 돌며 난다). **원혼은 탄 보스의
틀**이다 — FOES에 shotImg가 있는 보스는 스킬(BOSSKILL)이 범위 폭발 대신
거리 불문 해골 귀화 탄이 된다(fly 탄: 빙글 안 돌고 진행 방향으로 기운다 —
v2.39부터 얼음·번개 등 모든 fly 탄이 나는 각도로 회전. 왼쪽은 거울 뒤
반전각으로 뒤집힘 방지, 파공권과 같은 방식. drawShots).
폐촌 등장표 낭인2·들개2·강도1·유령불1.
근접 사거리는 위험도별(FOES.range: 유령불38 < 강도44 < 들개52 < 표범60
< 낭인64), **보스는 모든 범위**(BOSS.range 999 — 사용자 확정).
**옆모습 대치**(v2.29, 사용자 확정 3안) — 스프라이트가 옆모습이라
전투의 주 방향을 좌우로 편향: 잡몹 등장원의 세로 성분을 0.45로 압축
(SPAWN.flat — 완전 수평은 위아래가 비어 보여서 편향만), 보스 소환문은
주인공 **오른쪽 +150·같은 땅 높이**(SUMMON.side — v2.31.1 사용자 확정:
주인공은 중앙 조금 왼편·보스는 오른편, 카메라 2:1이 그 구도를 만든다.
사거리 999라 소환점이 곧 대치 구도), 보스는 높이만 슬며시 맞추고(BOSS.alignY), 주인공은
보스 거리를 몸 가장자리(sw×BOSS.edge 0.35)에서 재서 파고들지도, 허공을
때리지도 않는다(foeRad — v2.30: 0.5는 주먹이 몸에 안 닿아 "공격 안 함"
제보가 왔다. heroAttack 거리도 조준·판정과 같은 눌린 척도로 통일).
시트는 채팅 그림도 된다 — 붙여넣기 이미지가 디스크(uploads)에 저장 안 될 때가
있는데(파일 첨부만 저장됨), 그럴 땐 **대화 기록 jsonl의 base64 인라인 이미지를
디코드해 복원**한다: `~/.claude/projects/-home-user-Wuxia-/<세션>.jsonl`을 줄단위로
파싱해 content의 `type:'image'` 블록 `source.data`(webp base64)를 base64 디코드→
PIL로 열어 png 저장(v2.60에서 폐촌·동굴·설산·천산 4장 이렇게 복원).
5구역 × (10단계 + 보스). 무기 없음, 맨손 정권만.
**이동 애니 질주 6프레임**(v2.71.1, sheets/hero_run2 1줄 — hero_run2.py. 아래는 옛 10프레임 이력) **← v2.44 10프레임** — v2.43 8프레임이 조깅 느낌이라 아쉽다는
피드백 → docs/프롬프트-주인공.md 질주 프롬프트로 재작업 시트를 받아 교체
(ANIM.run [10,15], 전방 기울임·옷자락 날림). 8컷 공통 배율(크기 안정)+몸통 무게중심
가로 정렬(좌우 흔들림 방지). **대기=정면 전투 자세** 단일 컷(시트 14, ANIM.idle
[1,4]) — 멈추면 기수식. **경공 착지=웅크림**(시트 15)으로 dashland 교체.
**경공**(v2.41, 사용자 아이디어) — 절정(DASH.realm 12)부터, 가장 가까운 적이
DASH.min(170) 이상 멀면 쿨(5초)마다 훌쩍 날아가 코앞에 착지 후 공격.
원거리 몹(빙백령 등) 견제를 무력화한다. 스프라이트는 sheets/hero_dash.png의
날기·착지 두 컷만 뽑아 씀(회오리·나선 궤적은 코드 먼지 잔상으로 대체).
P.dashT(비행)·dashHold(착지 경직)·dashCd. drawHero의 dashfly/dashland 분기는
공중 lift. 비행 중 step은 카메라만 갱신하고 return(0.1~0.5초라 적은 잠깐 정지).
**날기·착지 컷은 시트가 확대(줌인)로 그려져 머리가 컸다**("경공 쓸 때 머리
커짐" 제보, v2.53) — 렌더 규격만 줄여 대기/질주 컷과 인물 크기를 맞췄다
(DASH.fw/fh/lw/lh 축소, lift 낮춤). 재추출이 아니라 그리는 배율만 바꾼 것.
거리 고정으로 재는 zone 테스트는 DASH.min=1e9로 경공을 끈다.
**폰트**(v2.42): **주아(Jua)** 로 통일(--fw·--fb) — 둥글고 친근한 게임 폰트.
Do Hyeon 폴백. 캔버스 텍스트도 ctx.font에 Jua. 온라인 로드, 오프라인 시스템
대체. (v2.40의 붓글씨 Gugi는 "폰트 구림" 피드백으로 교체. 메이플 폰트는
라이선스·용량 때문에 임베드 불가라 사용자 확정으로 주아 대체.)
**보스 등장**(v2.40): SUMMON.side 150→92 — 등장 거리 ≈ 전투 대치 거리라
주인공이 거의 안 움직이고 등장 구도 그대로 싸운다("카메라 튐" 해소).
문은 등장 후 SUMMON.fade 동안 아래로 가라앉으며 옅어져 어둠에 스러진다
(S.gateT, drawGateFade — source-atop 사각 자국 없이 알파+타원 그림자로).
**등장 흐림·죽음**(v2.42): 소환·문 페이드 동안(summonT·gateT) 몸 뒤 경지
기운을 끈다 — 3.6초 정지 시 흰 안개가 짙어 캐릭터가 흐려 보였다("등장 때
흐려짐"). beginSummon에서 만회복 — 낮은 HP로 진입하면 등장 직후 순삭됐다
("보스 나오기 전에 죽음"). 단계 진입은 enterStage가 이미 회복시키니 등장
직후에도 죽으면 성장 부족(밸런스)이다.

은자가 첫 재화로 들어갔고(처치 드랍 + 보스 첫 격파 + 오프라인 정산),
표기는 글자 대신 **은원보 픽셀 아이콘**(assets/silver.png, coin() 헬퍼 —
v2.7.1, "글 말고 아이콘" 피드백). HUD·수련·무공·복귀·기연 카드 공통.
v2.30부터 가운데 6×6 네모 구멍을 파서 **엽전 모양**이다(사용자 요청 —
좌·상 둘레 그늘·우·하 둘레 밝게, 파인 면 표현).
**수련**이 첫 소비처다 — v2.1에서 8종으로:
공격·체력·회복(+2%/Lv) · 공격 속도·치명타·치명 피해·이동 속도·은자 획득(점근식).
스텟마다 비용 기초(cb)·성장률(cg)이 다르다 — 가치가 큰 것이 비싸고 가파르다.
구매 배수 토글 x1·x10·x100·MAX — 배수 줄은 스크롤해도 상단 고정(v2.5).
정액 배수(x10 등)는 **정확히 N개 값**을 표기하고 모자라면 비활성 —
"살 수 있는 만큼"으로 개수가 흔들리면 금액이 이상해 보인다는 피드백.
MAX만 유동(×개수 표기). 꾹 누르면 연속 구매.
스텟 이름은 쉬운 말로 쓴다(용어 어렵다는 피드백). 무협 맛은 설명 문구로만.
상한은 최고 경지 × 10. 점근식은 asym:[최대,절반점]이라 최대치를 못 넘는다.
공격 속도는 정권 애니 재생·판정·쿨다운을 함께 당긴다(40-step·30-combat).
치명타는 노란 숫자로 뜬다. 8종 곡선은 sim으로 검증 — 진행 속도는 v2.0과
동일하고 후반 은자 적체가 해소됐다(docs/QA-밸런스.md 절차).

**장비가 들어갔다** (v2.66 → v2.67 도감형 → **v2.70 표준형**, 사용자: "흔한 방치형처럼") —
세 자리(무기·방어구·장신구), 16종×5등급(일반·고급·희귀·영웅·전설) 80종. 처치 드랍(EQUIP.dropCh
5%, 보스 100%, 등급은 구역별 가중 gradeW)이 주머니(S.inv[종류][등급] 개수)에 쌓인다. **합성은
수동**(같은 것 3개 → 한 등급 위, 일괄 합성 버튼). **아이템마다 레벨**(S.itemLv, 은자, 등급별 상한
20~100, 레벨당 +3%) — 장착 효과·보유 효과가 둘 다 레벨을 탄다. **보유 효과** = 얻어 본 아이템
(S.codex 비트)마다 장착 효과×codexRate 영구 가산 → 안 끼는 것도 강화할 이유. 자동 장착 버튼은
장착 효과(레벨 반영) 최대로. 종류별 부가 효과(kinds[2], subRate). 효과 전부 %.
패널: [무기][방어구][장신구] 탭 · 낀 것 · 일괄 합성/자동 장착 · 등급별 아이템 카드(누르면 상세:
장착·강화·합성). 데이터 EQUIP(00-data)·모듈 68-equip.js(eqBonus·eqGain·eqMerge/eqMergeAll·
eqAutoEquip/All·eqWear·levelItem·rollDrop·패널)·저장 S.equip/inv/itemLv/codex(옛 eqLv·장비 lv는
낀 아이템 레벨로 이월)·sim spend(일괄 합성→자동 장착→낀 것 강화)·검증 equiptest. 아이콘 eq_* 16종.
오프라인엔 드랍 없음(보수적). **무기 자리는 무림 무기 체계다**(v2.70.3, 사용자 확정) — 권갑(권)·검·도·창·봉·부채(v2.70.4 철구 삭제·철선→부채). 맨주먹
계열은 권갑(拳套) **한 종류**(권은 분류이지 여섯 종류가 아니다). 권갑 아이콘 eq_fist 대기. **아이콘 없는 종류는 화면·드랍에서 빠진다**(v2.70.2, eqKinds —
에셋이 오면 저절로 등장, sim은 assets/ 목록으로 ASSET 스텁). **시작 장비**는 부팅 때 빈 자리에
일반 하나(EQUIP.starter, eqStarter) — 무기는 권갑뿐이라 아이콘이 올 때까지 **맨손 시작**(v2.72.5). 등급 줄은 일반이 위. **문구는 한 줄에 하나**(v2.70.1, "가독성이 떨어져 줄바꿈 필수") — 무공 artFxLines·
수련 .trv·장비 .eqlab·토스트 `\n`(pre-line). 새 설명 문구도 이 규칙을 따른다.
**무공이 들어갔다** (v1.14) — 초식 5(자동 시전·절차 이펙트) + 심법 5(% 증폭)
+ 기연 전용 2(구양신결·건곤이형, 표에만 보임). 습득 = 경지 도달 + 은자.
**동시 시전 금지** (v2.15, 사용자 확정) — 한 번에 초식 하나만: 시전 중이거나
숨 고르기(CASTQ.gap 0.35초) 중엔 쿨이 차 있어도 기다린다. 쿨다운은 그동안에도
돌아서 밸런스 영향은 없다(sim 확인). **스킬창**(#sbar, 우하단) — 익힌 초식이
입체 원판 슬롯으로 보이고(v2.51 단장), 쿨다운은 시계 방향 원형 스윕
(conic-gradient, 60-ui가 각도 갱신)+초 숫자로 걷힌다. 쿨 중엔 한자 숨기고
숫자만(.cooling), 준비되면 문파색 발광 한자만(겹침 제거). 자동/수동 알약은
수동일 때 호박색. 건곤이형도
표시(피격 발동이라 쿨만 보임). 검증은 artstest.
**무공 탭 = 일반 스킬창, 트리 = 스킬 심화창** (v2.54, "노드는 방치형엔 번거롭다
→ 무공 배우기는 일반 스킬창 탭으로, 노드 업그레이드는 심화창으로") — 무공 탭이
다시 **63-arts 평면 타일표**다: [초식][심법] 탭으로 나눠 배우기·연마·돌파를
그 자리에서(은자+경지). 트리(문파 무공도)는 **스킬 심화창**(#dpanel)으로
강등 — 무공 탭 우상단 '스킬 심화' 버튼(#adeepen)으로 연다. 67-treepanel은
openDeepen/closeDeepen/deepenHud로 #dbody에 그린다(옛 openArts→심화로 이관).
**스킬 심화 특성**(v2.55) — 배운 무공에 쿨감·위력·사거리·증폭 특성을 무공점으로
켠다. 데이터 TRAITS(00-data, 초식 cdcut·power·reach / 심법 amp), 엔진 traitBuy·
traitMul·traitCdcut, 전투 반영은 30-combat(쿨·피해·회복·사거리)·artMul(amp),
UI는 67-treepanel(renderTraits, 카드+칩). 배운 무공에만·되돌리기 없음. 저장
S.traits. sim spend에 특성 sink. 검증 artstest.
**문파 무공도 접기**(v2.55.2, "심화 탭 둘 다 필요하냐 + 노드 번거롭다") —
심화창은 **스킬 특성 하나**만. 트리(문파 무공도)는 화면·저장·sim에서 뺐고
무공점은 전부 특성에 쓴다. 로드 시 S.tree 안 되살림(옛 노드 무공점 환급),
treeBonus는 빈 트리라 0. **트리 엔진(66-tree·65b-treedata·트리 렌더)은 잠자게
보존** — 되돌릴 수 있다. 무공 동선: 배우기(은자)→연마·돌파→심화 특성.
**문파 무공도(스킬트리)** (v2.47) — 문파 탭 + 거미줄 노드망.
데이터 TREEDATA(src/65b-treedata.js, 8문파 182노드), 엔진 66-tree.js
(treeAlloc/treeBonus/skillPts — 경지 성당 +2). 패시브 노드는
treeBonus로 전투 수식에 합산(수련과 별개 축). **무공 마디(eff.art)는 트리로
습득 안 됨**(v2.54.2 "배우기도 전에 쓰네") — 트리는 무공을 주지 않고 배운
무공을 '익힘'으로 비추기만 한다(treeArtNode·treeGift, treeApplyArt 폐지·
treeAlloc/Avail/Dealloc이 거부). 안 배운 마디는 "무공 탭에서 먼저 배운다".
옛 저장의 트리 부여 무공은 treeReapply가 S.arts에서 회수(기연·구매는 유지).
교차 키스톤(강유상제 등)은 두 문파 노드를 다 익혀야 열린다. 검증은 treetest.
신규 심법 6종 편입, 액티브 3종 보류(docs/트리안/신규무공.md·설계-스킬트리.md).
**배우기 편의**(v2.50, "트리라서 배우기 힘들다") — 노드 선택 시 **경로 일괄
습득** "여기까지 익히기 · N마디 M점"이 안 익힌 조상 마디를 한 번에 켠다
(need가 all-of라 선택지 없음 → treeClosure/treeAllocPath, 67-treepanel). 지금
익힐 수 있는 마디는 **맥동 고리**(.tpulse), 선택 노드까지 켜질 경로는 트리에
밝게 미리보기. 단일 노드는 그대로 "익히기". artsHud는 무공점 바뀔 때만
재렌더(v2.49.2 — 매 프레임 재렌더가 클릭을 먹통 만들었다).
**트리 단장·완화**(v2.52, "트리창도 예쁘게·안 배웠는데 씀·배우기 김·주요노드
적음") — 익힌 길은 두 겹(넓은 빛+가는 밝은 선), 익힌 무공·비전 마디는 후광+
유리 광택으로 이정표처럼 빛난다(mixCol로 밝은 문파색, 랜드마크 반지름 키움).
성급당 무공점 1→2(SKILLTREE.ptsPerStar — 같은 경지에서 두 배로 익힘, sim
확인·구역 진행 그대로). 주요 노드 **개수** 자체를 늘리는 건 별도 콘텐츠 작업.
**기연 전수 표시**(v2.52) — 은거기인이 무공을 직접 전수하면 S.arts엔 있고
트리엔 없어 "안 배운 마디를 실전에서 씀" 불일치가 났다. treeGift(그 무공의
트리 마디를 전수받음으로 침)로 트리에 익힘 표시 — 금 점선 고리 + "기연 전수".
무공점 안 들고(skillPtsSpent는 S.tree만) 되돌릴 수 없다.
**오토/수동 모드**(v2.42, 사용자 요청) — S.skillManual. 자동은 stepArts가
알아서 시전, 수동은 쿨만 돌고 스킬창을 눌러 castByHand로 시전한다. 건곤이형
(반격형)은 피격 발동이라 모드 무관. 스킬창 왼쪽 [자동/수동] 토글, 수동+준비
초식은 맥동(.tap). 저장됨. 내공 없는 수동은 "공짜 스킬 손시전"이라 보류했던
설계(v2.2)와 상충하나 사용자 명시 요청 — 내공 도입 때 소비·보너스로 재설계.
표에는 전 무공이 조건과 함께 보인다(사다리 패널과 같은 철학). 심법은 %라
수련(고정치)과 역할이 안 겹친다. 숫자는 00-data.js의 ARTS.

**무공 숙련도** (v1.20, 사용자 확정) — 쓸수록 오른다: 초식은 시전 횟수,
심법은 지닌 채 처치한 수(오프라인 포함). 게이지(40×3^성)가 차면 은자
(습득비×3^성)로 **성 돌파**, 성당 효과 +25%, 상한 4성. 돌파하면 숙련은
0부터(이월 금지 — 벽이어야 한다). 돌파에 재료·기연 조건은 재료 시스템이
들어오면 얹는다. 숫자는 MASTERY.
탭바: [무공][수련][장비][사냥터].

**무공 연마** (v2.2) — 익힌 무공을 은자로 레벨업, 레벨당 효과 +1.5%.
상한 = 성×10(최대 40). **돌파 조건 = 연마 상한 도달 + 숙련 게이지 만충
+ 은자** (v2.7, 사용자 확정: "레벨이랑 횟수 다 차야 업글") — 갈고,
손에 익히고, 그다음에야 성을 넘는다. 돌파하면 연마 상한이 열린다.
비용 = 습득비×0.25×1.2^(Lv-1). artEff = (1+0.015(Lv-1))×(1+0.25(성-1)).
타일 배지 ↑(연마 가능, 파랑)이 ▲(돌파)와 +(습득) 사이 우선순위로 뜬다.
기연 무공도 v2.6부터 실효가 있다: **구양신결**(심법 — 공+20%·체+20%·
회복+30%), **건곤이형**(반격형 초식 — 맞는 순간 받은 피해 50%를 흘리고
3배로 가장 가까운 적에게 되돌림, 14초 쿨, 자동 시전 없음·피격 발동).
cost 20만은 연마·돌파용(습득은 여전히 기연만). 태극 원반 연출(gshield)
+ 시전 동작(castg)은 sheets/geongon_fx1·2.png에서 추출.
효과 표기는 성이 반영된 실효값 + "돌파하면 →" 예고(v2.1 — "성이 올라도
안 좋아져 보인다"는 피드백). 타일도 v2.1에서 상태가 보인다: 익힌 건
문파색 바탕·테두리, 안 익힌 건 흑백, ▲ 돌파 가능(금색 점멸)·+ 배울 수
있음(청록) 배지 — "뭘 할 수 있는지 안 보인다"는 피드백.

**기연이 들어갔다** (v1.18) — 공짜 랜덤이 아니라 누적의 정산(조사 결론·
장무기 공식). 인연(S.karma)이 처치(+구역배율)·보스(+120)·**쓰러짐(+60,
고난이 씨앗)**으로 쌓이고, 차면 단계 진입 순간 기연 카드가 뜬다:
낡은 비급(은자) · 천년 영약(회복+수련치) · 은거기인(미습득 무공 무료 전수)
· 실전 비급 조각(4회차부터, 3개 모으면 구양신결/건곤이형 해금).
회차마다 필요 인연 ×1.4. 오프라인에도 쌓이고 복귀 카드에 "✦ 기연" 예고.
인연 게이지는 일부러 안 보여준다 — 놀라움이 기연답다. 숫자는 FATE.
기연 카드는 5초, 오프라인 복귀 카드는 6초 뒤 **스스로 진행**된다
(v2.5, FATE.autoSec·OFFLINE.autoSec — 팝업 누르기 귀찮다는 피드백).

**경지가 들어갔다** (v1.12, 표기 v1.13) — 처치로 쌓이는 수련치(S.rexp)로 오르고
무한하다. 삼류→이류→일류→절정→초절정→화경→현경→생사경→자연경 (각 1~4성),
그 뒤 신화경 1성·2성·… 세부 단계는 숫자 성으로 쓴다 — 초입·소성 같은 말은
순서가 안 읽힌다는 피드백. HUD의 경지를 누르면 전체 사다리 패널이 열린다
(용어의 해법은 쉬운 말이 아니라 "사다리에서 내 위치가 보이는 것").
자동 성장(GROW)과 수련 상한이 모두 경지 기준이다.
단계(lv)는 이제 구역 진행도일 뿐 힘과 무관하다. HUD 좌하단에 경지·진행% 표시.
테스트 단계 이동은 seedExp()로 걸맞은 수련치를 채워준다 — 안 그러면 못 버틴다.

`00-data.js`의 `TEST = true`가 켜져 있다. **출시 전에 꺼야 한다.**
테스트 전용은 전부 `TEST` 게이트 + `[테스트 전용]` 주석으로 표시해 뒀다
(한 번에 걷어내기 위함). 좌하단 **시험** 버튼 하나로 모아 놨다 —
배속 x1~x100(`70-main.js`의 `TESTSPEED`, 같은 dt로 여러 번 step),
강제 쓰러짐, 저장 초기화. 단계 이동 버튼만 사냥터 패널 안에 있다.

**눈에 보이는 성장이 들어갔다** (v2.3) — 사용자가 뽑아준 AI 시트 6장
(sheets/hero_fx·pagong_fx·whirl_fx·baekbo_fx·bungsan_fx·hwalin_fx)에서
추출. 숫자는 HFX, 검증은 fxtest.js.
- **경지 기운**: 서 있거나 걸을 때 몸 뒤에 안개가 돈다. 절정 흰 → 화경 금
  → 생사경 청 → 신화경 자주 (assets/aidle_w·g·b·p, 심법 시트의 안개만
  분리해 4색으로 구움). 심법별 기운은 안 쓴다 — 여러 개 겹치면 난장판
  (사용자 동의). 색은 경지 하나가 말한다.
- **기본공격**(v2.49 각도별 발차기 → v2.71 시트 교체) — ATKMOVES `[{key,need}]` = **양주먹(punch,
  need0)·옆차기(kickside, need5)·돌려차기(kickround, need10)·뛰어차기(kickhigh, need15)** 4종.
  발차기 3종은 sheets/hero_kick3.png(3줄×6칸)에서 hero_kick3.py로 뽑는다(v2.76.1 — 아래 v2.49 설명은 옛 시트 이력).
  **gridless 기준 컷(stand_cell)은 곧게 선 자세로** — 내지름·챔버처럼 낮은 자세를 기준으로 잡으면 다른 시트와 배율이 어긋난다.
  **무기별 무브셋**(v2.72) — WEAPONMOVES[무기 종류]가 있으면 그 무브셋(검 = 찌르기·베기·회전베기 hero_sword2 · 부채 = 휘두르기·회전·찌르기 hero_fan2 · 도 = 베기·내려찍기·회전베기 hero_saber2 · 창 = 찌르기·쓸기·회전 hero_spear2 · 봉 = 휘두르기·쓸기·회전 hero_staff2 — v2.72.4로 무기 5종 완성),
  없으면 맨손 ATKMOVES. heroWeaponKind()가 S.equip.weapon.k를 본다. **v2.75부터 무기 동작은 전부 need 0**(든 순간부터
  3동작 순환), 맨손은 주먹 5종+옆차기 2종이 처음부터(v2.76.2 — 새 시트 6동작 + 옛 권기 정권·옛 발차기 5동작 = 11종, **옛 동작은 버리지 않고 합친다**)·돌려차기 5·뛰어차기 10 — 사용자: "스타일마다 3가지 이상이어야 실감". 새 무기 시트가 오면 WEAPONMOVES에 얹고
  HFX.aw·loadImg만 더하면 spritetest·fxtest가 WEAPONMOVES를 돌며 자동 검사한다. atkPool()=열린
  동작, heroAttack이 P.atkMove로 돌려 쓰고 P.atkKey로 이번 타 결정(삼류=양주먹만,
  이류부터 옆차기, 초절정부터 높은차기 — 성급 오를수록 발차기 각도가 는다).
  **주먹은 3종**(v2.76 — punch 정권·punchdbl 연환권·punchup 승룡권, 사용자 시트 hero_punch4.py. 두 판 교대
  punch/punchb·P.atkAlt는 폐지, 동작 키가 곧 스트립). 옛 권기 정권(hero_punch3)은 raw/punch3_old 보관.
  **잔상(반투명 분신) 컷은 마젠타 시트에서 못 뽑는다** — 잔상 픽셀이 (224,0,224)라 배경 판정에 먹혀 윤곽만 남는다.
  잔상이 필요하면 시트 프롬프트에 "afterimage in solid non-magenta color"를 넣거나 코드 고스트로 만든다.
  **옛 시트에 쓸 만한 컷이 있으면 버리지 말고 조합한다**(사용자 확정) — 새 시트가 더 나쁠 수 있다. 발차기는 각기 hero_kickside(폭 54)·
  hero_kickhigh(폭 60), 전 공격 4컷·임팩트 index2(HITFRAME 2 유지 위해 4컷 리샘플,
  마지막 컷=완전히 뻗음). 높은차기는 든 다리가 높아 캔버스(51) 위 여백 3px 남게
  배율을 가장 큰 프레임 기준으로 줄였다. spritetest에 발차기 2종 명시 검사 추가
  (ANIM에 없어 여태 검사 밖이었다). 사용자 시트 c0f865d3("발차기도 각도별로 있어")에서 추출 —
  옆모습 좌→우 시퀀스라 뻗는 다리가 옆칸으로 붙어, 행별 덩어리 라벨링으로 분리하고
  높은차기 붙은 컷은 열밀도 골(x466)에서 갈랐다. 배경판정 통일(초록이 적·청 둘보다
  낮음)로 어두운 마젠타까지 걷고 몸의 따뜻한 검은 외곽선은 보존, 축소는 엣지 확장 후
  리샘플(링잉·마젠타 번짐 차단). **declutter**(1px 침식→최대 덩어리→dilate)로 옆칸
  발 조각이 머리 위 검은 막대로 붙어 온 것을 끊었다(v2.49.1, "검은선" 제보). 옛 kick.png 제거. 화염 발차기류는 뺐고(v2.48),
  cresckick·burstkick·flykick·firekick 에셋은 초식 스킬 후보로 보관(ATKMOVES에는
  없음). 새 동작은 목록에 need와 함께 얹으면 는다. 검증은 fxtest(무브셋 3종·각
  스트립 제 폭 렌더·3종 cycle).
- **붙는 데드존 HERO.hold(7)**(v2.48) — 붙은 뒤 stopD+hold 안에선 안 걷는다.
  쿨다운 중 경계에서 idle↔run 깜빡이던 "걷는 현상" 제거. 40-step 이동/공격 분기.
  기존 권기 정권(katka/katkb)은 기본공격에서 물러남(에셋 보존, 성장은 경지 기운·
  타격 이펙트). 질주 폭은 44로(HFX.aw.run — 보폭·옷자락 좌우 잘림 방지).
- **(구) 권기 정권**: **오른손(katka)·왼손(katkb) 스트립 교대**였다
  (v2.23 재컷 — hero_fx 시트의 두 정권 줄, 4컷×2판, 폭 70). v2.45에서 기본공격이
  양주먹·발차기로 바뀌며 물러남. **맨손 판(atka·atkb)은 폐기**(v2.36,
  raw/hero_bare/ 보관) — 권기 판에서 파란 권기만 지웠더니 그 안에 있던
  주먹이 배 앞 살덩이로 떠 옷이 뚫린 듯 보였다("옷이 이상해" 피드백).
  시트에 권기가 구워져 있어 맨손 합성이 무리. 그래서 삼류부터 권기 정권을
  쓴다 — 경지 성장 표현은 몸 뒤 경지 기운(절정부터)이 담당한다.
  v2.18의 주먹 빛무리(kfist)·주먹 끝 흰 점(FIST)도 폐기 — 판에 주먹이 다 있다.
- **이펙트 팩**(v2.31, "이펙트가 조잡하다" 피드백): FXD — 가산 합성
  wave 충격파·rays 속도선·sparks 파편·flash 섬광, fxBlast() 묶음.
  평타·치명타·광역 초식·탄 명중·보스 등장/스킬에 붙어 있다. 그라디언트
  없이 동심원·픽셀 파편이라 jsdom 스텁 무해. 보스 스킬 'shock'은
  렌더러가 없어 여태 안 보였다(교체됨).
- **네온 타격 연출**(v2.61, "픽셀헌터 키우기 이펙트" 참고 — 도트 위 네온 발광
  핵앤슬래시): 참격 호(slash)·피격 브라이튼(hitflash: 스프라이트를 'lighter'로
  n겹 덧그림, source-atop 금지)·치명 링(critring)+미세 경직(hitstop — 70-main
  loop에서 step만 멈춤, sim 무관)·탄 잔상(drawTrail, fly 탄·권기·지풍, 술병 제외)·
  피해 숫자 팝(dmgpop)·castGlow. 수치 전부 FXD. **createRadialGradient는 jsdom
  스텁이 없어 테스트가 터진다 — 쓰지 마라.** 검증 fxtest.
- **초식 시전 동작**: 액티브 5종 전용 cast 스트립 6~9프레임(HFX.cast,
  [에셋·폭·수]), castFps 16. **컷 수는 숙련 성에 비례**(v2.4, castStar):
  1성은 절반쯤(뼈대), 4성이 전체 — 처음·끝 컷은 지키고 중간을 덜어낸다
  (castN/castFrame). 파공권 권기 탄(pashot)·암향지 지풍 빔(bshot)은
  **날아가는 방향으로 기울어** 난다(왼쪽은 거울 뒤 반전각 — 뒤집힘 방지).
  P.castT·P.castK(40-step·30-combat). **절차 발광**(v2.5, HFX.glow):
  무공 색 동심원 빛무리를 시전 손끝과 탄(파공권 몸통·지풍 촉끝)에 가산
  합성으로 얹는다 — 스프라이트의 작은 점이 안 보인다는 피드백.
- 건곤이형 시트 2장은 v2.6에서 통합됐다(반격 연출·시전 동작).
  붕산장 시트의 화염 기운 16프레임은 미사용(후보).

저장·오프라인 진행은 들어갔다(`65-save.js`, v1.9 → 정산 개편 v1.20).
**오프라인엔 단계를 넘지 않는다**(사용자 확정) — 지금 단계 제자리 사냥으로
은자(전부)·수련치(시간 기준: 8시간 = 승급 3회 분량)·인연(필요량 1.5배 상한)
·심법 숙련만 쌓인다. 진행은 돌아와서 직접. 상한 8시간, 효율 온라인의 70%.

### 아직 없는 것 (중요한 순서대로)

1. **남은 성장 축** — 문파가 없다(장비는 v2.66에 들어갔다). 발동 3모드는 내공 재화와 함께
   (설계 방침 참조). 오프라인 모델은 초식을 안 치므로 약간 보수적 —
   재화 환산 때 같이 다시 잡는다.
2. **눈에 보이는 변화 — 남은 것** — 경지 기운·권기·시전 동작(v2.3),
   **구역별 분위기**(v2.8: 죽림 댓잎·폐촌 낙엽·동굴 어둑함+반딧불·설산
   눈·천산 구름 그림자 — AMB, 무상태 입자라 저장·sim 무관),
   **배경 소품 스프라이트**(v2.60: 5구역 전부 완성 — 사용자 제미나이 시트 추출.
   죽림 8[bambooprops.py] + 폐촌 10·동굴 8·설산 8·천산 8[props_extract.py, 34종].
   PROPS kind:'sprite'·drawProps/propSprite가 넓은 격자에 가중 랜덤·바닥 중앙 렌더,
   인물 뒤 층·저장·sim 무관. 추출은 어두운 보라 테두리로 격자 검출→칸 안 마젠타+
   보라그림자 제거→칸 조각 병합→tight bbox, review/props_*.png로 눈검사.
   v2.57~58.2 절차 지형·소품은 전부 폐기 — 사용자: "내가 그린 건 쓰지 말고".
   시트 프롬프트 docs/프롬프트-지형.md). 날씨 입자(AMB: 눈·재·낙엽·반딧불·
   구름 그림자)는 계속 절차. 남은 축: 무기(장비가 생기면 손이 바뀐다).
   **타이틀 화면**(v2.65): 사용자 배경 일러스트+주인공 초상, #title, closeTitle 뒤 복귀 카드.
   **UI 아트**(v2.62~63, 사용자 시트 — UI류는 픽셀 아니어도 됨): 기연 카드 일러스트 4(#fart,
   FATE.art)·수련 스텟 아이콘 8(.tico)·장비 아이콘 16(eq_*, 장비 시스템 대기)·무공 메달 16
   (art_*, 스킬창 .gi/타일 .gi, 없으면 한자)·문파 엠블럼 8(sch_*, 타일 .se)·보스 초상 5
   (boss_*, 등장 문구 컷인 FACECUT/BOSSFACE). 프롬프트 docs/프롬프트-아이콘.md.
   **상단 원경 배경**(v2.61.6 5구역 완료 — 프롬프트 docs/프롬프트-배경.md. 시트는 폭
   1024로 축소+PNG-8 팔레트 저장: 2000px 그대로 넣으면 빌드 16MB 한도를 친다. jsonl
   복원 때 내가 Read한 스크린샷도 이미지로 섞이니 크기·종횡비로 걸러라): 화면 위 34%에 구역별 원경을 가로
   타일링·카메라 x의 0.22배로 흐르게(패럴럭스) — BACKDROP(00-data)·drawBackdrop/
   v2.61.7: 지평선(hz 0.30)과 그림 높이(hDef·h[zone], 동굴 0.21)를 분리, 위 남는 하늘은
   sky색. 페이드는 **그림 알파를 계단으로 빼는 디졸브**(fade 64·fadePow) — 평균색 칠하기는
   평평한 띠가 생겨 폐기. bg_extract가 시트 아래 균일 땅색 띠를 자동 제거(디졸브엔 해롭다)./
   horizonY(50-render)·bg_extract.py·docs/프롬프트-배경.md. 아래 40px는 땅색 알파
   계단(40단, 16단은 줄무늬)으로 지평선 잇고, 지평선 위 소품은 컬링. 이미지 없으면
   안 그림. 시트 오면 `python bg_extract.py <zone> [--patch=x,y,w,h] --pad=48` → 자동 로드.
   **--pad는 필수**(v2.69.2): 시트는 집·나무·바위가 맨 아랫줄까지 닿아 있어 디졸브가 그걸 녹인다 —
   아랫줄 땅 픽셀만 섞어 만든 띠를 덧대 그 띠가 녹게 한다(줄을 그대로 이으면 벽이 물에 비친 듯 줄무늬).
   **v2.63.6 원경 3대 사고**: 디졸브는 px 고정이 아니라 그림 높이 비율(fadeR 0.14 — 64px
   고정은 폰에서 그림 3/4를 녹여 집이 잘렸다) · 팔레트 저장은 반드시 디더(quantize method=2
   무디더는 18~72색으로 뭉개져 줄무늬) · 시트→화면 축소는 nearest 금지(backdropScaled 캐시).
   **바닥 텍스처**(v2.61.1, 죽림 완료): 32px 타일이 아니라 큰 무봉 그림 한 장을 카메라와
   1:1로 2D 타일링 — GROUNDTEX(00-data)·drawGround(50-render)·ground_extract.py(가장자리
   48px 크로스페이드 무봉화·--patch). 저대비 시트여야 가독성이 산다(프롬프트에 명시).
   있으면 이동감 격자 생략. 위쪽은 원경 페이드가 덮어 지평선으로 녹는다.
3. **몬스터 — 5구역 보스 완성(v2.32 뇌운신장)**. 선택 잔여: 파수꾼(죽림
   카탈로그 ref5). 대표 컷·프롬프트는 `docs/프롬프트-몬스터.md`.

### 설계 방침 (조사로 정한 것)

- **주인공상은 김용 「의천도룡기」의 장무기형이다** (사용자 확정).
  선량하고 꾸밈없는 청년이 착실히 크다가 기연(奇緣)으로 도약하는 서사.
  정파·사파를 가리지 않는 품. 성장 설계·이벤트·문구를 여기에 맞춘다.

- 일일 숙제를 만들지 않는다. 하루 한 번 **운기조식**으로 쌓인 내공을 어디에
  넣을지 고르는 것 하나면 된다. 1분이면 끝나고 안 해도 손해가 크지 않다.
- 프레스티지로 되돌리지 않는다. **경지는 쌓이기만 하고 무한하다** (사용자 확정).
  재료는 처치 수련치 — 추후 다른 재료(운기조식 등)를 더할지 다시 상의한다.
  사다리: 삼류~자연경 9경지 × 1~4성, 이후 신화경 N성 무한. 경지 이름은
  장르 표준으로 유지하고 세부 단계만 숫자 — 사다리 패널로 문턱을 낮춘다.
- 광고를 넣지 않는다.
- 은자 소스는 **처치 드랍 + 보스 첫 격파 보너스 + 오프라인 정산** 세 가지.
  단계 클리어 보너스는 넣지 않는다.
- 액티브 무공 발동 3모드(자동/반자동/수동)는 **내공(마나) 재화가 들어올
  때까지 보류** (사용자 확정, v2.2) — 소비 자원이 없으면 수동은 "공짜
  스킬을 손으로 누르는 것"일 뿐이라 의미가 없다. 내공이 들어오면
  내공 소비·수동 보너스와 함께 다시 설계한다.
- **문파 구성 = 유명 문파 그대로 + 우리 창작 문파** (사용자 확정, v1.17).
  소림·무당·화산·아미·개방·당문·마교는 그대로 쓴다 — 실존 지명·일반명사라
  자유롭고(화산귀환도 그대로) 인지도가 최고다. 지역 연고 창작 문파(야우문
  등)는 폐기했고, **청죽문**만 우리 문파로 남았다(주인공의 죽림 뿌리 계보).
  앞으로 우리 문파를 더 만든다. 주인공은 장무기형이라 한 문파에 안 갇히고
  정·사 불문 여러 계보를 모은다 — "가입"이 아니라 "인연이 닿으면 배운다".
  확정안의 문파 축(제자·오프라인 수익)은 "내 문파 경영"으로 별개.
- **무공 작명 규칙** (조사 확정, `docs/조사-무공작명.md`): 김용 고유
  창작명·소림 72절기 실명은 쓰지 않는다(소송 사례 있음). 문파명은 예외 —
  실존 지명·일반명사라 자유. 접미사 문법(초식 ~권·장·지·참 / 심법
  ~심법·공·결 / 신공 ~신공)은 장르 공용으로 쓰고 수식부를 신조한다.
  초식명은 시전 시 외치므로("파공권!") 3~4자가 좋다.
- 재화는 필요해질 때 하나씩 추가한다 (은자 → 비급·내공 순).
- 코어는 끝까지 "자동 전투를 구경하는 것"이다. PvP를 붙이더라도 코어를
  밀어내면 안 된다.

### 밸런스 — v2.0 지수 재설계 (콘텐츠 소모 속도)

"몬스터가 약하다"의 진짜 뜻은 **진행이 빨라 콘텐츠가 금방 끝난다**는
우려였다(사용자). 선형 몹은 벽이 안 생겨 60분에 천산까지 갔다.

- **전역 단계 g(1~50)가 난이도의 축**: 적 체력 22×**1.46**^(g-1) (v2.68 — 1.30은
  주인공 승급 1.31과 같아 벽이 없었다. 사용자 확정 "하루에 천산 초입"), 피해 3×1.19^(g-1),
  처치 목표 24+7g. 구역 배율(ZONES.mul) 폐지 — 전부 DIFF에서 나온다.
  **벽은 "체력이 안 깎여 기다리는 것"이다** — 적 피해를 올리면 쓰러짐만 수백 번 늘고
  진행은 그대로(쓰러짐 벌칙이 약함), 수련치 곡선은 효과 미미(v2.68 실험 X/Y/Z).
- **주인공은 승급당 곱연산**: 피해 ×1.31(적과 등가)·체력 ×1.31·회복 ×1.31.
  수련 스텟도 전부 %(레벨당 +2%) — 지수 세계에서 고정치는 무의미
- **수련치 수입 1.07^g vs 승급 필요 25×1.30^k** — 수입이 필요보다 훨씬
  완만해야 벽이 선다. 벽에서 벌어서(수련·숙련·기연) 뚫는 게 루프다
- **sim(소비 전략 spend 포함) 실측, v2.68**: 5분 죽림6 → 15분 폐촌2 → 1시간 동굴2
  → 4시간 설산2 → 8시간 설산6 → **24시간 천산 1** → 완주는 며칠+. 쓰러짐 24h에 12.
  탐침(정권/적hp 비율)은 scratch sim에 line()마다 heroDmg()/foeHp()를 찍어 본다.
- sim.js는 24시간 기본(SIM_MIN=분 으로 단축), 30초마다 수련 구매·무공
  습득·돌파를 흉내 낸다. **잔고가 계속 쌓이면 소비처 부족, 계속 0이면
  곡선이 가파른 것** — 후반 잔고가 쌓이기 시작하므로 장비·문파가 다음 sink다

### 알려진 자잘한 것

- ~~강도 공격 그림 0.43초 vs 판정 0.55초 멈춤~~ 해결(v2.55.3, atk fps 7→5.4)
- ~~주술사 스킬 그림 1.09초 vs 시전 1.0초 마지막 컷 누락~~ 해결(v2.55.3, skill fps 5.5→6.2)
- 안 쓰는 에셋: `bandit_dodge0~2`(회피 미구현), `shaman_atk`(m0로 대체됨)
- 마왕 `swing3` 오른쪽 돌기둥이 칸 경계에서 직선으로 끊겨 있다 — **그냥 둔다**
  (사용자 확정, 2026-09-15). 돌기둥이 시트 칸 오른쪽 테두리를 넘어 그려져,
  추출기의 "이웃 칸 조각 버리기" 가드가 정당한 기둥과 이웃 파편을 구분 못 해
  자동 복원 시 다른 23프레임이 마젠타 잔털을 끌어와 회귀함(closing/near 키우면
  마젠타 140→460). 깔끔한 길은 "돌기둥만 떼어내 합성"이나 보스 캔버스 성장
  (114→~130)+데이터까지 건드려야 해서, 실플레이에 거의 안 보이는 흠 대비 과함.

---

자세한 변경 이력은 `VERSION.md`, 짧은 인수인계는 `HANDOFF.md`.
