# 인수인계 — 무협 방치형

## 지금 상태
죽림 5종 + 보스 1종. 5구역 × (10단계 + 보스). 무기 없음, 맨손 정권.
`wuxia.html` 784KB · 테스트 모드 켜짐 (`TEST = true`)

## 시작하는 법
```
npm i                     # jsdom (test.js용) 한 번만
python3 build.py          # → dist/wuxia.html
```

## 구조
```
src/00-data.js    숫자 전부 — 밸런스·적·구역·이펙트 상수
src/10-engine.js  캔버스·이미지·반응형
src/15-audio.js   합성음
src/20-state.js   S(게임) · P(주인공)
src/30-combat.js  공격·피해·투사체·쓰러짐
src/40-step.js    매 프레임 갱신
src/50-render.js  그리기 전부
src/60-ui.js      HUD·사냥터 패널
src/70-main.js    시작·루프

assets/           스프라이트 (파일명 = 키)
*.py              추출 스크립트 (종류마다 하나)
*.js              검증 도구
```

## 반드시 지킬 것
0. **스프라이트를 자르면 먼저 눈으로 검사받는다.** `python review.py [종류]`로
   확대 대조 PNG를 만들어 사용자에게 보이고, 확인을 받은 뒤에 게임에 반영한다.
   `spritetest.js`가 통과해도 '그림이 이상한 것'은 못 잡는다. 이걸 안 해서
   잘린 지팡이·구체·정수리를 세 번 놓쳤다.
1. **숫자는 `00-data.js`에만.** 코드에 상수를 박지 않는다.
2. **파일을 잘라낸 뒤엔 `node lint.js`.** 함수가 통째로 날아간 적이 두 번 있다.
3. **밸런스를 만지면 `node sim.js`.** 감으로 조정하지 않는다.
4. **스프라이트는 한 종류 한 규격.** 동작마다 크기가 다르면 위치가 튄다.

## 검증
```
node lint.js         정의 없는 함수 호출 (가장 중요)
node test.js         브라우저 실행 · 오류
node sim.js          60분 진행
node hittest.js      방향별 명중 (아래쪽 못 때리는 문제 잡을 때)
node sweeptest.js    클리어 연출
node bosstest.js     보스 5종 격파
node shamantest.js   주술사 원거리·마법
node savetest.js     저장·오프라인 진행
node frogtest.js     개구리 혀 사거리·규격
python demonsheet.py 마왕 시트 → 24프레임 (뒤에 demonclean.py 를 돌린다)
python demonclean.py 마왕 테두리 잔재·마젠타 잔털 손질 (--dry 로 미리보기)
python review.py     자른 결과를 그림으로 확인 (자를 때마다 필수)
node spritetest.js   전 스프라이트 규격·잘림 (가장 먼저 돌린다)
python castshot.py   샤먼 두 동작 미리보기 그림
node animtest.js     동작 끊김
node screens.js      기기별 레이아웃
node shot.js [모드]  스냅샷 (boss/skill/slam/summon/sweep/shot)
```

## 최근 고친 것들 (같은 실수 반복 금지)
- **아래쪽 적을 못 때림** → 판정을 발 위치에서 재고 세로 1.55배 (`HERO.atkFlat`)
- **소환 중 캐릭터 사라짐** → 진입 연출 검은 덮개를 0.45초에 걸쳐 걷음
- **주술사 몸이 사라짐** → `m2`는 인물이 아니라 구체 그림. 스프라이트로 쓰지 않는다.
  시전 이펙트(몸 앞에 덧그림)와 날아가는 탄, 두 군데에서만 쓴다
- **개구리 혀가 잘림** → 렌더러가 가로 중앙 기준이라 캔버스가 좌우 대칭이어야 한다.
  몸보다 넓은 적은 `sw`(몸 폭)·`bh`(몸 높이)를 따로 준다
- **개구리 얼굴이 잘림** → 혀를 지워 이동 프레임을 만들려고 '초록의 끝'에서 잘랐다.
  주둥이 끝은 누런빛이라 초록 판정에 안 들어와, 자르는 자리가 얼굴을 파먹었다.
  없는 동작을 합성하지 말고 **시트에 있는 칸을 쓴다**
- **개구리 앞다리가 잘림** → 혀를 줄이려고 세로로 통째 잘랐더니, 그 자리에
  있던 앞발이 같이 잘렸다. **혀만 따로 떼어내 줄이고 몸 위에 다시 얹는다.**
  혀 판정은 살구빛 덩어리 + 열마다 그 높이에 걸친 덩어리(외곽선까지)
- **줄인 프레임이 1px 씩 흔들림** → 프레임마다 딱 맞게 잘라내면 잘리는 자리가
  달라진다. 재단 뒤에는 자르지 말고 공통 캔버스에서 한 번에 정리한다
- **개구리 정수리가 납작** → 배경 판정이 '어둡고 채도 낮은 것'을 다 빼면서
  몸에 붙은 검은 외곽선까지 지웠다. 알파를 만든 뒤 검은 픽셀 쪽으로 2px 되살린다
- **얇은 것은 통째로 배경색에 물든다.** 마왕 촉수가 1~2px 이라 전체가 분홍이 됐다.
  지우면 촉수가 사라진다. **지우지 말고 마젠타 성분만 걷어낸다** (r·b 를 g 쪽으로).
  자주색이 그림인 프레임(폭발·포효 후반)은 분홍 비율 35%로 갈라낸다
- **AI 시트의 칸은 프레임 경계가 아니다.** 샤먼 마법 시트는 가운데 두 칸이
  한 그림이었다 — 기운이 지팡이에서 나와 옆 칸으로 이어진다. 칸마다 자르면
  그림이 두 동강 난다. **칸이 아니라 내용을 보고 자른다.** 마젠타가 아닌
  덩어리를 라벨링하면 이어진 칸이 하나로 붙어 나오므로 그걸로 판단한다
- **샤먼 지팡이 끝이 잘림** → 인물 덩어리 기준으로 칸을 재서 지팡이가 밖으로 나갔다.
  기본 5동작은 시트가 없어 `shamanstaff.py`로 단면 기울기를 재어 이어 붙였다.
  마법 3동작은 시트를 다시 받아 `shamanmagic.py`로 제대로 뽑았다
- 잘림은 눈으로 찾지 말고 `node spritetest.js` 로 잡는다. 그림이 캔버스
  가장자리에 닿으면 거기서 잘린 것이다
- **스프라이트 잘림** → 시트에서 인물 덩어리를 실측하고 여백 8~10px 확보
- **표범 회색 테두리** → 외곽선을 넣지 않는다
- **이펙트가 조잡** → 스프라이트를 겹치지 말고 점→선→면으로 그린다

## 남은 일
- [ ] 폐촌·동굴·설산·천산 몬스터 (지금은 죽림 것 재사용)
- [ ] 성장 요소 (무공 · 장비 · 은자)
- [ ] 난이도 재설계 (성장 축이 들어온 뒤)
- [ ] 저장
- [ ] 사막 구역 (배경 시트 필요)

## 현재 밸런스 (임시)
```
 1분  죽림 3단계    33처치
 5분  죽림 8단계   229처치
10분  폐촌 4단계   443처치
보스 격파 39~69초
```
