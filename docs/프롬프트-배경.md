# 배경 시트 프롬프트 — 원경(상단) + 바닥(전면) (한 블록 = 한 장, 그대로 복사)

두 층으로 구성한다. 그림은 전부 사용자 제미나이 시트, 코드는 안 그린다.

1. **원경(상단 34%)** — 먼 산·탑·안개·구름. 카메라보다 느리게(패럴럭스). 아래 지평선
   띠는 **그 구역 땅색**으로 끝나서 바닥과 이어진다. 하늘은 마젠타(투명 처리).
   렌더 `drawBackdrop`(50-render) · 데이터 `BACKDROP`(00-data) · 추출 `bg_extract.py`.
2. **바닥(전면 전체)** — 32px 타일이 아니라 **큰 정사각 무봉(4방향 seamless) 텍스처
   한 장**. 저대비·오브젝트 없음(소품은 PROPS로 따로 깔림)·기본색=땅색. 카메라와 1:1로
   흐른다. 마젠타 없음(투명 필요 없음, 꽉 채운 그림). 렌더/추출은 시트 오면 붙인다
   (drawGround 교체·`GROUNDTEX`·`ground_extract.py`).

구역 땅색(ZONES.ground): 죽림 `#6a7a52` · 폐촌 `#6b6350` · 동굴 `#474d54` ·
설산 `#a4b3c0` · 천산 `#7f9a86`.

시트가 나오면 자르지 말고 **채팅에 그대로 붙여넣기**(저장 안 되면 jsonl에서 복원).
**원경·바닥 모두 보라·자주 계열 금지** — 원경은 마젠타로 오인, 바닥은 톤이 튄다.

---

## 원경 — 죽림

```
2D pixel art parallax background strip, 16-bit retro JRPG style, one wide horizontal distant-horizon scene (about 3:1 aspect) meant to sit at the TOP of a vertical mobile game screen and scroll slowly behind gameplay. Seamless left-right tiling (left and right edges continue into each other). The empty sky area is solid magenta (#FF00FF) so it can be made transparent; use NO purple or magenta tones anywhere in the artwork itself. Only distant, low-contrast, hazy silhouettes — no foreground detail, nothing in sharp focus, muted and atmospheric so gameplay stays readable in front. The bottom of the image must end in a flat, simple ground band colored exactly #6a7a52 (the game's ground color) so it blends seamlessly into the play field below. Crisp pixel shapes, limited palette, no text, no watermark. Scene: a distant BAMBOO-FOREST valley — faint layered green hills, a misty far bamboo grove silhouette, soft dawn haze. Cool green and pale mist palette.
```

## 원경 — 폐촌

```
2D pixel art parallax background strip, 16-bit retro JRPG style, one wide horizontal distant-horizon scene (about 3:1 aspect) meant to sit at the TOP of a vertical mobile game screen and scroll slowly behind gameplay. Seamless left-right tiling (left and right edges continue into each other). The empty sky area is solid magenta (#FF00FF) so it can be made transparent; use NO purple or magenta tones anywhere in the artwork itself. Only distant, low-contrast, hazy silhouettes — no foreground detail, nothing in sharp focus, muted and atmospheric so gameplay stays readable in front. The bottom of the image must end in a flat, simple ground band colored exactly #6b6350 (the game's ground color) so it blends seamlessly into the play field below. Crisp pixel shapes, limited palette, no text, no watermark. Scene: a distant ABANDONED VILLAGE — silhouettes of ruined tiled rooftops and a broken watchtower on bare withered hills, overcast dusty air, a few dead trees. Muted brown, ash grey and faded ochre palette, desolate.
```

## 원경 — 동굴

```
2D pixel art parallax background strip, 16-bit retro JRPG style, one wide horizontal distant scene (about 3:1 aspect) meant to sit at the TOP of a vertical mobile game screen and scroll slowly behind gameplay. Seamless left-right tiling (left and right edges continue into each other). Any empty area is solid magenta (#FF00FF) so it can be made transparent; use NO purple or magenta tones anywhere in the artwork itself. Only distant, low-contrast, hazy shapes — no foreground detail, nothing in sharp focus, muted so gameplay stays readable in front. The bottom of the image must end in a flat, simple ground band colored exactly #474d54 (the game's ground color) so it blends seamlessly into the play field below. Crisp pixel shapes, limited palette, no text, no watermark. Scene: the far wall and ceiling of a vast UNDERGROUND CAVERN — dark rock strata, rows of hanging stalactites fading into blackness, a few faint cyan crystal glints deep in the dark. Charcoal, slate grey and near-black palette with sparse cyan accents.
```

## 원경 — 설산

```
2D pixel art parallax background strip, 16-bit retro JRPG style, one wide horizontal distant-horizon scene (about 3:1 aspect) meant to sit at the TOP of a vertical mobile game screen and scroll slowly behind gameplay. Seamless left-right tiling (left and right edges continue into each other). The empty sky area is solid magenta (#FF00FF) so it can be made transparent; use NO purple or magenta tones anywhere in the artwork itself. Only distant, low-contrast, hazy silhouettes — no foreground detail, nothing in sharp focus, muted and atmospheric so gameplay stays readable in front. The bottom of the image must end in a flat, simple ground band colored exactly #a4b3c0 (the game's ground color) so it blends seamlessly into the play field below. Crisp pixel shapes, limited palette, no text, no watermark. Scene: distant SNOW-CAPPED MOUNTAIN PEAKS — layered jagged white summits under a cold pale sky, thin drifting snow haze, a faint far line of frosted pines. White, ice blue and soft grey palette, cold and quiet.
```

## 원경 — 천산

```
2D pixel art parallax background strip, 16-bit retro JRPG style, one wide horizontal distant-horizon scene (about 3:1 aspect) meant to sit at the TOP of a vertical mobile game screen and scroll slowly behind gameplay. Seamless left-right tiling (left and right edges continue into each other). The empty sky area is solid magenta (#FF00FF) so it can be made transparent; use NO purple or magenta tones anywhere in the artwork itself. Only distant, low-contrast, hazy silhouettes — no foreground detail, nothing in sharp focus, muted and atmospheric so gameplay stays readable in front. The bottom of the image must end in a flat, simple ground band colored exactly #7f9a86 (the game's ground color) so it blends seamlessly into the play field below. Crisp pixel shapes, limited palette, no text, no watermark. Scene: HEAVENLY PEAKS above a sea of clouds — towering distant pinnacles rising out of a rolling cloud sea, tiny far pagoda silhouettes on the crags, a golden dawn glow at the horizon. Sage green, warm gold and soft cloud-white palette, lofty and serene.
```

---

## 바닥 — 죽림

```
2D pixel art seamless GROUND TEXTURE, 16-bit retro JRPG style, a single large square image that tiles perfectly in all four directions (top edge continues into bottom edge, left edge continues into right edge, no visible seams or repeating hotspots). Top-down-ish view of flat ground seen from slightly above, as in a side-view action RPG play field. LOW CONTRAST and subtle: this is a floor that characters and effects will be drawn on top of, so no objects, no plants standing up, no rocks, no strong shapes, no lighting hotspots — only gentle surface variation. Overall average color exactly #6a7a52. No purple or magenta tones. No text, no watermark. Surface: soft forest floor of short mossy grass with faint dirt patches and scattered fallen bamboo leaves, muted olive green palette.
```

## 바닥 — 폐촌

```
2D pixel art seamless GROUND TEXTURE, 16-bit retro JRPG style, a single large square image that tiles perfectly in all four directions (top edge continues into bottom edge, left edge continues into right edge, no visible seams or repeating hotspots). Top-down-ish view of flat ground seen from slightly above, as in a side-view action RPG play field. LOW CONTRAST and subtle: this is a floor that characters and effects will be drawn on top of, so no objects, no plants standing up, no rocks, no strong shapes, no lighting hotspots — only gentle surface variation. Overall average color exactly #6b6350. No purple or magenta tones. No text, no watermark. Surface: packed dry dirt of an abandoned village lane with faint cracks, dust, sparse dead grass stubble and a few scattered ash flecks, muted dusty brown-grey palette.
```

## 바닥 — 동굴

```
2D pixel art seamless GROUND TEXTURE, 16-bit retro JRPG style, a single large square image that tiles perfectly in all four directions (top edge continues into bottom edge, left edge continues into right edge, no visible seams or repeating hotspots). Top-down-ish view of flat ground seen from slightly above, as in a side-view action RPG play field. LOW CONTRAST and subtle: this is a floor that characters and effects will be drawn on top of, so no objects, no crystals standing up, no boulders, no strong shapes, no lighting hotspots — only gentle surface variation. Overall average color exactly #474d54. No purple or magenta tones. No text, no watermark. Surface: damp dark cave floor of smooth worn stone with faint cracks, subtle gravel grain and a few tiny wet glints, charcoal slate-grey palette.
```

## 바닥 — 설산

```
2D pixel art seamless GROUND TEXTURE, 16-bit retro JRPG style, a single large square image that tiles perfectly in all four directions (top edge continues into bottom edge, left edge continues into right edge, no visible seams or repeating hotspots). Top-down-ish view of flat ground seen from slightly above, as in a side-view action RPG play field. LOW CONTRAST and subtle: this is a floor that characters and effects will be drawn on top of, so no objects, no trees, no rocks, no strong shapes, no lighting hotspots — only gentle surface variation. Overall average color exactly #a4b3c0. No purple or magenta tones. No text, no watermark. Surface: wind-packed snow crust with faint drifts, soft footprint-free undulations and a few subtle icy sparkles, pale cool white-blue-grey palette.
```

## 바닥 — 천산

```
2D pixel art seamless GROUND TEXTURE, 16-bit retro JRPG style, a single large square image that tiles perfectly in all four directions (top edge continues into bottom edge, left edge continues into right edge, no visible seams or repeating hotspots). Top-down-ish view of flat ground seen from slightly above, as in a side-view action RPG play field. LOW CONTRAST and subtle: this is a floor that characters and effects will be drawn on top of, so no objects, no shrubs standing up, no boulders, no strong shapes, no lighting hotspots — only gentle surface variation. Overall average color exactly #7f9a86. No purple or magenta tones. No text, no watermark. Surface: high mountain plateau turf of short wind-flattened grass over pale weathered stone, with faint lichen patches, muted sage green and pale stone palette.
```

---

# 본진 원경·바닥 (v2.94.1, 사용자 "문파별 원경하고 텍스처 바닥 뽑자")

본진마다 원경 한 장 + 바닥 한 장. 위 사냥터 원경·바닥과 **같은 틀**이고 장면 문장과 땅색만 다르다.
땅색(HQZONE.ground — 코드에 넣는다): 개방 `#6e6349` · 소림 `#7a7368` · 무당 `#6c7a7c` · 화산 `#7d6658` · 아미 `#647a5a` · 당문 `#5e5450` · 마교 `#4a4448` · 청죽문 `#6a7a52`(죽림과 같음).
추출: `python bg_extract.py hq_<문파> --pad=48` → `python bg_fix.py hq_<문파>`, 바닥 `python ground_extract.py hq_<문파>`. 에셋 키 `bg_hq_<문파>`·`ground_hq_<문파>` → BACKDROP.keys/GROUNDTEX.keys/avg 에 얹으면 rzone() 이 본진 키로 찾는다.

## 원경 틀 (장면 문장·땅색만 바꿔 쓴다)

```
2D pixel art parallax background strip, 16-bit retro JRPG style, one wide horizontal distant-horizon scene (about 3:1 aspect) meant to sit at the TOP of a vertical mobile game screen and scroll slowly behind gameplay. Seamless left-right tiling (left and right edges continue into each other). The empty sky area is solid magenta (#FF00FF) so it can be made transparent; use NO purple or magenta tones anywhere in the artwork itself. Only distant, low-contrast, hazy silhouettes — no foreground detail, nothing in sharp focus, muted and atmospheric so gameplay stays readable in front. The bottom of the image must end in a flat, simple ground band colored exactly {땅색} (the game's ground color) so it blends seamlessly into the play field below. Crisp pixel shapes, limited palette, no text, no watermark. Scene: {장면}
```

| 본진 | 땅색 | 장면 |
|---|---|---|
| 개방 | #6e6349 | the outskirts of a run-down river town where the BEGGAR SECT gathers — distant crooked shacks and a leaning wooden bridge over a hazy river, a ruined temple gate half-collapsed, drying rags on poles, thin campfire smoke, warm dusty ochre and grey-brown palette |
| 소림 | #7a7368 | the SHAOLIN monastery high on a forested mountain — distant tiered temple roofs and a tall stone pagoda among pine-covered slopes, a long stone stairway climbing into mist, faint bell-tower silhouette, muted warm grey and saffron-tinted haze |
| 무당 | #6c7a7c | the WUDANG mountain peaks — sheer misty crags with Taoist temple halls perched on cliff tops, thin waterfalls, cloud sea below the peaks, a lone crane silhouette, cool blue-grey and pale teal palette |
| 화산 | #7d6658 | MOUNT HUA — knife-edge red-brown rock ridges and a narrow plank path along the cliff, plum trees in faint bloom on the ledges, a small temple on the summit, hazy pinkish-white blossom drift (NOT purple), warm rust and cream palette |
| 아미 | #647a5a | MOUNT EMEI — soft green mountain folds wrapped in cloud, a nunnery with curved roofs half-hidden by cedars, a stone bridge over a mountain stream, gentle morning light, mossy green and pearl-mist palette |
| 당문 | #5e5450 | the TANG CLAN stronghold in a Sichuan gorge — dark stone walls and watchtowers built into a cliff, hanging vines, a narrow rope bridge, greenish poison-mist pooling in the valley, dark slate and murky green palette |
| 마교 | #4a4448 | the DEMONIC CULT's black citadel on a volcanic plateau — jagged basalt spires, a fortress with red banners on a crater rim, faint orange glow of lava fissures and drifting ash, charcoal black and ember-orange palette |
| 청죽문 | #6a7a52 | a small BAMBOO-GROVE sect compound — a modest thatched hall and a training yard fence among dense bamboo, a distant green ridge, soft morning mist, fresh green palette (a lived-in version of the bamboo-forest backdrop) |

## 바닥 틀 (표면 문장·평균색만 바꿔 쓴다)

```
2D pixel art seamless GROUND TEXTURE, 16-bit retro JRPG style, a single large square image that tiles perfectly in all four directions (top edge continues into bottom edge, left edge continues into right edge, no visible seams or repeating hotspots). Top-down-ish view of flat ground seen from slightly above, as in a side-view action RPG play field. LOW CONTRAST and subtle: this is a floor that characters and effects will be drawn on top of, so no objects, no plants standing up, no rocks, no strong shapes, no lighting hotspots — only gentle surface variation. Overall average color exactly {땅색}. No purple or magenta tones. No text, no watermark. Surface: {표면}
```

| 본진 | 땅색 | 표면 |
|---|---|---|
| 개방 | #6e6349 | hard-packed dusty dirt yard with faint cart ruts, scattered straw and a few dry weed tufts pressed flat, muted khaki-brown palette |
| 소림 | #7a7368 | worn grey flagstone courtyard, large square paving stones with soft joints and faint moss in the cracks, warm grey palette |
| 무당 | #6c7a7c | pale blue-grey slate paving with thin drifting mist patches and faint water sheen, cool grey-teal palette |
| 화산 | #7d6658 | rust-red bare rock ground with fine cracks and a scatter of small pale plum petals lying flat, warm brown-red palette |
| 아미 | #647a5a | soft mossy ground with damp dark soil patches and a few flat cedar needles, muted green palette |
| 당문 | #5e5450 | dark wet cobblestones with faint greenish moss film and thin trickles of moisture in the joints, dark slate palette |
| 마교 | #4a4448 | black volcanic gravel and cracked basalt with faint dull-orange ember flecks in the deepest cracks, charcoal palette |
| 청죽문 | #6a7a52 | packed earth training yard edged with short grass, scattered fallen bamboo leaves lying flat, olive-green palette |
