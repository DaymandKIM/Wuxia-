# 몬스터 시트 프롬프트 — 완성형 (한 블록 = 한 몬스터, 그대로 복사)

공통 규칙이 이미 합쳐져 있다. 블록 하나를 통째로 제미나이에 붙이면 된다.
시트가 나오면 자르지 말고 채팅에 그대로 붙여넣기 — 원본 복원해서 추출한다.
낭인은 v2.9로 이미 게임에 들어갔다. 추천 순서: 위에서 아래로.

## 폐촌 · 들개 (근접)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a mangy feral dog with ribs showing, patchy dark grey-brown fur, foaming jaws, low aggressive posture. Walk row = low prowling run, attack row = lunging bite. Dusty grey-brown palette.
```

## 폐촌 · 보스 — 촌주의 원혼 (스킬형 보스)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. It is a large imposing BOSS monster - the body fills most of each cell. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a vengeful village-elder ghost in a rotting funeral robe, long white hair floating upward, pale green spectral flames drifting around the body, feet fading into mist. Attack = sweeping claw of green mist. Skill = inhales deeply then releases a skull-shaped green ghost-fire projectile flying RIGHT (projectile in its own cell). Pale green, ash grey and bone white palette, mournful and menacing.
```

## 동굴 · 독충 (근접)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a giant venomous centipede-beetle hybrid, glossy black segmented carapace with toxic violet markings, many clattering legs, dripping mandibles. Walk row = skittering crawl. Attack row = rearing up and striking down with venomous mandibles. Black and violet palette with sickly green venom drips.
```

## 동굴 · 석굴 박쥐 (스킬형·비행)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a large cave bat with tattered wings and glowing yellow eyes, always airborne - idle row = hovering in place with wing flaps, walk row = flying forward. Attack = swooping bite. Skill = opens mouth wide and emits a visible concentric sonic-scream ring projectile flying RIGHT (rings in their own cell). Dark grey and dull yellow palette. Keep the body at the same height above the cell bottom in every frame.
```

## 동굴 · 보스 — 석암거인 (근접 보스)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. It is a large imposing BOSS monster - the body fills most of each cell. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a huge stone golem awakened from the cave wall, mossy boulder shoulders, glowing amber core in the chest, stalactite spikes on its back, slow and massive. Attack row = raising both fists high and slamming the ground with flying rock debris. Grey stone and amber palette.
```

## 설산 · 설랑 (근접)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a white snow wolf with frost-tipped fur, icy blue eyes, breath visible as puffs of frost. Walk row = loping run through snow. Attack row = pouncing bite with a small burst of snow. White, pale blue and grey palette.
```

## 설산 · 빙백령 (스킬형·부유)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a floating ice spirit shaped like a robed woman sculpted of translucent ice, lower body trailing off into drifting snowflakes, always hovering - idle row = floating in place, walk row = gliding forward. Attack = a slash of frost from her sleeve. Skill = condenses frost between her palms and fires a sharp ice shard projectile flying RIGHT (shard in its own cell). Ice blue, white and pale silver palette, cold elegance. Keep the body at the same height above the cell bottom in every frame.
```

## 설산 · 보스 — 설산백호 (스킬형 보스)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. It is a large imposing BOSS monster - the body fills most of each cell. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a massive white tiger with frost-armor plates growing from its shoulders and forehead, exhaling blizzard breath, regal and ferocious. Attack = a huge claw swipe with a snow burst. Skill = rears back, roars, and breathes a cone of blizzard wind - the flying ice-shard cone as the projectile in its own cell, flying RIGHT. White, ice blue and silver palette.
```

## 천산 · 천산수리 (근접·비행)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a giant golden-brown mountain eagle with storm-grey wingtips and fierce eyes, always airborne - idle row = soaring in place, walk row = gliding forward. Attack row = folding wings and diving into a talon strike. Gold-brown and slate palette. Keep the body at the same height above the cell bottom in every frame.
```

## 천산 · 수호무사 (스킬형·정예)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: an elite guardian warrior in white-and-gold mountain-sect robes with a straight jian sword, calm disciplined stance, hair in a topknot. Attack row = a precise lunging sword thrust. Skill = sheathes the blade, gathers glowing white sword-qi, then releases a crescent-shaped sword-wave projectile flying RIGHT (crescent wave in its own cell). White, gold and sky blue palette.
```

## 천산 · 최종 보스 — 뇌운신장 (스킬형 보스)

```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. It is a large imposing BOSS monster - the body fills most of each cell. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a colossal thunder-cloud deity general - upper body formed of dark storm clouds wearing golden armor pieces, crackling lightning veins across the body, stern temple-guardian face, lower body dissolving into swirling cloud. Attack = a lightning-wreathed fist smash. Skill = raises both arms skyward and hurls a forked lightning bolt projectile flying RIGHT with electric arcs (bolt in its own cell). Storm grey, gold and electric blue palette, divine wrath.
```

---

# 몬스터 외 시트 프롬프트 (v2.22 추가)

## 주인공 · 양손 정권 (한손 정권 해결용 — 왼손 컷이 그림에 없다)

```
2D pixel art sprite sheet of the SAME hero character, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. The character: a young Chinese martial artist with a long dark brown ponytail, beige hanfu robe with a blue sash and blue trousers, black boots, bare hands. Row1 = RIGHT-hand straight punch, 4 frames (guard stance, right fist drawn back at the chest, right arm fully extended forward, return to guard). Row2 = LEFT-hand straight punch, 4 frames (guard stance, left fist drawn back at the chest, LEFT arm fully extended forward with the torso twisted so the far arm clearly crosses in front, return to guard). The two rows must clearly punch with OPPOSITE arms.
```

## 지형 타일셋 — 구역별 5장 (v2.32 전면 개정)

공통 설계: **저채도·저대비** — 바닥이 화려하면 인물이 묻힌다. 줄1 = 이음매
없는(seamless) 민바닥 4칸(현재 단색 바닥을 대체할 기본 질감), 줄2 = 액센트
4칸(민바닥 위에 가끔 섞을 변화), 줄3 = 소품(마젠타 배경, 낱개). 한 구역씩
뽑아서 보내면 그때그때 얹는다 — **죽림부터**.

### ① 죽림 (기본 바닥색 #6a7a52 계열)
```
2D pixel art ground tileset, 16-bit retro JRPG style, top-down 3/4 view. MUTED, LOW-CONTRAST colors so characters drawn on top stay readable - no busy patterns, no strong highlights. A strict square grid of equal cells with thin dark borders. Row 1 = four SEAMLESS plain mossy forest-floor tiles in muted sage green (subtle grass texture variations only, each tile fills its whole cell edge-to-edge and tiles seamlessly with itself). Row 2 = four accent tiles on the same green base: scattered fallen bamboo leaves, a small dirt patch, a mossy stone half-buried, thin roots crossing. Row 3 = decorative props each alone in its own cell on solid magenta background (#FF00FF): a bamboo stalk cluster, a young bamboo shoot, a mossy rock, a fallen log, a small stone lantern. No text, no labels, no watermark, limited palette of muted greens and earth tones.
```

### ② 폐촌 (기본 바닥색 #6b6350 계열)
```
2D pixel art ground tileset, 16-bit retro JRPG style, top-down 3/4 view. MUTED, LOW-CONTRAST colors so characters drawn on top stay readable - no busy patterns, no strong highlights. A strict square grid of equal cells with thin dark borders. Row 1 = four SEAMLESS plain packed-earth tiles in dusty grey-brown (subtle dry soil texture variations only, each tile fills its whole cell edge-to-edge and tiles seamlessly with itself). Row 2 = four accent tiles on the same earth base: cracked dry ground, sparse dead weeds, a few scattered broken roof tiles, worn flat footpath stones. Row 3 = decorative props each alone in its own cell on solid magenta background (#FF00FF): a broken wooden fence section, a collapsed wall corner of an old house, an abandoned cart wheel, a bare dead tree, a cracked clay jar. No text, no labels, no watermark, limited palette of dusty browns and greys.
```

### ③ 동굴 (기본 바닥색 #5c5f5c 계열)
```
2D pixel art ground tileset, 16-bit retro JRPG style, top-down 3/4 view. MUTED, LOW-CONTRAST DARK colors so characters drawn on top stay readable - no busy patterns, no strong highlights. A strict square grid of equal cells with thin dark borders. Row 1 = four SEAMLESS plain cave stone-floor tiles in dark cool grey (subtle rock texture variations only, each tile fills its whole cell edge-to-edge and tiles seamlessly with itself). Row 2 = four accent tiles on the same stone base: hairline cracks, scattered small pebbles, a shallow dark puddle with a faint glint, a patch of tiny faintly glowing teal mushrooms. Row 3 = decorative props each alone in its own cell on solid magenta background (#FF00FF): a stalagmite, a pile of rocks, a glowing pale-blue crystal cluster, old scattered bones, a burnt-out torch stub. No text, no labels, no watermark, limited palette of dark cool greys with faint teal accents.
```

### ④ 설산 (기본 바닥색 #b9c9d2 계열)
```
2D pixel art ground tileset, 16-bit retro JRPG style, top-down 3/4 view. MUTED, LOW-CONTRAST PALE colors so characters drawn on top stay readable - no busy patterns, no strong highlights. A strict square grid of equal cells with thin dark borders. Row 1 = four SEAMLESS plain packed-snow tiles in pale blue-white (subtle snow texture variations only, each tile fills its whole cell edge-to-edge and tiles seamlessly with itself). Row 2 = four accent tiles on the same snow base: a slick ice patch, a low wind-blown snow drift, frozen rock breaking through the snow, sparse frost-covered grass tufts. Row 3 = decorative props each alone in its own cell on solid magenta background (#FF00FF): a cluster of ice shards, a small snow-covered pine sapling, a frozen boulder, a weathered wooden signpost capped with snow. No text, no labels, no watermark, limited palette of pale blue-whites and cold greys.
```

### ⑤ 천산 (밝은 하늘빛 바닥 계열)
```
2D pixel art ground tileset, 16-bit retro JRPG style, top-down 3/4 view. MUTED, LOW-CONTRAST PALE colors so characters drawn on top stay readable - no busy patterns, no strong highlights. A strict square grid of equal cells with thin dark borders. Row 1 = four SEAMLESS plain sacred mountain-summit stone tiles in pale grey-blue cloudstone (subtle weathered stone texture variations only, each tile fills its whole cell edge-to-edge and tiles seamlessly with itself). Row 2 = four accent tiles on the same pale stone base: a thin cloud wisp drifting over the stone, cracked ancient floor tiles with faded gold inlay lines, soft moss in the stone joints, small white flowers growing from a crack. Row 3 = decorative props each alone in its own cell on solid magenta background (#FF00FF): a small stone shrine, a weathered round stone pillar stump, a floating rock fragment with a faint glow underneath, an ancient bronze bell on a wooden stand. No text, no labels, no watermark, limited palette of pale grey-blues with small gold accents.
```

## 재화·장비 · 아이콘 시트 (비급·내공·장비 준비)

```
2D pixel art item icon sheet, 16-bit retro JRPG style, each icon inside its own thin dark-bordered square cell on solid magenta background (#FF00FF), no text, no watermark, crisp 1px black outlines, consistent scale and angle. Icons one per cell: an old martial-arts manual book with a faded cover, a glowing blue inner-energy orb, a silver ingot, a small brown medicine gourd, a jade pendant on a red cord, an iron straight sword, a wooden staff, cloth hand wraps, a folded silk robe, a woven straw hat, leather boots, a plain iron ring.
```

---

# 죽림 티어 확장 7종 (v2.24 — 카탈로그 시트 기반, 대표 컷과 함께 쓴다)

제미나이에 **대표 컷 그림을 첨부**하고 아래 프롬프트를 붙인다.
대표 컷: raw/catalog-bamboo.png 에서 자른 ref1~ref7.

## ① 대나무 정령병 (엘리트·근접) — ref1
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a bamboo spirit soldier - a humanoid figure formed of living green bamboo segments and vines, mossy plate armor, glowing pale-green eyes, wielding two short bamboo blades. Attack row = a crossing double-blade slash. Green and moss palette.
```

## ② 죽림 추적자 (엘리트·스킬형) — ref2
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = ranged skill 3 frames (gather energy, cast pose, recover) + the projectile alone in its own separate cell, row5 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a hunched jungle stalker in tattered dark red-brown rags with glowing red eyes and a long bamboo spear. Attack = a quick low spear thrust. Skill = spins and hurls a whirling razor bamboo-leaf projectile flying RIGHT (the spinning leaf in its own cell). Dark brown and dull red palette.
```

## ③ 대나무 방울뱀 (레어·근접) — ref3
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (coiled, tongue flicking), row2 = walk 4 frames (slithering forward), row3 = attack 4 frames (rearing back, lunging bite with bared fangs, follow-through, recoil), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a jade-green bamboo rattlesnake with a cream underbelly, thick coiled body, rattle tail raised and shaking. Jade green and cream palette with a touch of venom green.
```

## ④ 대나무 무뢰배 (일반·근접) — ref4
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a scruffy forest thug in ragged brown clothes with a heavy wooden club, unshaven mean face, hunched aggressive posture. Attack row = an overhead club smash. Brown and tan palette.
```

## ⑤ 대나무 파수꾼 (일반·근접) — ref5
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a watchman soldier in green lacquered bamboo-slat armor carrying a bamboo spear and a small round wooden shield. Attack row = a spear thrust from behind the raised shield. Green and wood-brown palette.
```

## ⑥ 등딱지벌레 (일반·근접) — ref6
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (breathing), row2 = walk 4 frames, row3 = attack 4 frames (windup, strike, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a large bamboo shell beetle with a hard glossy brown carapace, stubby horns and short clawed legs, low to the ground. Attack row = a charging headbutt ram with its horned shell. Brown and copper palette.
```

## ⑦ 대나무 말벌 (일반·근접 비행) — ref7
```
2D pixel art monster sprite sheet, 16-bit retro JRPG style, side view, facing RIGHT in every frame, same character size and same ground level in every cell. Each frame inside a thin dark-bordered cell of equal size, no empty cells. Solid magenta background (#FF00FF). No text, no labels, no watermark. Crisp 1px black outlines, limited palette. Use the attached reference image for the character design. Rows: row1 = idle 4 frames (hovering with blurred wings), row2 = walk 4 frames (darting flight forward), row3 = attack 4 frames (rearing, diving stinger jab, follow-through, recover), row4 = hit reaction 1 frame + death 2 frames (collapsing, fallen). The monster: a giant bamboo wasp with translucent white wings, black and amber striped body, dangling stinger, always airborne. Keep the body at the same height above the cell bottom in every frame. Black, amber and white palette.
```
