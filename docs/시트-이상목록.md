# 문파 인물 시트 — 남은 흠 목록 (v2.94.28~)

사용자 확정 2026-09-17 "이상한 건 기억해 둬, 나중에 다시 그리든가 하게".
추출로는 못 고치고 **시트를 다시 받아야** 하는 것만 여기 모은다.
고친 것·뺀 칸은 각 `FOES.*` 주석과 VERSION.md 에 적혀 있다.

## 다시 그려야 고쳐지는 것

| 시트 | 흠 | 지금 상태 |
|---|---|---|
| **화산 수습** `hs_disc2` | **걷기 8칸만 검이 없다.** 대기·공격은 검을 들고 있어 걸을 때만 맨손이 된다 | 그대로 씀. 걷는 시간이 짧아 덜 띈다 |
| **개방 장로** `gb_elder2` | **걷기 8칸만 지팡이가 없다.** 같은 증상 | 그대로 씀 |
| **개방 장로** `gb_elder2` | 공격 B·C 줄에 **지팡이가 통째로 사라진 맨손 칸** 3개(r3c7·r4c3·r4c4) | 그 칸만 뺐다 |
| **개방 장로** `gb_elder2` | 찌르기 청록 줄기가 **두 칸에 걸쳐 그려져** 칸 경계에서 평평하게 끊긴다(`atk3_4`) | 통짜 줄기를 `fx_qiray` 로 따로 뽑아 뒀다. 탄·초식 이펙트로 쏘는 편이 낫다 |
| **소림 장로** `sr_elder2` | 쓰러짐 줄 한 칸에 **인물이 둘 겹쳐** 그려져 있다(무릎 꿇은 몸 + 엎어진 머리) | 그 칸을 뺐다 |
| **화산 수습** `hs_disc2` | 쓰러짐 줄 마지막 칸만 **머리 방향이 반대**라 앞 칸에서 거울로 튄다 | 그 칸을 뺐다 |
| **화산 장로** `hs_elder2` | **걷기 10칸만 검이 없다** (화산 수습·개방 장로와 같은 증상) | 그대로 씀 |
| **화산 장로** `hs_elder2` | 땅 내리꽂기 줄에서 **치켜든 검 끝이 위 테두리 띠에 6~7px 잘린다**(r4c4·c5) · 한 칸은 **검날이 왼쪽 테두리에서 평평하게 잘렸다**(r4c9) | 잘린 두 컷은 살려 쓰고(칼이 화면 위로 뻗은 자세라 덜 띈다) 검날 잘린 칸은 뺐다 |
| **아미 수습** `am_disc2` | 공격 줄에 **파란 강조 테두리(액자)로 둘러싸인 칸**이 있다. 그림이 아니라 표시다 | 추출 때 빼거나 테두리를 지운다 |
| **무당 정예** `md_elite2` | `atk0` 머리 위 원호 오른쪽 끝이 옅어 **축소에서 끊긴다** · `atk2_2` 흰 나선이 칸 좌우 테두리에서 끝난다 | 시트에서 이미 거기서 끝나 복구 불가 |
| **개방 수습** `gb_disc2` | `death1`·`death2` 밑에 **어두운 올리브색 땅 풀 1~2px 띠**가 몸에 붙어 있다(원본 그림) | 죽림 바닥색과 비슷해 그대로 뒀다 |
| **개방 정예** `gb_elite2` | `atk2_6` 호 바깥 끝에 1×5px 자주회색 점선 잔재 | 게임 배율에서 외곽선과 구분 안 돼 그대로 뒀다 |
| **소림 장로** `sr_elder2` | 금빛 후광·초승달은 **단독 칸이 없다**(전 칸에 인물이 같이 있다) | `fx_halo`·`fx_crescent` 를 못 뽑았다. 연꽃만 `fx_lotus` 로 남겼다 |
| **당문 장로** `dm_elder2` | **침 부채가 자주색(144,69,154)** 이라 배경 판정(`r>g+40 & b>g+40`)에 통째로 먹힌다. 프롬프트엔 green 이라고 썼는데 무시됐다 | 배경이 순마젠타(253,1,250)라 문턱을 올리면 갈라진다 — 침만 연녹으로 물들여 살렸다 |
| **화산 정예** `hs_elite2` | 찌르기 줄 **칼끝이 칸 테두리에서 잘려 있다**(칸5 오른쪽 끝 3열 5·5·4 — 줄지 않고 끝난다). 옆 칸으로 이어지지도 않는다 | 온전한 컷에서 칼날을 떼어 이어 붙이는 손질을 했다 |

## 시트 프롬프트에 이미 반영한 것 (v2.94.28)

같은 사고를 막으려고 `docs/프롬프트-문파인물.md` 에 넣었다.
- 무기·이펙트를 **칸 안에 여백 두고** 넣고 테두리에 닿지 말 것
- **걷기 줄에도 같은 무기**를 들 것
- 칸에 **강조 테두리를 그리지 말 것**
- 쓰러지는 컷은 **방향을 한쪽으로 통일**할 것
- 배경은 **순마젠타**로(옅은 자주면 도복이 배경으로 지워진다 — 소림 정예 시트)
- 실존 사찰·문파 이름을 넣지 말 것(그림 생성이 거부된다)
- 이펙트 색을 **파랑·청록·흰·초록·금**으로 못 박을 것. 자주·분홍은 배경 판정에 먹힌다 — 당문 장로 침이 그렇게 사라질 뻔했다

## 추출기에 접어 넣을 것 (sr_sheet.py)

지금 여러 에이전트가 같은 스크립트를 쓰고 있어 미뤄 뒀다. **이번 묶음이 끝나면 반영한다.**

1. **마젠타 걷어내기에 `|r-b| < 40` 조건을 더한다.** 지금 조건(`r>g+10 & b>g+10`)이 **붉은 술·진홍 장포**(r 200·b 60)까지 눌러
   **검은 막대**로 만든다(화산 장로 대기 컷에서 잡혔다). 마젠타 번짐은 r≈b 라 이 조건으로도 그대로 걷힌다.
   **진홍이 들어가는 화산·마교 시트에 특히 필요하다.**
2. **작은 덩어리 버림 문턱 40 → 8 을 옵션으로.** 화산 장로의 **흰 매화 꽃잎**이 8~40px 조각이라 통째로 사라진다
   (그 공격의 정체가 없어진다). 같은 이유로 그 시트는 `--nodeclutter` 가 필요했다.

## 그 밖에 지켜볼 것

- **빌드 용량.** 컷을 늘리면서 단일 빌드가 23.6MB, 웹 조각이 7+7+6.9+1.7MB 가 됐다.
  남은 문파(당문·마교·청죽)가 들어오면 한도에 닿는다. 스프라이트를 **팔레트 PNG** 로 저장하면 절반 아래로 준다 —
  `sr_sheet.py` 의 `pack()` 저장을 `sectdiff.save_pal` 식으로 바꾸는 일이 남았다.
