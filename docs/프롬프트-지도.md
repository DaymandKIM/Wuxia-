# 여정 지도 재작업 프롬프트 (v2.93.6, 사용자 "지도도 변경해야 하지 않나 — 파일 주고 바꿀 점 알려줘, 다시 그려 올게", 2026-09-17)

지금 지도: `assets/zone_map.png` 720×720 수묵 양피지풍. 다섯 사냥터(죽림 좌하 · 폐촌 우중 · 동굴 좌중 · 설산 우상 · 천산 좌상 구름 위 탑)와
우상단 성채(이름 없음). 노드 좌표는 `ZONES[].map`(360×360 기준)이라 새 그림이 오면 다시 잰다(review 스크린샷으로 손으로 맞춤).
UI 그림이라 픽셀 아트일 필요 없다(도트 통일 규칙은 게임 화면만). 글자·이름표는 게임이 그리니 그림엔 **글자 없음**.

## 바꿀 점
1. **문파 터(내 문파, 무명문)** — 죽림 바로 옆, 여정의 출발점에 작은 마당(돌담·전각 몇 채·대나무 울타리)을 하나. 게임 마당 배경(sect_bg)과 같은 느낌.
2. **문파 본진 8곳**(본진 비무 상대) — 사냥터와 겹치지 않는 빈 자리(가운데·위 산맥·오른쪽 가장자리)에 각각 알아볼 수 있는 실루엣으로:
   소림(산 위 절·탑) · 무당(봉우리 위 도관·구름) · 화산(깎아지른 절벽 위 정자) · 아미(안개 낀 산의 암자) · 개방(성문 밖 움막·모닥불) ·
   당문(사천 안개 속 담장 높은 장원) · 마교(어두운 성채 — 지금 우상단 성채를 그대로 마교로) · 청죽문(대숲 속 작은 문파 — 죽림 안쪽).
3. 길이 **한 줄기 여정**으로 읽히게: 문파 터 → 죽림 → 폐촌 → 동굴 → 설산 → 천산. 본진들은 그 길에서 갈라진 샛길 끝.
4. 다섯 사냥터 자리는 **지금 구도 그대로**(좌표를 크게 안 바꾸려고). 크기는 1024×1024 정사각.
5. 톤은 지금과 같은 수묵·양피지. 워터마크·글자·나침반 글자 없음.

## 프롬프트 (지금 지도를 참고 이미지로 첨부)
```
Redraw the attached hand-drawn ink-wash parchment fantasy map (square, 1024x1024) in exactly the same style, palette and composition. Keep the five existing locations where they are: bamboo grove (bottom-left), ruined village (right-middle), dark cave (left-middle), snowy mountain with a hut (upper-right), and the pagoda on a cloud-wreathed peak (top-left). Keep the winding rivers and roads.
ADD these places, each as a small distinctive ink-drawn landmark, placed in the empty areas so nothing overlaps: (1) a small martial-arts sect courtyard right next to the bamboo grove at the start of the road — low stone walls, a few timber halls, bamboo fence; (2) a Buddhist temple with a pagoda on a mountain (Shaolin); (3) a Taoist temple on cloud-wrapped twin peaks (Wudang); (4) a pavilion on a sheer cliff (Huashan); (5) a misty mountain nunnery (Emei); (6) a beggars' camp of huts and a campfire outside a city gate (Beggars' Sect); (7) a high-walled manor in fog (Tang Clan); (8) turn the existing fortress in the top-right corner into a dark, forbidding demonic cult stronghold (Demon Cult) — black banners, jagged walls; (9) a tiny hidden gate deep inside the bamboo grove (Green Bamboo Sect).
Connect them with faint side paths branching off the main road, so the main road still reads as one journey: sect courtyard → bamboo grove → village → cave → snow mountain → sky pagoda.
Aged parchment texture, sepia and indigo ink washes, brush linework, soft paper edges. NO text, NO labels, NO letters or characters anywhere, no compass letters, no watermark, no border frame.
```

- 받으면: `assets/zone_map.png` 교체(폭 720~1024, 팔레트 PNG 아님 — UI 그림이라 그대로), ZONES[].map 좌표 재측정, 본진 노드는 v2.94 비무에서 `SECT.hq[]` 로 추가.
