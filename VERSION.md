# v2.92.4 — 문파 탭은 마당만, 시트는 배지를 누를 때 (사용자 "문파 여전히 아래 탭이 열리네", 2026-09-16)

- 문파 탭을 누르면 **시트 없이 마당**만 열린다. 마당 위 오버레이(#sover): 위 현판(문파 이름·한자·✎ 이름 바꾸기·✕ 닫기), 아래 배지 둘 —
  왼쪽 명성(단계·다음 문턱·게이지), 오른쪽 제자(N/자리·초당 수익). 배지나 ✎를 누르면 옛 시트(이름 줄·명성·제자 줄)가 올라오고,
  시트 ✕는 시트만 닫는다. 마당은 현판 ✕나 탭 재클릭으로 닫는다(sectView 기준).
- 제자 말풍선에 둘째 줄(계보 출신·자질·계보 보너스). 마당에선 스킬창·시험 버튼을 숨긴다. 검증 secttest 갱신. 스크린샷 scratch sect_over.png.

# v2.92.3 — 전각 시트 2판(건물별 세세한 프롬프트) 반영 (사용자 "못생긴 건가 ㅋㅋ 프롬프트 좀 건물별로 세세하게", 2026-09-16)

- 새 시트 sheets/halls.png(옛 것은 halls_v1.png): 제미나이가 **5줄 × 4칸**으로 그렸다(기와를 두 판) + 칸 경계가 줄마다 어긋남(464/484·700/722/854)
  + 장경각 탑이 2·3줄에 걸침. 격자 자르기를 버리고 **덩어리 라벨링**으로: 테두리 줄(가로 전체 검출 + 줄 띠마다 후보 ±6px 어두운 세로 열)만 지우고
  마젠타 아닌 덩어리를 무게중심이 든 칸(LAYOUT: 종류→(줄 범위, 칸))에 배정. 줄을 지운 자리(탑 허리)는 위아래 보간으로 메움(seal_seams).
  얇고 긴 세로 조각(폭≤5·높이≥30 = 자주색 테두리 잔재, 초가 왼쪽에 붙어 왔다)·떠 있는 작은 조각(워터마크)은 버림. 분홍 밧줄은 b<g로 갈색화.
- 배율 0.514(최대 높이 110·폭 165): 탑 61×110, 대전 127~159×48~54, 터 108×28~42. 기와 5채가 전부 다른 실루엣(개방 연무장·3층 탑·긴 약방·
  등롱 객당·삼문 패방).
- 제자 걷는 띠를 앞줄 전각 앞(0.55~0.645)으로 — 전각 발치에 들어간 제자가 지붕 위에 선 듯 보였다. 스크린샷 scratch sect_halls3.png.

# v2.92.2 — 전각 시트 반영 (사용자 시트 sheets/halls.png "이게 괜찮은 거냐", 2026-09-16)

- `halls.py`: 어두운 테두리 줄로 5×3 격자 검출 → 칸 안 자줏빛 배경 제거 → 땅에 닿은 덩어리만(산문 칸 우상단 제미나이 별 워터마크는 '떠 있는 작은 조각'
  규칙으로 버림) → 자주끼 걷기(r·b가 g보다 25 넘게 크면 g+30/+12로 — 말뚝·밧줄이 통째로 분홍이었다) → 축소(알파 따로 문턱 96, 색은 가장자리 번짐 뒤
  LANCZOS). **배율은 폭으로 정했다**: 첫 안 높이 118은 폭 355로 화면(390)을 넘겨서, 최대 폭 165 → 배율 0.517, 기와 높이 52~55(주인공 키 47 정도).
  결과 assets/hall_<k>_<0|1|2>.png 15장 + 패널 아이콘 hall_<k>.png(기와 44px). 검사판 review/halls.png(바닥색·키 자).
- 마당 배치를 시트 크기에 맞춤: 뒷줄 장경각(0.22)·약방(0.78), 가운데 뒤 산문(0.50), 앞줄 연무장(0.27)·객당(0.73), 사부 0.61. 탭 판정은 그림 크기.
- 흠(감수): 기와 단계 3채(연무장·약방·객당)가 거의 같은 대전 — 이름표로 구분. 거슬리면 3열만 재작업.

# v2.92.1 — 전각 올리기는 마당에서 (사용자 "문파 업글은 아래쪽에 놓으니 별로, 화면에서 누르면 뭔가 뜨면서 해도 될 듯", 2026-09-16)

- 시트의 전각 카드 5장을 뺐다. **마당의 전각을 누르면 그 위에 팝업**(#hpop: 낙관·이름·Lv/상한·효과 지금→다음·[올리기 은자] 꾹 누르면 연속·✕).
  빈 곳을 누르거나 탭을 닫으면 사라진다. 자리는 전각 위(캔버스 단위 → 오버레이 px, 화면 안으로 클램프).
- 시트는 36%로 얇아지고(이름·명성·제자 줄만), 마당을 아래로 넓혔다(SECT.scene 위치·yardY 0.43~0.60·사부 0.58).
- 검증 secttest(마당 탭 → 팝업 · 버튼으로 세우기 · 빈 곳/✕ 닫힘). 스크린샷 scratch sect_pop.png.

# v2.92 — 문파 2층: 제자 + 문파 터 화면 (사용자 "문파는 그냥 탭인가 → 멋지네 → 좋으네", 2026-09-16)

- **문파 터 화면**: 문파 탭을 열면 화면이 **내 문파 마당**으로 바뀐다(어느 구역에 있든 죽림 원경·바닥, rzone). 전각 5채가 자리에 서고(뒷줄 장경각·
  산문·약방, 앞줄 연무장·객당), 사부(주인공)가 가운데 서 있고, 제자들이 마당을 거닐고 목검을 휘두른다. 아래 시트는 절반 높이. 전투는 뒤에서 계속
  돈다(step은 그대로, render만 분기). 시트 전엔 전각 = **팻말**(나무 기둥 + 낙관 도장, Lv 0은 말뚝) — hall_<k>_<0|1|2>.png가 오면 터·초가·기와
  (SECT.scene.stageLv 1·6·20). 제자 = **주인공 스트립의 도복만 계보색으로 물들인 것**(tintedStrip — 살·머리 그대로) → disciple_walk/train 시트가 오면 교체.
  **탭**: 전각을 누르면 시트의 그 카드로 스크롤·금색 강조, 제자를 누르면 말풍선 한마디(SECT.disciple.say). 위 현판에 문파 이름.
- **제자**(SECT.disciple): 합류 = 인연 — 명성 단계가 오를 때(자리 있으면), 기연 카드 **'입문 청'**(자리 있을 때 풀에 섞임), (v2.93) 본진 비무.
  계보 8 중 하나·자질 하/중/상/천(명성 단계별 가중치)·신조 이름(성 16 × 이름 24). 육성 없음.
  **수익** = 지금 사냥터 전투 수입(killSilver/offKillTime) × 7% × 자질(0.6/1/1.6/2.6) × 객당 → 초당 자동 입금, 오프라인에도(70%, 복귀 카드 '제자 수익').
  **계보 보너스** = 그 계보 무공 artEff × (1 + 자질 bonus% 합) — "소림 제자가 있으니 소림 무공이 는다". 자리 = 명성 단계(1·2·3·4·6) + 객당 10레벨당 1.
- 패널: 명성 띠 아래 **제자 줄**(N/자리, 이름·계보 점·자질·계보 보너스, 초당 수익·이번 접속 벌이). 객당 효과 은자 0.6% → 문파 수익 +3%·은자 0.4%.
- sim 8h 설산 8(v2.91과 같음), 4h 설산 4(3). 검증 secttest(자리·명성 합류·rollDisciple·만석·계보 보너스→artEff·초당 수익·10초 입금·저장·복원·
  오프라인 정산 줄), fatetest(입문 청 보상). 스크린샷 scratch sect_scene.png.

# v2.91.2 — 문파 이름 짓기, 기본 무명문 (사용자 확정 "추천으로 가자, 기본은 무명문", 2026-09-16)

- 청죽문 → **무명문 無名門**이 기본. 명성 '무명'과 맞물린다("무명문이 천하제일이 됐다"). 청죽문은 무공 8계보 중 하나로만 남는다.
- **이름은 플레이어가 짓는다**: 문파 탭 처음엔 '문파 이름을 정한다' 줄이 펼쳐져 있고(입력 + [정한다]), 지은 뒤엔 머리글 ✎로 다시 연다.
  공백 정리·8자 상한(SECT.nameMax), 빈 값·기본 이름은 저장값 빈 문자열(= 기본). 직접 지은 이름엔 한자를 안 붙인다.
  sectName()/sectHan()/setSectName()/sectCleanName()(69b-sect), 저장 S.sectName(65-save), 검증 secttest(기본·짓기·상한·✎·빈 값·복원).

# v2.91.1 — 문파 임시 아이콘 · 태스크바 히어로 조사 (사용자 "이미지는 없으니 아이콘 등으로 일단 만들어 / 태스크바 히어로 조사해 봐", 2026-09-16)

- **문파 탭 아이콘** = 청죽문 엠블럼(sch_bamboo, 사용자 시트)을 시트가 올 때까지 쓴다(tab_sect가 오면 그것이 우선). 원형 클립.
- **전각 아이콘** = 에셋 없으면 **낙관 도장**(붉은 인장 + 흰 한자, CSS만 — .hico.seal). hall_<k>.png가 오면 그림으로 바뀐다.
- docs/조사-2026-09-태스크바히어로.md — 벤치마킹 후보 정리: **"다음 목표 한 줄"**(HUD·복귀 카드)·**오프라인 장비 드랍(보수적)** 가져온다,
  장터·리셋·파티는 안 가져온다(방침), 등급=접사 칸 수는 보류.

# v2.91 — 문파 1층: 청죽문 전각 5채 + 명성 + 문파 탭 (사용자 "문파 슬슬 만들어봐 → 일단 만들어봐 좀 보자", 2026-09-16)

- **설계**(docs/설계-문파.md): 주인공은 문파에 안 든다(장무기형) — 죽림 뿌리 **청죽문을 다시 세운다**. 1층 전각(은자 sink·영구 %) +
  명성(상한 해제). 2층 제자(방치 수익·계보 보너스)와 3층 **문파 본진 비무**(사용자 아이디어: 소림사·무당산 본진에서 싸워 그곳 출신 제자가
  내게 온다)는 v2.92~93.
- **전각 5채**(SECT.halls): 연무장(공격 +1.5%·공속 +0.5%/Lv) · 장경각(숙련 획득 +4%·연마 비용 −1.5%/Lv) · 약방(체력 +1.5%·회복 +2%·
  쓰러짐 회복 −2%/Lv) · 객당(은자 +0.6%/Lv, 제자 자리는 v2.92) · 산문(은자 +0.8%·명성 획득 +3%/Lv). 비용 cb×cg^Lv(100~200, ×1.30~1.34).
  sBonus()가 heroDmg·hp·regen·aspd·killSilver에 수련·장비와 같은 자리로 합산, artLvCost·DOWN_TIME은 나눔, artXpGain은 곱(숙련 3곳).
- **명성**(S.fame): 처치 killFame = 1×1.06^(g−1) · 보스 60 · 첫 격파 +240 · 업적 받기 30 · 오프라인 처치도. 단계 무명 0 → 향리 500 →
  일방 4,000 → 명문 25,000 → 천하제일 150,000, 전각 상한 5 → 15 → 30 → 50 → 80. 단계가 오르면 토스트. 첫 안(400/2500/12000/60000)은
  8h sim에 2시간 만에 명문이라 올렸다.
- **문파 탭**(5탭 [무공][수련][장비][문파][사냥터], #spanel): 명성 띠(단계·다음 문턱·게이지) → 전각 카드 5(한자 액자·레벨/상한·효과 지금 → 다음·
  [세우기/올리기 은자], 꾹 누르면 연속) → 안내. 탭 알림점 = 세울 수 있는 전각. 탭 아이콘 tab_sect·전각 아이콘 hall_<k>는 에셋이 오면 저절로.
- **sim(spend 싼 전각 틱당 3회)**: 8h 설산 8(기준 7), **24h 천산 3**(v2.90 천산 2, 설계 범위 "천산 초입" 유지). 전각 합 24h 149(상한 250),
  명성 4h 명문·24h 천하제일 미달(며칠). 24h 잔고 30만 — 후반 sink는 제자·내공에서 더 잡는다.
- 저장 S.halls·S.fame(65-save, 오프라인 명성), 모듈 src/69b-sect.js, 검증 secttest(전각 효과·상한·명성 단계·산문·업적 명성·장경각·약방·
  저장·패널·오프라인). 스크린샷 scratch sect_panel.png.

# v2.90.3 — 시트 꽉 채움 · 장비 상세 창 고정 (사용자 "장비 상태창이 아래로 내리니 안 보여서 불편, 모든 창은 화면을 꽉 채우면", 2026-09-16)

- **모든 시트가 상단 바 아래부터 탭바까지 꽉 찬다**: 패널 컨테이너 inset 위를 `--toph`(60-ui hud가 상단 바 offsetHeight를 재서 CSS 변수로,
  바뀔 때만) 로, .zsheet는 height:100%. 옛 76%·92% 개별값 폐지. 상단 바(체력·은자)는 그대로 보인다.
- **장비 위쪽 창 고정**: 탭·상세 창·[일괄 합성][자동 장착]을 #eqhead 한 덩어리로 묶어 머리글(44px) 아래에 sticky. 등급 줄을 끝까지 내려도
  누른 장비의 장착·강화·합성 버튼이 보인다. 스크린샷 scratch eq_full_pair.png.
- 검증 test·uitest·equiptest·achvtest·savetest.

# v2.90.2 — 저장 막힘 알림·저장 코드 · 업적 토스트 (사용자 "저장이 안 되고, 업적 은화 받을 때 알림이 짤려 보이고 빨리 사라짐", 2026-09-16)

- **저장이 안 됨**: 크로뮴 단독 탭(http)에선 저장·재로드 왕복이 정상이다(scratch savert.js). 문제는 **환경** — 아티팩트 뷰어의 iframe·앱 안
  웹뷰·사파리 서드파티 저장소 차단·시크릿에선 localStorage.setItem이 던지는데 saveNow가 삼켜 "저장했다"고 거짓말했다.
  이제 saveNow가 성공 여부를 돌려주고(saveOk, 써 넣은 값을 읽어 확인) ≡ 메뉴 '지금 저장'에 **막힘** 표시 + "저장 못 했다" 토스트.
- **저장 코드**(≡ → 저장 코드): 진행을 `WX1.`+base64(JSON) 글로 뽑아 복사하고, 붙여넣어 그 자리에서 불러온다(새로고침 없음 — 막힌 브라우저는
  새로고침하면 다시 잃는다). 막힌 환경·기기 이동용. saveData/saveCode/parseCode/loadCode(65-save), 시트 #cpanel·openCode/copyCode/pasteCode(60-ui),
  SAVE.codeTag. 복사는 clipboard → execCommand → 실패 시 안내.
- **업적 보상 토스트**: #toast에 z-index가 없어 업적 시트(DOM 뒤) 밑에 깔려 잘려·흐려 보였다 → z-index 45(메뉴 30·기연 40 위, 타이틀 50 아래).
  표시 1.8초 → **2.8초**(ACHV.toastSec).
- 검증 savetest 5절(코드 왕복·거부·시트·막힌 저장소에서 false·막힘 표시·토스트 문구). 스크린샷 scratch save_pair.png.

# v2.90.1 — 시전 전 컷 항상 · 무기 든 대기 자세 (사용자 "붕산장 구체가 안 보이게 빠르던데, 저 이미지들 다 쓰고 있는 건 맞지?", 2026-09-16)

- **시전 컷 덜어내기 폐지**: v2.4의 castStar(1성 0.55 → 4성 1.0)가 1성 붕산장을 6컷 중 3컷(0·3·5)만 틀어 손에 모이는 구체 컷(1·2)이
  통째로 빠졌다 — "구체가 안 보이게 빠르다"의 원인. castStar 전부 1.0으로 성과 무관하게 **전 컷 재생**(6컷 10fps ≈ 0.6초, 선풍퇴 9컷 0.9초).
  castN/castFrame 함수는 남겨 둠(전부 항등). fxtest의 "1성 < 4성" 검사를 "1성 = 4성 = 전 컷"으로 바꿈.
- **cast 스트립 5종 점검**(review/cast_now.png): 파공권 7·선풍퇴 9·암향지 6·붕산장 6·활인기공 8컷 전부 게임에 그대로 들어가 있고 같은 계열·같은
  톤이라 **새 시트 불필요**. docs/프롬프트-주인공.md C는 보류로 표시.
- **무기 든 대기 자세**: 무기를 끼면 대기(idle)를 그 무기 무브셋 첫 스트립의 0번 컷(무기 든 자세)으로 그린다 — 검·도·창·봉·부채 다섯 다
  0번 컷이 무기를 들고 선 자세라 별도 시트 없이 됨(review/weapon_stance.png, scratch weapon_idle.png). drawHero의 WEAPONMOVES 분기, 폭 HFX.aw[키].
  권갑·맨손은 옛 대기 그대로. 질주·피격은 맨손 컷(요청 시 확장). docs/프롬프트-아이콘.md 12(든 무기 미니 시트)는 불필요로 표시.
- 검증 fxtest(검 낌 → hero_swordthrust를 제 폭으로, 권갑 → hero_idle, 붕산장 castN 6).

# v2.90 — 업적 (사용자 "업적 메뉴 채우기 → 2 1하자", 2026-09-16)

- **업적 11종 · 50단계**: 백인참(처치)·수호자 격파(보스)·경지·천하 유람(사냥터)·장비 도감·명품(낀 장비 등급)·단조(합성)·연마(강화)·박학(무공)·
  기연·칠전팔기(쓰러짐). 데이터 ACHV(00-data), 모듈 src/69-achv.js(achvValue/Reached/Claim/ClaimAll/achvHud/패널), 새 누계 S.merges·S.levels,
  받은 단계 S.achv(저장), 알린 단계 S.achvNote.
- **보상 = 받는 순간의 처치 은자 × rewardMul[단계]**(8·25·70·200·600·1500) — 늦게 받아도 손해 없음. 첫 안(30~4000)은 4h sim에서 설산 3 → 4로
  한 단계 당기고 60분 잔고 1만 → 1/3로. 조정 뒤 4h 설산 4(2h까지 동일). spend()가 achvClaimAll로 받는다.
- **알림**: 새 단계에 닿으면 토스트 한 번("업적 달성 · 백인참 2단계 / ≡ 메뉴에서 받는다") + ≡ 버튼 금점(#menudot). ≡ 메뉴 '업적' 줄에 "받을 것 N".
- **패널 #vpanel**(≡ → 업적): 카드 = 이름·별(받음 ★/달성 ☆/남음 ·)·설명·진행(값/목표 또는 경지·등급 이름)·진행 바·[받기 + 은자] 버튼,
  받을 것이 위, 완료는 흐리게. 머리글에 받은 단계/전체(0/50).
- 24h sim(장신구 6자리, 업적 전): 천산 2단계(기준 v2.68 천산 1) — 설계 범위. 검증 achvtest(단계·받기·누계·알림 1회·저장·패널). 스크린샷 scratch achv_pair.png.

# v2.89 — 장신구 종류별 6자리 (사용자 확정 "장신구는 종류별로 다 하나씩 끼는 건 이상한가? → 그래 하자", 2026-09-16)

- **장착 자리 3 → 8**: 무기·방어구 + 옥패·반지·염주·부적·호리병·비단끈 각 한 자리. 데이터 EQUIP.slots[2].perKind, 68-equip
  eqWearSlots()/eqWearSlot()/eqWearKeyOf()/eqWornOf() — 자동 장착·직접 장착·벗기·기운 등급·eqBonus 전부 자리 단위. S.equip 키 8개(20-state).
- **장신구 스탯 = 종류별 프로필**(무기 v2.82와 같은 결): 옥패 치명타·반지 치명 피해·염주 회복·부적 공격력·호리병 체력·비단끈 이동 속도 0.6 +
  은자 획득 0.25 공통. 여섯 합 5.1(옛 한 자리 1.4). 실효 등급은 여섯을 따로 키워야 해서 낮다 — **sim 8시간: 기준 설산 6 → 설산 7**(4시간 설산 2 → 3), 은자 잔고 비슷. 한 단계 빠른 정도라 재조정 없음.
- 시작 장비는 탭에 낀 게 없을 때만(옥패). 옛 저장 equip.trinket {k:'ring'} → S.equip.ring 이월(65-save). 시뮬 spend는 낀 것 전부 강화.
- 패널: 장신구 탭에서 종류마다 착용 카드(금색)가 줄마다 하나씩, 위쪽 창은 누른 것(없으면 첫 낀 것). 안내 문구 한 줄.
  equiptest에 6자리 검사(동시 착용·프로필·자리별 자동 장착·기운 등급·이월). 스크린샷 scratch trinket6.png.

# v2.88.1 — 체력바 글자 잘림 · 기운을 모든 동작에 (사용자 "글씨 짤림" · "오라가 모든 모션에 들어가야지", 2026-09-16)

- 기운이 대기·질주에만 붙었다 → drawAuraGlow 헬퍼로 공격·시전·피격·운기조식·경공(날기·착지)까지 전 동작. 보스 등장 연출 중만 끈다. fxtest에 atk·hit·medit 검사.

- 상단 바 체력바 7px 안의 8.5px 숫자("111만 / 111만")가 위아래로 잘렸다 → #hp 11px + 글자 9px. 바 높이가 4px 늘어 원경은 자동으로 그만큼 내려간다(uiTopUnits).

# v2.88 — 몸 뒤 기운을 장비 등급색으로 (사용자 확정 "오라는 장비 등급색으로 바꾸자", 2026-09-16)

- 경지 4색(절정 흰·화경 금·생사경 청·신화경 자주) → **낀 장비 세 자리 중 최고 등급의 색**(EQUIP.grades[g].c). 희귀부터 보이고
  등급이 오를수록 짙다(HFX.auraGrade — 일반·고급은 빛 없음, 아이콘 프롬프트와 같은 결). auraKey()가 {col,a}를 주고 50-render auraGlow가
  **이 컷의 실루엣을 등급색으로 8방향×3px 번지게 한 발광 캔버스**((동작·컷·색)마다 캐시, 'lighter' + 맥동)를 몸 뒤에 얹는다.
  옛 안개 스트립(aidle_*)에 색을 입히는 첫 시도는 폐기 — 평균 알파 6에 '흰' 안개가 실은 회보라(109,86,129)라 물들여도 안 보였다.
  **draw() 헬퍼는 캔버스를 거른다**(complete·naturalWidth 없음) — 캔버스는 ctx.drawImage로 직접. eqAuraGrade(68-equip).
  aidle_* 4장은 보관만. 검증 fxtest 4)(P.auraCol). 스크린샷 scratch aura_grades.png.
- 그림 토스트를 노드로 조립 — innerHTML이 sim DOM 스텁에서 자식을 안 만들어 rollDrop→toast가 sim을 터뜨렸다(v2.86 회귀, sim 30분 정상).

# v2.87.5 — 원경 선 더 옅게 · 죽림 원경 축소 (사용자: "아직도 선이 있어 좀 더 옅게 · 죽림만 윗부분이 넓어 보여", 2026-09-16)

- 남아 있던 선 둘: ① **불투명 첫 줄의 테두리 줄**(죽림 156 vs 197, 폐촌 81 vs 144, 설산 209·227, 천산 223) — 상단 바 바로 아래에 걸렸다,
  ② **띠 경계 단차**(디졸브 중간 알파 .5~.7에서 9~15 밝기 차가 선으로). 새 `bg_fix.py`(assets/bg_<zone>.png 제자리 손질): 위 테두리 줄
  투명화(다음 3줄 평균과 8↑ 차) · 띠 위 6줄의 1px 이상줄 메움 · 띠 경계 ±5줄 선형 블렌드 · `--crop-top=N`. **한 시트에 한 번만** 돌린다
  (블렌드가 누적된다 — 이번엔 두 번 돌아가 조금 더 부드러워졌을 뿐).
- **죽림 원경 축소**: 빈 안개 24줄 crop-top + BACKDROP.h.bamboo 0.26(그림 253→220px, 지평선 299→269 — 다른 구역 241~281과 비슷).
- 그림 토스트 글자 nowrap("일반 부채 획/득" 두 줄 방지). 스크린샷 scratch hz_pair3·zones5c·tops5.png.

# v2.87.4 — 원경 아래 선 제거 (사용자: "죽림과 천산 원경 아래 선이 보이는데 거슬림", 2026-09-16)

- **죽림**: 시트 293번째 줄에 어두운 1px 줄(테두리 잔재, 밝기 119 vs 155)이 디졸브 중간(알파 .57)에 걸려 선으로 보였다 → 위아래 줄 평균으로 메움
  (assets/bg_bamboo.png, bg_extract와 같은 팔레트 저장).
- **천산**: 아래 균일 띠(bg_extract --pad 48줄 + 시트 자체 15줄 = 63줄 ≈ 44px)가 디졸브(35px)보다 두꺼워 띠 윗변이 불투명 선으로 남았다 →
  ① 시트 아래 40줄 잘라 띠 23줄로(그림 7% 커짐, 366→326), ② **디졸브 길이 = max(fadeR×높이, 띠 높이 + fadePad 16)** — backdropScaled가 축소
  캔버스에서 아래 균일 띠 높이(줄 평균 밝기 차 ≤3, bdBand)를 재고 backdropFade가 쓴다. horizonY도 같은 길이. 5구역 재확인 scratch zones5b.png.

# v2.87.3 — 장신구 아이콘 7등급 완성 (신화·초월, 2026-09-16)

- 신화(5) `eqicons.py 5 --slot=trinket`, 초월(6) `--pale` 신설: 옅은 얼음색 아이템 + 흰 후광 시트에서 이웃 차 번짐(--flood)은 후광에서
  아이템 몸통으로 매끄럽게 이어져 안까지 먹었다(염주 알·부적 몸통·호리병 윗불 사라짐). --pale은 **자주 계열(r·b > g+20) 또는 순백** 픽셀만
  지나는 번짐(binary_propagation) — 얼음색은 g가 r보다 커서 안 걸리고, 안쪽 흰 하이라이트는 외곽선에 갇힌다. 검사판 review/eq_trinket_5·6.png.
- 이로써 장비 16종 × 7등급 = 112종 아이콘 전부 등급별 그림. 빌드 14.9MB(한도 16MB).

# v2.87.2 — 장신구 전설·영웅·고급 아이콘 · 복귀 카드와 기연 카드 같은 자리 (사용자, 2026-09-16)

- 장신구 고급(1)·영웅(3)·전설(4) 6종씩 반영(시트는 jsonl 복원). 전설은 순마젠타 테두리 + 연분홍 칸이라 `--flood`가 테두리에서 멈췄다 →
  eqicons.py: 색 규칙이 잡은 마젠타에 닿은 밝은 픽셀도 번짐 씨앗으로(2px 팽창), bg = 번짐 ∪ 색 규칙. 검사판 review/eq_trinket_1·3·4.png 깨끗.
- **복귀 카드·기연 카드 같은 자리**("수련 계속과 기연이 뜨는 위치를 같게 해서 바로바로 클릭"): #opanel도 바 아래 84px에서 시작(가운데 정렬 폐지),
  .ocard 폭 300·min-height 404·flex 세로, 버튼은 margin-top:auto + 같은 패딩 → #obtn·#fbtn 사각형이 완전히 같다(68,429,254×42, scratch cards_pair.png).
  savetest·fatetest·equiptest 통과.

# v2.87.1 — 장신구 아이콘 일반·희귀 등급 (사용자 시트 "1"·"3", 2026-09-16)

- 희귀(2): sheets/eq_trinket_2.png(jsonl 복원) → eq_*_2 6종, 검사판 review/eq_trinket_2.png 깨끗. 고급("2")·영웅("4")은 작업 중간에 붙여넣어
  기록에 안 남아 재전송 필요.

- sheets/eq_trinket_0.png(대화 기록 jsonl에서 복원 — 붙여넣기 그림이 uploads에 안 남았다) → `python eqicons.py 0 sheets/eq_trinket_0.png --slot=trinket`
  → eq_pendant/ring/beads/talisman/gourd/ribbon_0. 검사판 review/eq_trinket_0.png(끊김·잔재 없음). 장신구 탭 일반 줄이 새 아이콘으로 뜬다
  (scratch trinket_tab.png). 나머지 등급은 옛 기본 아이콘 그대로(시트 오면 같은 명령). equiptest·test 통과.

# v2.87 — 빙백령 반전 컷 · 표범·풍뎅이 축소 · 암향지 방향 · 장신구 프롬프트 · 무기 덮어씌우기 시험 (사용자, 2026-09-16)

- **빙백령 공격 때 반대 보기**: 시트의 cast3 컷 하나만 왼쪽을 보고 있었다(review/spirit_dir.png) — 좌우 반전해 저장(review/spirit_fixed.png).
- **표범·풍뎅이 너무 큼**: FOES에 종류별 그리기 배율 `sc`(표범 0.72·등딱지벌레 0.74·옥갑충 0.78). drawFoe가 그림·그림자·체력바에
  같이 곱한다. 판정 거리(range)는 그대로. 대조 scratch size_now.png.
- **암향지 시전 때 휙 돌아섬**: "가장 먼 적"이 등 뒤면 돌아서 쏘고 돌아왔다 → 보고 있는 쪽의 가장 먼 적을 먼저, 앞에 없을 때만 뒤(30-combat pick).
- **장신구 아이콘 프롬프트** docs/프롬프트-아이콘.md 11(옥패·반지·염주·부적·호리병·비단끈, 7등급).
- **무기 덮어씌우기 시험**(반영 안 함): review/weapon_overlay2.png — 등급색 오라는 실루엣 발광으로 잘 된다. 무기 아이콘을 줄여 얹는 건
  뭉개지고 판자처럼 붙어 못 쓴다 → 주인공 배율 옆모습 '든 무기' 미니 시트가 필요(프롬프트 12).

# v2.86 — 장비 획득 팝업에 그림 · 스킬 심화 카드에 무공 메달 (사용자, 2026-09-16)

- **그림 토스트**(60-ui toast(msg, {icon, color, sec})): 아이콘이 있으면 왼쪽 액자(44px, 픽셀) + 글자, 테두리·빛은 등급색.
  장비 드랍(rollDrop)·상세 합성(eqdmerge)이 eqIcon(k,g)·EQUIP.grades[g].c로 띄운다(2.2초). 옛 글자 토스트는 그대로.
- **스킬 심화 카드 머리**(67-treepanel renderTraits): 한자 대신 무공 메달 art_*(문파색 테두리), 오른쪽 끝에 문파 엠블럼 sch_*.
  메달 없는 무공은 옛 한자. 스크린샷 scratch pic_both.png. lint·test·equiptest·artstest 통과.

# v2.85 — 상단 UI바 + ≡ 메뉴 · 자동 알약 불 켜짐 (사용자, 2026-09-16)

- **상단 바 #topbar**("원경 위쪽에 안 쓰는 부분이 있네 거기 가리고 UI바 만들어"): 원경 그림 윗부분(빈 안개·하늘)을 불투명 띠
  (어두운 그라데이션 + 아래 테두리)로 덮고 v2.84의 얇은 HUD(#card)를 그 안에 넣었다. 오른쪽에 **≡ 버튼**(#menubtn, 열리면 ✕로 변형).
  **원경은 안 가린다**(사용자 정정 "원경 위쪽 반복 텍스처 쓰는 곳 가리고 거기 둬라, 원경 가리지 말고"): 원인은 원경 시트의
  **투명 윗줄**(죽림 32·폐촌 83·설산 32·천산 63줄, 동굴 알파 경사) — 거기로 바닥 타일이 비쳐 "반복 텍스처"로 보였다.
  50-render backdropScaled가 축소 캔버스에서 투명 띠 높이(bdClear)를 재고, `backdropTop` = max(바 − 투명 띠, 지평선 − 높이)로
  **불투명 첫 줄이 바 바로 아래** 오게 둔다(죽림은 그림이 47px 내려감, 폐촌 14px). 하늘색은 투명 띠까지 채워 어디서도
  바닥이 안 비친다. 지평선·소품 컬링(horizonY)도 같이 내려간다. 바 높이는 #topbar.offsetHeight를 캔버스 단위로 환산(uiTopUnits,
  jsdom 0). 스크린샷 scratch bar_pair.png(죽림·동굴).
- **≡ 메뉴 #mpanel**(바 아래 오른쪽 드롭다운, 바깥 누르면 닫힘, 다른 시트와 배타): 설정 — 효과음 켬/끔(S.mute, 저장·sfx 게이트)·
  지금 저장 / 기록 — 업적(준비 중, 비활성)·여정(처치·쓰러짐·기연 토스트) / 판(GAME_VER). hud()의 panelOpen·tbtn 숨김 목록에 mpanel 추가.
- **GAME_VER**: build.py가 VERSION.md 첫 제목의 판번을 `const GAME_VER`로 주입 — 손으로 안 맞춘다(테스트는 dist를 읽으니 그대로 보임).
- **자동/수동 알약**("자동일 때 불이 들어오게"): 자동 = 초록 발광 + 점 맥동(.smode.auto), 수동 = 회색 + 꺼진 점. 옛 호박색 수동은 폐기.
- 기연 카드 top 78 → 84px(바 아래). lint·test·uitest·screens·savetest·fatetest·artstest 통과.

# v2.84 — 상단 HUD를 얇은 투명 띠로 (사용자: "위에 체력바 등이 배경을 좀 가리네", 2026-09-16)

- #card의 불투명 상자(배경·테두리·패딩)를 없애고 제목 한 줄 · 체력바 7px · 수련 바 3px · 경지·은자 줄로 압축 — 높이 ~105 → ~62px.
  글자는 그림자(0 1px 2px + 0 0 6px)로 밝은 원경 위에서도 읽힌다. 바는 반투명 어두운 트랙 + 1px 그림자 테두리.
- 원경(대나무·안개)이 HUD 뒤로 온전히 보인다. lint·test·uitest·screens 통과. review: scratch hud_top.png.

# v2.83 — 기연 버튼 튼튼하게 · 장비 위쪽 창 = 누른 장비 · 착용 카드 금색 (사용자, 2026-09-16)

- **기연 "받아들인다"**("동작 안 함"): 실기 재현에선 마우스·터치·긴 누름 모두 눌렸지만 취약점 둘을 막았다. ① stepFate가 매 프레임
  innerHTML을 갈아끼워 손가락이 닿은 <span>이 떼기 전에 사라질 수 있었다 → 초가 바뀔 때 <i> 글자만 갱신. ② #fpanel에 z-index 40 —
  패널·스킬창 위에 확실히.
- **장비 위쪽 창 = 누른 장비**(없으면 낀 것): 등급·이름·Lv/보유·장착 스탯(→ 다음 Lv)·보유 스탯·이 자리 보유 합, 행동 버튼
  (장착 / 착용 중·벗기 · 강화 · 합성)이 전부 위쪽 창에. 아래 등급 줄 밑 상세 칸은 뺐다(카드를 누르면 스크롤 맨 위로).
- **착용 카드 금색**(#ffd95e 테두리·발광·"착용" 글자), 위쪽 창 아이콘도 착용이면 금색 + "착용 중" 태그.
- equiptest 갱신(미보유 카드 → 위쪽 창 미보유·장착 비활성, 낀 카드 → 착용 중). fatetest·test·savetest·lint 통과.

# v2.82.9 — 방어구 아이콘 초월(등급 6) 4종 반영 — 방어구 7등급 × 4종 완성 (사용자 시트 sheets/eq_armor_6.png, 2026-09-16)

- 초월 시트는 배경이 옅은 분홍 그라데이션 + 흰 후광이라 색 규칙이 양쪽으로 틀렸다(후광이 그림으로 남고, 도복의 분홍빛 하이라이트가
  배경으로 먹혀 구멍). eqicons.py에 `--flood`: 시트 가장자리에서 밝은 픽셀을 이웃 채널 차 ≤26으로 번져 들어가 배경을 잡는다 —
  어두운 외곽선에서 멈추고 안쪽 구멍엔 안 닿는다. 검사판 review/eq_armor_all.png(7등급 28종).
- 무기 42 + 방어구 28 = 70장 등급 아이콘. 남은 건 장신구 6종(프롬프트는 요청 시).

# v2.82.8 — 방어구 아이콘 신화(등급 5) 4종 반영 (사용자 시트 sheets/eq_armor_5.png, 2026-09-16)

- `python eqicons.py 5 --slot=armor`. 화염 오라는 주황이라 살아남았다. 검사판 review/eq_armor_5.png. 초월 1장 대기.

# v2.82.7 — 방어구 아이콘 전설(등급 4) 4종 반영 (사용자 시트 sheets/eq_armor_4.png, 2026-09-16)

- `python eqicons.py 4 --slot=armor`. 흰금 오라·반짝임은 배경으로 걷힘. 검사판 review/eq_armor_4.png. 신화·초월 2장 대기.

# v2.82.6 — 방어구 아이콘 영웅(등급 3) 4종 반영 (사용자 시트 sheets/eq_armor_3.png, 2026-09-16)

- `python eqicons.py 3 --slot=armor`. 검사판 review/eq_armor_3.png. 전설·신화·초월 3장 대기.

# v2.82.5 — 방어구 아이콘 희귀(등급 2) 4종 반영 (사용자 시트 sheets/eq_armor_2.png, 2026-09-15)

- `python eqicons.py 2 --slot=armor`. 도롱이 옆 반짝임은 배경으로 걷힘. 검사판 review/eq_armor_2.png. 영웅~초월 4장 대기.

# v2.82.4 — 등급 아이콘을 팔레트 PNG로 (빌드 15.0MB → 14.6MB, 2026-09-15)

- 등급 아이콘 50장이 RGBA 7~9KB였고 빌드가 16MB 한도에 근접했다(방어구·장신구 62장이 더 올 예정). eqicons.py가 quantize(127색,
  디더 없음)+투명 인덱스 127로 저장 → 장당 ~3KB, 합 472KB→166KB. 전 등급 재추출, 육안·spritetest·equiptest 동일.
- 첫 시도는 양자화 팔레트를 새 P 이미지에 안 옮겨 아이콘이 검게 나왔다 — 팔레트는 quant.getpalette()에서 가져와 붙인다.

# v2.82.3 — 방어구 아이콘 고급(등급 1) 4종 반영 (사용자 시트 sheets/eq_armor_1.png, 2026-09-15)

- `python eqicons.py 1 --slot=armor`. 검사판 review/eq_armor_1.png. 희귀~초월 5장 대기.

# v2.82.2 — 방어구 아이콘 일반(등급 0) 4종 반영 (사용자 시트 sheets/eq_armor_0.png, 2026-09-15)

- `python eqicons.py 0 --slot=armor` → eq_robe/vest/lamellar/cloak_0. 검사판 review/eq_armor_0.png. 고급~초월 6장 대기.

# v2.82.1 — 합성은 낀 것도 재료 · 착용은 "얻어 본 적"에 묶임 (사용자: "낀 것도 업글해도 되지, 한 번 얻은 건 열어주는 식으로", 2026-09-15)

- v2.82의 '낀 것 제외' 폐기. 합성은 개수만 본다(3개→1개, 낀 것 포함). 대신 **착용은 도감(eqSeen)에 묶여** 낀 것을 합성해 개수가
  0이 돼도 계속 끼고 있고, 얻어 본 장비는 언제든 다시 낀다(eqWear·eqBest·상세 장착 버튼). 한 번 얻은 건 열린다.
- equiptest 갱신(합성 뒤 착용 유지·개수 0 재장착·미획득 장착 거부). sim 15분 폐촌3(자동 장착이 얻어 본 것 전부에서 고르니 살짝 빠름).

# v2.82 — 장비 패널 4가지 (사용자: 안 가진 장비도 눌러 보기 · 무기별 스탯 조합 · 합성 때 낀 것 제외 · 행동창 위치, 2026-09-15)

- **안 가진 장비도 누르면 상세**(미보유 표시·Lv 상한·장착/보유 스탯을 Lv0 기준으로) — "얼마나 센지 봐야지". 버튼은 비활성(흐리게).
- **무기별 스탯 조합**(EQUIP.profile, kindEff/itemStats): 권갑 공격 .85·공속 .5·이속 .3 / 검 공격 1·치명 .45 / 도 공격 1.1·치명피해 .45 /
  창 공격 1·치명 .25·치명피해 .3 / 봉 공격 .8·체력 .45·회복 .3 / 부채 공격 .75·은자 .5·이속 .25 — 장착 효과(등급×레벨 %)에 곱한다.
  합 1.4~1.6으로 옛 '주 1.0+부가 0.4'와 같은 급. 보유 효과도 같은 조합을 탄다(codexStat). 방어구·장신구는 옛 규칙(주 1 + 부가 subRate).
  eqBonus가 profile을 읽는다. sim 15분 동일(죽림7→폐촌2).
- **합성은 착용 중인 것을 재료에서 뺀다**(eqSpare/eqWornN) — "합성하면 장비가 하나도 없어지는 건 이상". 낀 것 하나는 남는다.
  버튼 문구 '(낀 것 제외)'. mergeCount·일괄 합성도 여분 기준.
- **상세·행동창은 누른 카드의 등급 줄 바로 아래**에 뜬다(buildEquipPanel이 그 줄 뒤에 #eqdet를 둔다) — "맨 아래 뜨니까 불편".
- 낀 것 줄·상세는 스탯 줄을 조합대로 전부 보여 준다. equiptest 갱신(낀 것 제외 합성·조합·미보유 계산). review/ingame_eqdet.png.

# v2.81.3 — 무기 아이콘 초월(등급 6) 6종 반영 — 7등급 × 6종 무기 아이콘 완성 (사용자 시트 sheets/eq_weapons_6.png, 2026-09-15)

- 하늘색 오라·후광 고리는 배경(분홍)과 갈려 그대로 살았다. 권갑은 오라로 두 장갑이 한 덩어리. 검사판 review/eq_weapons_all.png(7등급 42종).
- 무기 자리는 이제 모든 등급이 제 그림이다. 방어구·장신구는 종류 기본 아이콘 그대로(등급별 시트가 오면 같은 eqicons 틀로 — KINDS만 바꿔).

# v2.81.2 — 무기 아이콘 신화(등급 5) 6종 반영 (사용자 시트 sheets/eq_weapons_5.png, 2026-09-15)

- 화염 오라는 주황이라 그대로 살았고, 봉 옆 토막은 50% 규칙으로 걷혔다. 검사판 review/eq_weapons_all.png(6등급). 남은 시트: 초월(7).

# v2.81.1 — 무기 아이콘 전설(등급 4) 6종 반영 (사용자 시트 sheets/eq_weapons_4.png, 2026-09-15)

- 옅은 분홍 배경의 흰금 오라·반짝임은 배경 판정에 걷혔다(아이콘엔 얇은 밝은 테두리만 남음 — 등급 색 테두리가 발광을 대신한다).
- eqicons.py: 칸 안 가장 큰 덩어리의 50% 미만 조각은 버린다 — 전설 봉 시트가 봉 옆에 짧은 토막(0.28)을 따로 그렸다. 권갑 두 장갑(0.94↑)은 남는다.
  등급 0~4 전부 재추출(0~3 결과 동일). 검사판 review/eq_weapons_all.png(5등급). 남은 시트: 신화(6)·초월(7).

# v2.81 — 장비 등급 7단계: 전설 위에 신화·초월 (사용자: "전설 다음 두 단계 더 만들자", 2026-09-15)

- EQUIP.grades에 **신화**(#ff6b81, 장착 62%, Lv 상한 120)·**초월**(#9df5ff, 장착 85%, Lv 상한 140). 기존 5등급 8/14/22/32/45의 증분(6·8·10·13)을
  이어 17·23. 총 16종×7등급 = 112종.
- gradeW 7칸: 신화는 천산에서만 1%, 초월은 드랍 없이 **신화 3개 합성으로만**. 전설도 이제 합성된다(→신화).
- 저장은 NG(등급 수) 기준이라 옛 5칸 배열이 0으로 늘어난다(65-save 그대로). 아이콘은 eq_<종류>_5/_6이 오기 전까지 종류 기본.
- equiptest: 카드 42·전설 합성 가능·초월 합성 불가·옛 저장 등급 잘림 6. sim 15분 동일(죽림7→폐촌2).
- 프롬프트: docs/프롬프트-아이콘.md 9 (신화 = 진홍·흑 화염 결, 초월 = 수정·별빛 결).

# v2.80.3 — 무기 아이콘 영웅(등급 3) 6종 반영 · 아이콘 추출도 그림 기준으로 (사용자 시트 sheets/eq_weapons_3.png, 2026-09-15)

- 영웅 시트는 검이 옆 칸까지 넘어가 있어 칸 단위 크롭에선 칼끝이 도 칸에, 자루 끝이 권갑 칸에 조각으로 붙었다. eqicons.py를
  **시트 전체 덩어리 라벨링 → 픽셀이 가장 많은 칸에 통째로**(hero_sheet와 같은 원칙)로 바꿨다. 등급 0~3 전부 재추출(0~2 결과 동일).
- 검사판 review/eq_weapons_all.png(4등급 한눈에). equiptest·test 통과. 남은 시트: 전설(등급 4).

# v2.80.2 — 무기 아이콘 희귀(등급 2) 6종 반영 · 구분선 제거 (사용자 시트 sheets/eq_weapons_2.png, 2026-09-15)

- 희귀 시트는 배경이 옅은 분홍이고 칸 구분선이 **흰색**이라 배경 판정에 안 걷혀 아이콘 옆에 흰 막대로 남았다 — eqicons.py가
  '칸 높이 85%↑·폭 24px↓ 세로 덩어리'를 구분선으로 버린다. 일반·고급도 같은 규칙으로 다시 뽑았다(결과 동일).
- 검사판 review/eq_weapons_2.png. equiptest·test 통과.

# v2.80.1 — 무기 아이콘 고급(등급 1) 6종 반영 (사용자 시트 sheets/eq_weapons_1.png, 2026-09-15)

- `python eqicons.py 1` → eq_<종류>_1 6장. 부채 칸의 분홍 반짝임은 배경 판정에 걷혀 빠졌다(무해). 검사판 review/eq_weapons_1.png.

# v2.80 — 무기 아이콘 등급별 시트 1(일반) 반영 (사용자 시트 sheets/eq_weapons_0.png, 2026-09-15)

- `eqicons.py <등급>`: 1줄 6칸(권갑·검·도·창·봉·부채) 시트를 배경 판정+최대 덩어리로 잘라 96×96(긴 변 92, 기존 채움 0.96)
  `assets/eq_<종류>_<등급>.png` 6장. 검사판 review/eq_weapons_<등급>.png.
- eqIcon(k, g): 등급별 아이콘이 있으면 그것, 없으면 종류 기본(eq_<종류>). 카드·낀 것 줄이 등급을 넘긴다 → 일반 등급 6종은
  새 그림, 고급~전설은 시트가 올 때까지 기본 아이콘. 권갑 기본(eq_fist, 권기 주먹 임시)은 그대로 폴백.
- equiptest·test·savetest 통과, sim 동일.

# v2.79.1 — 무공 문구에서 환산 피해 제거 · 일괄 합성 버튼 활성 색 (사용자, 2026-09-15)

- 초식 효과 문구의 "(피해 ≈N)"·회복 "(≈N)"을 뺐다 — "스킬에 피해 얼마는 적지 말자". 공격력 %·쿨만 남는다(artFxText).
- 일괄 합성 버튼은 합성할 게 있으면 **호박색 발광**(#eqmerge.on) — "활성화되면 색을 바꿔 잘 보이게". 자동 장착의 파랑과 구분.

# v2.79 — 무기 종류에 권갑(권) 추가 (사용자: "무기 종류에 권을 추가해", 2026-09-15)

- 권갑은 데이터(EQUIP.slots[0].kinds 'fist')에 있었지만 아이콘 eq_fist가 없어 eqKinds에서 숨겨져 있었다. 아이콘을 사용자
  권기 정권 시트(sheets/hero_fx.png 2줄 2칸) 원본에서 **권기 두른 주먹**만 잘라 96×96으로 만들었다(assets/eq_fist.png —
  나중에 그려 주는 eq_fist.png로 덮어쓰면 그대로 교체). 이제 무기 6종(권갑·검·도·창·봉·부채), 권갑이 첫 자리.
- 시작 장비가 권갑 일반(EQUIP.starter weapon 'fist')이 되고, 권갑은 WEAPONMOVES가 없어 **맨손 무브셋**(주먹·발차기 11동작)으로
  싸운다. 드랍·합성·강화·보유 효과(부가 효과 spd)도 다른 무기와 같다. [벗기 · 맨손]은 그대로.
- equiptest: 시작 장비 권갑·무기 6종·카드 30장·권갑 드랍. lint·fxtest·test·savetest 통과, 10분 sim 동일.

# v2.78 — 동작별 임팩트 이펙트 · 운기조식 연출 · 피부톤 전수 통일 (사용자: "공격 모션과 운기조식에 알맞게 이펙트를, 잘 어울려서 이상하지 않고 멋지게. 피부톤도 확인", 2026-09-15)

- **동작별 임팩트**(FXD.moveFx, 30-combat heroImpactFx): 기본 파열(wave·sparks)은 그대로, 참격 호 자리에 동작 성격에 맞는 결 하나.
  주먹 정권·연환권 = 충격 별(짧은 방사선 6 + 작은 고리) · 승룡권 = 위로 솟는 빛줄기 + 위로 튀는 불티 · 권기 = 파란 대파열(섬광·속도선) ·
  옆/돌려차기 = 넓은 초승달 호 · 뛰어차기 = 위로 기운 호 · 검 베기·도 베기·창 쓸기 = 사선 검흔 · 도 내려찍기 = 세로 검흔 + 땅 먼지 ·
  검/창 찌르기 = 수평 관통 섬선 · 회전(검·도·창·봉·부채) = 발밑 큰 원형 충격파 두 겹 · 봉 = 둔탁한 고리 + 흙 튐 · 부채 = 흩날리는 청록 잎 조각.
  치명타는 색만 금빛. 없는 키는 옛 참격 호. 공격 들어갈 때 뒷발 쪽 **발밑 흙먼지**(stepdust, 가산 아님).
- 새 렌더러(50-render drawFx): streak(검흔·관통선·빛줄기, 흰 심+색 여운) · petals(잎 조각 호) · stepdust. rays에 n·len·짧은 뻗음,
  sparks에 up(위로), slash에 span·tiltA 오버라이드. 그라디언트 없음(jsdom 안전).
- **운기조식**(drawMeditAura + drawQi 빛알): 몸 뒤 온기 후광(동심원 세 겹, 맥동) · 발밑 광륜(숨 쉬듯) · 6컷 루프 위상에 맞춰 발밑에서
  퍼지는 호흡 고리 · 몸 둘레 반짝이는 빛알 6. 기존 기운 방울은 유지. 끝날 때 함께 잦아든다(fade).
- **피부톤**: 질주 외 전 스트립(무기 12·옛 발차기·경공·대기·피격·운기조식·권기)에 skinmatch를 한 번 더 걸어 밝은 살색을 정확히
  (225,160,132)로 통일(옛 발차기·경공이 7 밝았다). cast*는 금빛 발광이라 제외.
- fxtest: 동작 11종 임팩트 결 검사·흙먼지·운기조식 원 개수. review/ingame_fx_moves.png·ingame_fx_medit.png.

# v2.77 — 운기조식 시트 반영 (사용자 시트 sheets/hero_medit2.png, 2026-09-15)

- `hero_medit2.py`(gridless, 기준 컷 (0,0) 서기): medit [(0,2),(1,1),(1,2),(1,3),(1,4),(1,5)] 가부좌→기운 한 줄기→고리→고리
  여럿→손안 빛→가부좌, 6컷 루프. 폭 46(HFX.aw.medit), ANIM.medit [6, 3.2]. 확대 컷 (0,5)·(2,3)과 온몸 발광 (2,2)은 제외.
- 주먹4 계열 톤이라 `skinmatch.py medit`으로 질주 톤에 맞춤. 부유 조각 patch. 옛 4컷은 raw/medit_old.
- 코드 기운 방울(drawQi)은 그대로 얹힌다. fxtest 운기조식 6컷 검사 추가. spritetest·medtest·test 통과.
- 이제 옛 시트 스타일 컷은 **초식 시전(cast*)** 만 남았다.

# v2.76.9 — 무기 벗기 · 맨손 (사용자: "무기 중에 권이 없어서 주먹 모션을 못 봐", 2026-09-15)

- 권갑(eq_fist) 아이콘이 없어 무기 자리에 권 종류가 안 보이고, 검 등을 한 번 끼면 맨손으로 돌아갈 길이 없었다 — 주먹·
  발차기 무브셋(11동작)을 볼 수 없었다. `eqUnwear(slotK)` + 장비 탭 무기 자리에 **[벗기 · 맨손]** 버튼(낀 것 줄 오른쪽, 상세의
  '착용 중' 자리). 맨손이면 낀 것 줄에 "맨손 — 주먹·발차기로 싸운다". 자동 장착은 버튼을 눌러야만 다시 낀다.
- equiptest: 벗기→맨손·중복 벗기 거부·다시 끼기. fxtest 맨손 pool 확인. 권갑 아이콘이 오면 권갑 장착 = 같은 맨손 무브셋.

# v2.76.8 — 모든 동작 키 통일 · 질주 머리 잘림 복구 (사용자: "공격 모션이랑 뛸 때랑 크기가 달라, 머리 잘린 거 있는 것 같은데", 2026-09-15)

- **키 기준으로 통일**: 질주 scale_mul 0.85→1.02(키 39→46~47), 권기 0.86→0.94(키 40→44~45). 다른 동작은 44~47.
  머리 폭 기준 축소(v2.76.3·v2.76.6)는 키를 줄여 순환 중 인물이 작아졌다 커졌다 했다 — **크기 통일이 머리 비율보다 먼저**.
  질주 캔버스 52(위 여분 1, HFX.fh.run)·폭 34, 권기 캔버스 53(아래 여분 2 흙먼지)·폭 74. spritetest ANIM 검사가 fhOf를 본다.
- **머리 잘림**: 질주 시트 4칸은 머리끈이 칸 위 액자선 위까지 올라가 있어, 줄 띠(±2px) 제거 때 머리 위 2~4행이 같이
  지워졌다(3·4컷 정수리가 평평). hero_sheet: 가로 줄 띠 바로 밖 행에 그림이 있는 열은 띠 안의 어두운 비배경 픽셀을 이어진
  만큼 되살린다(세로 줄은 무기 끝에 줄 조각이 붙을 수 있어 제외). 시트 가장자리 줄 인덱스 범위 가드.
- 검사: 전 주인공 스트립 키·위여백 표(VERSION 참조 스크립트), blackcheck(run·qipunch), ingame_run.png. lint·spritetest·
  fxtest(3회 연속 통과 — 1회 간헐 실패는 기존 타이밍 플레이크)·test·animtest 통과.

# v2.76.7 — 톤 기준을 질주 컷으로 뒤집음 · 모든 동작 통일 (사용자: "걷고 뛰는 거에 톤을 다 맞춰, 모든 동작", 2026-09-15)

- v2.76.4~6은 새 시트(주먹4·발차기3) 톤으로 옛 것을 맞췄는데 사용자가 **질주 톤**을 기준으로 골랐다. 팔레트 조사 결과
  질주·옛 발차기(kick2)·권기(hero_fx)·경공·무기 4종(검4·도3·창3·봉3)은 원래 같은 톤이고(살 (225,160,132)·옷 파랑 (75,80,121)·
  도복 (212,191,167)), **주먹4·발차기3·부채3(+대기·피격)** 만 딴 톤(황갈 살 (203,139,125)·회청 옷 (88,78,112)·어두운 도복
  (217,182,155))이었다 — 다수가 질주 쪽이라 이 방향이 맞다.
- 옛 스트립 8종은 원본으로 되돌리고(skinmatch 미적용), skinmatch.py를 **질주 톤 → 새 시트 11종**(punch·punchdbl·punchup·
  kickside·kickround·kickhigh·idle·hit·fansweep·fanspin·fanstrike)으로 다시 썼다. 살색·옷·도복 세 마스크.
  도복 마스크는 r<=226·g<=200으로 크림 초승달을 제외(처음엔 초승달이 '밝은 30%'가 돼 도복이 어두워졌다).
- review/tone_compare.png(10동작 상반신)·ingame_run.png·blackcheck 확인. 전 검사 통과.

# v2.76.6 — 질주 컷 크기·옷색을 새 시트에 맞춤 (사용자: "걷고 뛰는 거 봐봐", 2026-09-15)

- 질주(hero_run2)는 머리 폭 27px(새 시트 20~23)로 컸다 — 질주 시트도 머리 큰 비율인데 서 있는 기준 컷이 없어 STAND_H
  그대로 뽑혔다. hero_run2.py에 scale_mul 0.85 → 머리 23, 폭 34→30(HFX.aw.run).
- **옷색**: 옛 시트(질주·발차기2·권기·경공)는 허리띠·바지가 파랑 (80,92,140), 새 시트는 회청 (88,80,112) — 순환 중 옷이
  파래졌다 돌아왔다 했다. skinmatch.py에 옷 단계 추가(cloth_mask: b>r+20·b>g+10, 기운 이펙트는 b>150·g>110으로 제외 →
  CLOTH_TARGET (88,80,112)로 비율 이동). 기본 대상에 run 포함, 8종 전부 재적용(살색+옷색).
- review/ingame_run.png(대기·질주 6컷·정권·옆차기 새/옛·권기·경공)·blackcheck로 확인. 전 검사 통과.

# v2.76.5 — 얼굴 톤 전부 통일 (사용자: "얼굴색 다시 봐봐", 2026-09-15)

- v2.76.4의 기준색 (210,178,146)은 얼굴이 아니라 **도복 베이지**였다(r-g 20 마스크가 도복을 살색으로 셌다) — 권기 얼굴이
  오히려 더 창백해졌다. 새 시트(주먹4·발차기3)의 얼굴은 **불그레한 황갈 (203,138,125)**(밝은 30% 평균, r-g>40 마스크).
- 세 톤이 섞여 있었다: 주먹4·발차기3 황갈 / 주먹2(대기·피격) 창백한 베이지 / hero_fx·발차기2 주황. 통일:
  옛 스트립 7종은 skinmatch TARGET (203,138,125)로 되돌려 재적용, **대기·피격은 주먹4 시트에서 재추출**(idle (0,0) 24폭,
  hit (2,3) 방어→(0,0) 34폭 — hero_pose2). 질주(202,142,120)·운기조식은 이미 같은 계열. 시전(cast*)은 금빛 발광이라 제외.
- review/face_compare.png(얼굴 10종 확대)·review/ingame_faces.png로 확인. 전 검사 통과.

# v2.76.4 — 옛 스트립 살색을 새 시트 톤으로 (사용자: "권기 중 피부색이 오묘하게 다른 게 있더라", 2026-09-15)

- 옛 시트(hero_fx 권기·hero_kick2 발차기·경공 컷)는 살색이 (224,152,120) 주황빛, 새 시트(hero_punch4·hero_kick3)는
  (208,176,144) 베이지 — 순환 중 권기·옛 발차기 차례에 얼굴 색이 바뀌어 보였다.
- `skinmatch.py`: 살색 픽셀(r>140·r-g>40·g-b>15 — 흙먼지·도복은 r-g 20 안팎이라 안 잡힘)을 밝은 살색 평균 → TARGET
  (210,178,146)으로 채널별 비율 이동(그늘은 같은 비율로 따라감). qipunch·qipunchb·kickside2·kickround2·kickhigh2·
  dashfly·dashland 7종 적용. 첫 시도는 새 시트에서 기준색을 자동으로 쟀다가 도복 베이지와 겹쳐 그늘만 잡혀 어두워졌다 —
  원본에서 되돌리고 기준색을 명시.

# v2.76.3 — 권기 정권은 마무리 일격으로 맨 끝 · 머리 크기 축소 (사용자: "기 모아서 치는 느낌인데 머리가 왜 이리 크냐", 2026-09-15)

- hero_fx 시트는 머리 큰 치비 비율 — 몸 높이 47로 맞추면 머리 폭 27px(새 시트 20~23). hero_sheet.extract에 `scale_mul`을
  두고 hero_punch3를 0.86으로 재추출 → 머리 폭 24, 몸 40. 폭 72, 높이 51(fh 삭제).
- ATKMOVES 순서: 권기 오른손·왼손을 **맨 끝**으로 — 주먹·발차기를 다 낸 뒤 기 모아 치는 마무리 일격. fxtest에
  "한 바퀴의 마지막 두 타 = qipunch·qipunchb" 검사 추가.
- docs/프롬프트-주인공.md "다음에 그릴 것": A 권기 정권 재작업(새 계열) · B 운기조식 · C 초식 시전 5종 · D 권갑 아이콘 · E 경공.
  공통 규칙에 "이펙트는 파랑·청록·흰·금 — 자주·분홍·마젠타 금지"(잔상 컷이 배경에 먹힌 v2.76 교훈).

# v2.76.2 — 옛 동작을 버리지 않고 무브셋에 합침 (사용자: "기존 것들도 잘 살려서 넣어", 2026-09-15)

- 되살린 5종: **권기 정권 오른손·왼손**(qipunch·qipunchb — hero_fx 시트, v2.74.1 hero_punch3 추출분)과
  **옛 발차기 3종**(kickside2·kickround2·kickhigh2 — hero_kick2 시트, v2.71). assets에 새 키로 복사(raw 보관본 그대로).
- ATKMOVES 11종, 주먹·발차기 번갈아 배열: 정권·옆차기·연환권·옛 옆차기·승룡권·권기 오른손·권기 왼손(need 0) ·
  돌려차기·옛 돌려차기(5) · 뛰어차기·옛 뛰어차기(10). 맨손도 처음부터 7동작 순환.
- loadImg 5종, HFX.aw/fh 등록, spritetest·fxtest(무브셋·pool·스트립 폭·14회 연속 공격 전 동작 순환) 갱신. 전 검사 통과.

# v2.76.1 — 발차기 3종 시트 반영 (사용자 발차기 시트 sheets/hero_kick3.png, 2026-09-15)

- **옆차기·돌려차기·뛰어차기** — `hero_kick3.py`(gridless, 3줄×6칸, hero_punch4와 같은 계열): 옆차기 [(0,0),(1,1),(1,2),(0,1)]
  챔버→차기→푸른 기운 임팩트→뻗음 · 돌려차기 [(2,0),(2,1),(2,2),(1,4)] 챔버→차기→작은 초승달→큰 초승달 ·
  뛰어차기 [(1,0),(2,4),(2,3),(0,4)] 챔버→도약→별 임팩트·흙먼지→착지 뻗음. 폭 70/72/70, 높이 51.
- 기준 컷 (0,0) 162px — hero_punch4의 (0,0) 161px와 같은 배율이라 주먹·발차기 인물 크기가 맞는다(처음 (0,2) 내지름
  컷을 기준으로 잡았더니 4% 커졌다 — **기준 컷은 곧게 선 자세로**).
- patch(clean): 150px 미만 부유 조각 + 자줏빛 덩어리(도약 컷 발밑 그림자, 마젠타에 물든 색) 제거.
- 옛 발차기(hero_kick2)는 raw/kick2_old 보관. spritetest fh 대응, fxtest 폭 문구 동적. 전 검사 통과.

# v2.76 — 주먹 3종 시트 반영 · 맨손도 처음부터 3동작 (사용자 주먹 시트 sheets/hero_punch4.png, 2026-09-15)

- **정권·연환권·승룡권** — `hero_punch4.py`(gridless, 3줄×6칸): 정권 1줄 [0,1,2,3] 기수식→잽→내지름→초승달 ·
  연환권 [(1,0),(1,1),(1,5),(1,3)] 잽→내지름 흙먼지→큰 원 · 승룡권 [(2,0),(2,5),(1,4),(0,4)] 뒤로 당김→치켜올림
  흙먼지→올려치기 (줄을 섞어 조합). 폭 54/56/54, 연환권 fh 53(위 2).
- 2줄 3칸 **잔상 두 방 컷은 폐기** — 잔상 픽셀이 마젠타 계열(224,0,224)이라 배경과 못 가른다(윤곽만 남음).
  얇은 흰 별빛·흙 곁 분홍 점은 patch(desmall)로 원본에서 150px 미만 부유 조각을 지웠다(크롭은 안 바꿔 정렬 유지).
- **두 판 교대(punch/punchb, P.atkAlt) 폐지** — 동작 키가 곧 스트립. ATKMOVES = 정권·연환권·승룡권·옆차기(need 0)
  ·돌려차기(5)·뛰어차기(10) 6종. 옛 권기 정권 판은 raw/punch3_old 보관, assets/punchb.png 삭제.
- spritetest 주먹 3종 · fxtest(무브셋 6종·낮은 성급 pool 4종·스트립 폭·연속 공격 순환) 갱신. lint·test·animtest·
  equiptest·savetest 통과. sim 15분 죽림7→폐촌3(진행 속도 동일 범위, 동작은 피해에 무관).

# v2.75 — 부채 시트 재작업분 반영 · 공격 스타일마다 처음부터 3동작 (사용자: "3가지 이상이어야 실감", 2026-09-15)

- 부채: 휘두르기 1줄 [1,2,3,4] · 회전 2줄 [0,2,3,4] · 찌르기 3줄 [0,1,2,3]. `hero_fan3.py`(gridless). 이로써 재작업 시트
  5장(검·도·창·봉·부채) 전부 교체 완료 — 액자선 없는 시트라 무기·기운이 온전하다.
- **동작 잠금 완화**: 무기 무브셋은 전부 need 0(무기를 든 순간부터 3동작 순환). 맨손은 양주먹 두 판(오른손·왼손 교대)
  +옆차기가 처음부터, 돌려차기 이류(5)·뛰어차기 절정(10). fxtest 갱신. sim 15분 진행 동일(죽림7→폐촌2).

# v2.74.6 — 검 찌르기·내려베기의 짧은 칼날을 이어 붙임 (사용자: "검끝이 짧은 건 어디서 잘라다가 좀 붙여라", 2026-09-15)

- 찌르기 별 컷(1줄 4칸)·내려베기 별 컷(2줄 3칸)은 별 임팩트 뒤로 칼날이 20~34px(원본)로 끝나 있었다.
  hero_sheet.extract에 **원본 해상도 손질 훅(patch)** 을 두고, hero_sword4.graft가 온전한 칼날(1줄 3칸, 55px)을
  자루 끝에 맞춰 목표 축으로 돌리고 100px로 늘려 **투명한 자리와 별(순백) 위에만** 칠한다 — 칼날이 별을 꿰뚫고
  칼끝이 별 너머로 나온다. 캔버스는 60px 여유를 두고 그린 뒤 다시 잘라 크롭 경계에 안 막힌다. 폭 90/74.

# v2.74.5 — 검 시트 2차 재작업분으로 교체 (sheets/hero_sword4.png, 2026-09-15)

- 두 검 시트 비교(review/sword34_compare.png): sword4는 검기가 다른 무기와 같은 하늘색이고 자세 기울임·임팩트가
  더 살아 있다 → `hero_sword4.py`. 찌르기 1줄 [0,2,3,4] · 베기 2줄 [0,1,2,3] · 회전베기 3줄 [0,1,2,3](땅에 꽂음).
- hero_sword3는 보관(흰 검기 판이 필요하면 되돌릴 수 있다).

# v2.74.4 — 봉 시트 재작업분 반영 (sheets/hero_staff3.png, 2026-09-15)

- 휘두르기 1줄 [0,1,3,2](연타 잔상 5칸은 반투명이라 뺌) · 쓸기 2줄 [0,1,2,3] · 회전 3줄 [0,1,2,3]. `hero_staff3.py`(gridless).
  폭 58/56/56, 휘두르기·회전 fh 52.

# v2.74.3 — 창 시트 재작업분 반영 (sheets/hero_spear3.png, 2026-09-15)

- 찌르기 1줄 [0,1,3,2] · 쓸기 2줄 [0,1,2,3] · 회전 3줄 [0,1,2,3]. `hero_spear3.py`(gridless). 폭 58, 회전 fh 52.
  옅은 마젠타 격자선은 배경 판정(자줏빛 ±70)에 걷힌다.

# v2.74.2 — 도 시트 재작업분 반영 (sheets/hero_saber3.png, 2026-09-15)

- 베기 1줄 [0,1,2,3] · 내려찍기 2줄 [0,1,2,3] · 회전베기 3줄 [0,1,2,3]. `hero_saber3.py`(gridless). 폭 60, fh 53/53/54.
- hero_sheet gridless: 옆 칸 검기가 옆 인물과 붙어 한 덩어리가 되던 것(3줄 1·2칸) — 두 칸에 각각 2000px 넘게
  걸친 덩어리는 칸 경계에서 잘라 다시 라벨링한다.

# v2.74.1 — 정권을 옛 권기 정권으로 되돌림 (사용자: "이전 시트에 쓸 만한 게 있으면 버리지 말고 조합해, 지금 주먹은 마음에 안 든다", 2026-09-15)

- 비교(review/punch_compare.png): 새 주먹 시트(hero_punch2) 1·3줄은 네 컷이 같은 자세로 팔만 뻗어 밋밋하고, 옛
  권기 정권(sheets/hero_fx 2·3줄)은 기수식→권기 내지름→흙먼지 임팩트→거둠으로 동작이 살아 있으며 그림체도 새
  시트와 같은 계열이다 → `hero_punch3.py`가 hero_fx를 새 추출기로 다시 뽑아 punch/punchb(폭 82, fh 52/55).
- hero_sheet: 큰 시트(2000px)의 12~17px 격자선 허용(maxw 20), **격자선은 칸 피치의 배수 자리 ±12%에 있는 것만**
  (인물의 어두운 열 0.72가 줄로 잡히던 것 차단), rows/cols 인자. spritetest 정권 검사가 fh를 본다.
- 대기·피격(hero_punch2 기수식)은 그대로 — 권기 정권 첫 컷과 같은 자세라 이어진다.

# v2.74 — 검 시트 재작업분 반영 (사용자 시트 sheets/hero_sword3.png — 액자선 없음·무기 온전, 2026-09-15)

- 재작업 프롬프트로 받은 첫 시트. 순마젠타 배경, 액자선·표식 없음, 무기·검기가 칸 안에 온전.
- hero_sheet **gridless 모드**: 격자선이 없으면 균등 6×3 분할로 칸을 정하고, 땅은 칸 바닥이 아니라 인물 발끝,
  배율은 기준 컷(1줄 1칸 기수식)의 인물 높이로 자동(165px → 47px, 0.285). `hero_sword3.py`.
- 검 3종 교체: 찌르기 [0,2,3,4]·베기 2줄 [0,1,2,3]·회전베기 3줄 [0,1,2,3]. 폭 84/52/54, 베기·회전은 위 여분(fh).
- 도·창·봉·부채는 같은 프롬프트로 시트가 오면 hero_<무기>3.py로 동일하게 뽑는다.

# v2.73.3 — 무기 끝 잘림의 진짜 원인: 시트의 안쪽 액자선 (사용자: "무기 끝들이 다 잘려 있어", 2026-09-15)

- review/clipped_cells.png: 무기 시트 5장은 칸마다 **안쪽 액자선**이 있고 무기·검기가 그 선에서 잘린 채 그려져
  있다(도 베기 칼끝·창 찌르기 창끝이 선에서 평평하게 끊김). 잘린 나머지는 액자 밖 여백에 파편으로 놓여 있다 —
  추출기가 그 파편을 이어 붙여 떠 있는 조각이 생겼었다. **원본이 잘린 것이라 복구 불가.**
- hero_sheet: 여백에만 있는 조각은 붙이지 않는다(칸 안 파편만). 21판+대기·피격·경공 재추출 — 떠 있는 조각은
  사라졌고, 잘린 끝은 원본 그대로다.
- 재작업 프롬프트를 docs/프롬프트-주인공.md에 적었다(액자선·표식 금지, 무기가 칸 안에 15px 여유로 다 들어오게,
  줄·칸 순서 고정). 시트가 오면 hero_<무기>2.py로 그대로 뽑힌다.

# v2.73.2 — 대기·피격·경공 컷도 새 시트로 통일 (사용자: "여전히 이상한데", 2026-09-15)

- 실기 대조로 찾은 원인: 대기(idle)·피격(hit)·경공(dashfly/dashland)·운기조식·초식 시전 컷은 옛 시트(흰 도복·연한
  바지·날씬한 체형)인데 공격·질주는 새 제미나이 시트(베이지 도복·짙은 바지·굵은 윤곽)라, 싸울 때 두 인물이 번갈아
  보였다(피격이 잦아 특히). 색 평균은 같았고(도복 ~190,170,148) 그림체 차이였다.
- `hero_pose2.py`: 대기 = 주먹 시트 1줄 1칸 기수식(정면 전투 자세), 피격 = 주먹 시트 뒤로 기운 기수식→기수식 2컷,
  경공 날기/착지 = 발차기 시트 도약/웅크림. HFX.aw.idle/hit 32, DASH fw/fh·lw/lh = 새 컷 규격(51 캔버스). 옛 컷은
  raw/pose_old/. 운기조식·초식 시전(cast*)은 새 시트가 없어 옛 컷 그대로(시전은 짧고 이펙트가 커 덜 튄다).
- 검증 spritetest·fxtest(대기 폭)·animtest·blackcheck·크로뮴 실기(8자세 나란히).

# v2.73.1 — 도복이 배경에 묻히던 것 · 잘린 칼날 (사용자: "도복이 배경에 묻히게 하지 말라고 했잖아, 검이 잘렸어, 다 다시 확인해", 2026-09-15)

- **도복이 비쳤다**: 도·창·봉·주먹 시트는 배경이 옅은 자주(171,138,167)라 도복의 어두운 베이지(185,165,130)가
  "배경 중앙값 ±40" 안에 들어 배경으로 지워졌다. 흰 배경 검사판에선 구멍이 안 보여 놓쳤다 → 배경 판정에
  **자줏빛 조건**(r·b가 g보다 큼)을 더하고(그 대신 자줏빛은 ±70까지 — 줄 곁 안티에일리어스도 함께 걷힘),
  검사판 배경을 **게임 바닥색**으로 바꿨다.
- **칼날이 잘렸다**: 찌르기 칼날이 원본 2px라 0.27배 축소에서 알파 문턱(128)에 사라졌다 → 축소 전 알파 1px
  팽창 + 문턱 96. 별 파편·초승달도 살아난다.
- **자주 세로선**: 부채 시트 4번째 칸 오른쪽 줄이 행마다 1~2px 어긋나 전체 높이 검출(70%)에 빠지고 그 칸이
  여백까지 170px로 잡혀 줄이 그림으로 남았다 → 칸 폭이 중앙값보다 15% 넘게 넓으면 앞 칸 폭 자리에서 줄을
  찾아 쪼갠다. 줄 띠 ±2px 확장, 여백의 가늘고 긴 조각(줄 잔재) 버림, 위 여분 계산에 발 간격 반영.
- 21판 재추출·spritetest(규격·접촉·줄 잔재)·fxtest·animtest·blackcheck·바닥색 검사판 8종·크로뮴 실기.

# v2.73 — 주인공 시트, 그림 기준으로 다시 자름 (사용자: "무기 형태가 잘리면 안 돼, 이미지 기준으로 잘라라, 주먹부터 다시 다 봐", 2026-09-15)

- 감사 결과 AI 시트는 창 끝·큰 원 검기·부채 호가 **칸 테두리를 넘어 여백까지** 그려져 있었다(창 r1c1 여백 1062px).
  칸 단위로 잘라 그 부분을 버리고 있었다 → **공용 추출기 `hero_sheet.py`**: 시트 전체 덩어리 라벨링, 테두리 줄을
  가로지르는 조각은 줄 픽셀을 이어 붙여 몸과 한 덩어리로(인물끼리는 절대 안 잇고, 조각은 접촉이 가장 긴 인물에),
  여백의 ㄱ자·눈금 표식은 버림, 크롭은 덩어리 합집합 bbox. 머리카락 무게중심으로 가로 정렬, 칸 바닥 = 땅.
- **캔버스 폭·높이 자동**(review/hero_specs.json → HFX.aw, 51을 넘으면 HFX.fh [높이, 위 여분]). drawHero가 fh·위
  여분을 받아 땅을 그대로 두고 위로 그린다(창·봉 회전 54, 검 베기·도 52 등). spritetest도 fh로 검사.
- 주먹·발차기·질주·검·부채·도·창·봉 21판 전부 재추출. 컷 교체: 봉 휘두르기 마지막 = 가로 찌르기(연타 잔상은
  옅어 끊겨 보임), 부채 찌르기 2컷 = 낮게 쓸기(머리 위 부채는 원본에서 테두리에 잘려 있음). 정권 두 판은 폭 공유.
- 검증 8종 검사판·blackcheck 21판·spritetest(규격·가장자리)·fxtest·animtest·크로뮴 실기(회전 원 머리 위 온전).

# v2.72.5 — 첫 시작은 맨손 (사용자: "첫 시작은 주먹 공격, 검 공격 이상한데", 2026-09-15)

- 시작 무기 목록에서 검을 뺐다(EQUIP.starter.weapon ['fist']) — 권갑 아이콘이 없는 동안은 **무기 없이 시작**해
  양주먹·발차기로 싸운다. 검·도 등은 사냥에서 떨어지면 빈 자리에 끼워져 그때부터 그 무기 동작.
- 검 찌르기 마지막 컷을 사선 호(1줄 5칸, 검이 안 보이고 호만 남아 어색)에서 **3줄 3칸 찌르기 뻗음**으로 교체.
- 검증 equiptest(무기 없음 시작)·fxtest·spritetest·savetest·크로뮴 새 게임(무기 null·무브셋 punch).

# v2.72.4 — 봉 공격 모션 (사용자 시트 sheets/hero_staff2.png, 2026-09-15) — 무기 5종 동작 완성

- WEAPONMOVES.staff = **휘두르기(staffswing, 0)·쓸기(staffsweep, 5)·회전(staffspin, 15)**. `hero_staff2.py`.
  컷 선택은 스크립트 머리말에(봉 둘 교차·세워 꽂음·낮게 가로 쥠·세워 쥠·가로 찌르기는 안 씀). HFX.aw 66.
- 이로써 검·부채·도·창·봉 5종이 제 동작을 갖는다. 권갑만 아이콘(eq_fist) 대기 — 맨손 ATKMOVES를 쓴다.
- 검증 review/hero_staff2.png·blackcheck·전 회귀(무기별 자동 검사)·크로뮴 실기.

# v2.72.3 — 창 공격 모션 (사용자 시트 sheets/hero_spear2.png, 2026-09-15)

- WEAPONMOVES.spear = **찌르기(spearthrust, 0)·쓸기(spearsweep, 5)·회전(spearspin, 15)**. `hero_spear2.py`.
  컷 선택은 스크립트 머리말에(창 둘 교차 컷·땅에 꽂음·가로 쥠·세워 쥠은 안 씀). 창이 길어 HFX.aw 66/66/66.
- 검증 review/hero_spear2.png·blackcheck·전 회귀(무기별 자동 검사)·크로뮴 실기.

# v2.72.2 — 도(刀) 공격 모션 (사용자 시트 sheets/hero_saber2.png, 2026-09-15)

- WEAPONMOVES.saber = **베기(saberslash, 0)·내려찍기(sabersmash, 5)·회전베기(saberspin, 15)**. `hero_saber2.py`.
  컷 선택은 스크립트 머리말에(땅에 꽂음·쥐고 섬·올려베기 호 컷은 안 씀). HFX.aw 62/62/64.
- 검증 review/hero_saber2.png·blackcheck·전 회귀(무기별 자동 검사)·크로뮴 실기.

# v2.72.1 — 부채 공격 모션 (사용자 시트 sheets/hero_fan2.png, 2026-09-15)

- WEAPONMOVES.fan = **휘두르기(fansweep, 0)·회전(fanspin, 5)·찌르기(fanstrike, 15)**. `hero_fan2.py`(hero_sword2 틀).
  컷 선택은 스크립트 머리말에(발차기+부채 별 컷·접은 부채 겨눔 컷은 안 씀). HFX.aw 62/64/62.
- spritetest·fxtest가 WEAPONMOVES를 돌며 무기별 무브셋을 자동 검사한다(새 무기는 데이터에 얹기만 하면 검사됨).
- 검증 review/hero_fan2.png·blackcheck·전 회귀·크로뮴 실기.

# v2.72 — 검 공격 모션: 검을 끼면 기본공격이 바뀐다 (사용자 시트 sheets/hero_sword2.png, 2026-09-15)

- **무기별 무브셋** WEAPONMOVES(00-data) — 무기 자리에 낀 종류(heroWeaponKind)로 atkPool이 무브셋을 고른다.
  검 = **찌르기(swordthrust, 성급 0)·베기(swordslash, 5)·회전베기(swordspin, 15)** 4컷씩. 다른 무기(권갑·도·창·
  봉·부채)는 아직 맨손 ATKMOVES — 시트가 오면 WEAPONMOVES에 얹는다. 대기·질주 컷엔 검이 없다(시트 없음).
- 추출 `hero_sword2.py`(hero_kick2 공용): 흰 검기 호가 테두리 줄을 가로질러 줄 검출 문턱 0.85→0.7. 컷 선택은
  스크립트 머리말에. HFX.aw 60/60/64, loadImg 3종.
- 지금은 시작 무기가 검이라(권갑 아이콘 대기) **새 게임이 검술로 시작**한다 — 권갑 아이콘이 오면 맨손으로 시작.
- 검증 review/hero_sword2.png·blackcheck·spritetest 3종·fxtest(검 무브셋·제 폭·검만 돌려 씀·벗으면 맨손)·크로뮴 실기.

# v2.71.2 — 주인공 주먹 공격 교체 (사용자 시트 sheets/hero_punch2.png, 2026-09-15)

- 양주먹 기본공격이 **사용자 시트 두 판 교대**가 됐다: 오른손 정권 punch = 1줄 [기수식→뻗음→별 임팩트→뻗음],
  왼손 기운 정권 punchb = 3줄 [기운 휘감기→뻗음→별 임팩트→뻗음]. P.atkAlt 교대는 그대로(옛 권기 판
  katka/katkb는 에셋·loadImg 보존, 렌더에서만 물러남). HFX.aw.punch 48. `hero_punch2.py`.
- 시트 5칸(주먹 클로즈업)·2줄 잔상 연타(회색 분신)·웅크려 치기·뛰어 치기는 안 썼다.
- **hero_kick2 추출기 일반화**: 이 시트는 배경이 옅고(중앙값 ±40으로 판정) 테두리가 회자주·칸 모서리에
  ㄱ자 표식이 있어 — 격자는 '배경보다 훨씬 어두운 얇은 줄 85%↑'로, 모서리 400px 미만 조각·40px 미만
  점은 버리고, 축소 뒤 떨어진 1~2px 점(머리카락 끝·발밑 그늘)도 지운다. 발차기·질주도 같은 코드로 다시
  뽑아 4~9픽셀 차이(떨어진 점 제거)뿐임을 확인.
- 검증 review/hero_punch2.png·blackcheck 6종·spritetest(정권 두 판)·fxtest(punch/punchb 교대)·animtest·크로뮴 실기.

# v2.71.1 — 주인공 질주 교체 (사용자 시트 sheets/hero_run2.png, 2026-09-15)

- 3줄×6칸 중 **1줄(곧게 선 질주 6컷)** 을 `hero_run2.py`로 뽑아 run.png 교체. ANIM.run [6,10].
  2줄(무릎 높이 걷기)·3줄(앞뒤 컷이 웅크림이라 사이클 불가)은 안 썼다. 발차기와 같은 배율(172→47px),
  몸통(35~65% 행) 무게중심 가로 정렬, 칸 바닥 기준. 옛 10컷은 raw/run_old/.
- 검증 review/hero_run2.png·blackcheck·spritetest·fxtest(6컷 전부 그려짐)·animtest.

# v2.71 — 주인공 발차기 교체 (사용자 시트 sheets/hero_kick2.png, 2026-09-15)

- 3줄×6칸 시트에서 `hero_kick2.py`로 추출: **옆차기(kickside)·돌려차기(kickround, 신규 need 10)·
  뛰어차기(kickhigh — 옛 높은차기 대체)** 각 4컷(임팩트 index2·마지막 컷 뻗음). 어두운 자주 테두리 줄로
  격자 검출, 배경은 r-g>50&b-g>50, 칸마다 덩어리 라벨링(초승달 기운은 떨어진 조각이라 살림), 공통 배율
  (서 있는 컷 172px→47px), **칸 바닥 기준**(도약 컷은 뜬 만큼 공중), 머리 무게중심 가로 정렬, 가장자리
  색 확장 뒤 LANCZOS·알파 128 문턱. 초승달의 분홍 물듦은 g 쪽으로 걷었다.
- 돌려차기 2칸(3칸과 같음)·뛰어차기 5칸(다리가 뒤로 뻗음)은 뺐다. 옛 kickside/kickhigh는 raw/kick_old/.
- ATKMOVES 4종(punch 0·kickside 5·kickround 10·kickhigh 15), HFX.aw.kickround 56, loadImg hero_kickround.
- 검증 review/hero_kick2.png·blackcheck·spritetest(돌려차기 추가)·fxtest(4종·제 폭·cycle)·animtest·크로뮴 실기.

# v2.70.6 — 지도 구역은 선택, 이동은 단계 줄에서 (사용자: "누르면 바로 가는 게 아니라 아래서 골라야지", 2026-09-15)

- 구역 노드를 누르면 금 고리로 **선택**만 되고 아래 단계 줄이 그 구역으로 바뀐다(zoneSel). 단계 버튼을
  눌러야 gotoZone. 단계 줄은 이제 테스트가 아니어도 항상 보인다 — **가 본 단계까지만**(S.reach[구역],
  enterStage가 갱신, 저장·복원, 옛 저장은 지나온 구역을 끝까지 가 본 것으로) 고를 수 있고 나머지는 잠김(.lock).
  TEST는 전부 열림. gotoZone도 reach를 넘는 단계는 거부.
- 검증 uitest(선택/이동 분리·reach)·전 회귀·크로뮴 실기.

# v2.70.5 — 단계 버튼 한 줄에 5개 (사용자, 2026-09-15)

- 사냥터 패널의 단계 이동 버튼을 5열 격자로 — 1~5 / 6~10 / 보스. #zstage .zst grid.

# v2.70.4 — 시험 패널이 다른 패널을 막던 것 · 지도 패널 높이 · 장비 탭 위 스킬창 · 무기 이름 (사용자, 2026-09-15)

- **시험 패널을 연 채 탭을 누르면 아무것도 안 먹혔다** — #tpanel이 DOM 맨 뒤라 다른 패널 위를 덮었다.
  closeSheets가 시험 패널도 닫고, 시험 버튼은 다른 시트를 닫고 연다.
- **사냥터 지도 패널 92%** — 지도 아래 단계 줄·보스까지 다 보이게 더 위에서부터 펼친다.
- **장비 탭에서 스킬창 쿨이 비쳤다** — hud()의 panelOpen 목록에 epanel이 빠져 있었다.
- 무기 종류: 철선 → **부채**, 철구 삭제(무기 5종 + 권갑 대기). 옛 저장의 철구는 로드 때 버려진다.
- 검증 equiptest·전 회귀·크로뮴 실기(시험→수련 탭, 장비 탭 스킬창 opacity 0, 지도 패널).

# v2.70.3 — 무기 자리 = 무림 무기 체계, 권은 한 종류 (사용자: "권은 한 종류지, 권 검 도 창 봉 체계를 적용", 2026-09-15)

- v2.70.1의 "권 여섯 종류"는 오해였다. 무기 종류 = **권갑(권)·검·도·창·봉·철구·철선** 7종. 맨주먹 계열은
  게임 관례대로 **권갑(拳套)** 한 종류 — 권 자체가 무기 분류다. 권갑 아이콘(eq_fist)이 없어 지금은
  6종만 보이고, 시작 무기는 검(아이콘이 오면 권갑이 먼저). 검류→철구 이월 코드는 걷어냈다.
- 검증 equiptest 27항목 + 전 회귀. 크로뮴 실기(무기 탭).

# v2.70.2 — 장비 줄 순서·시작 장비·아이콘 없는 종류 숨김 (사용자, 2026-09-15)

- **일반이 위** — 등급 줄을 일반→전설 순으로(낮은 게 위에서부터).
- **시작 장비** — 부팅 때 자리마다 아무것도 없으면 일반 하나를 주고 낀다(EQUIP.starter: 권갑→철구 순
  아이콘 있는 것, 무복, 옥패). 새 게임·옛 저장 공통, eqStarter(68-equip)·70-main.
- **아이콘 없는 종류는 화면·드랍에서 뺀다**(eqKinds — eq_<종류>/kinds[3] 에셋이 있는 것만). 권갑·권포·
  철권·조갑·완갑은 시트가 오면 저절로 나타난다. 옛 검류 저장 이월 대상은 EQUIP.legacyWeapon(철구).
  sim은 assets/ 목록으로 ASSET을 스텁한다(없으면 드랍이 0이 된다).
- 검증 equiptest 27항목 + 전 회귀 + sim 20분.

# v2.70.1 — 줄바꿈 · 무기 자리 = 권 (사용자: "가독성이 떨어져 줄바꿈이 필수 / 지금 무기는 맨손 혹은 권으로", 2026-09-15)

- **줄바꿈**: 무공 효과 문구(타일·상세·스킬 툴팁)는 artFxLines로 한 줄에 하나(공격력 → 피해 →
  쿨). 수련 수치는 `.trv` 블록으로 다음 줄. 장비 낀 카드·상세는 효과 한 줄에 하나(.eqlab 라벨
  열). 토스트는 `\n` 줄바꿈(white-space:pre-line) — "첫 격파 / 은자 +N", "쓰러졌다 / 운기조식".
- **무기 자리 → 권(맨손 계열)**: 주인공은 맨손 정권이라 검·도·창·봉·철선을 쥐는 게 어색했다.
  종류 6 = 권갑(치명)·권포(공속)·철권(치명 피해)·조갑(회복)·철구(체력)·완갑(은자). kinds[3]에
  아이콘 키, eqIcon()이 없는 아이콘은 수련 공격 주먹(train_atk)으로 대체 — 권 아이콘 시트가 오면
  eq_gauntlet 등으로 교체. 옛 저장의 검·도·창·봉·철선은 applySave가 권갑으로 합친다(개수 합·레벨
  최대·도감 합·장착 재키). 실제 무기(검·도…)는 나중에 별도 축으로.
- 검증 equiptest 25항목(이월 검사 추가) + 전 회귀. 크로뮴 실기(장비 탭·상세·무공 상세·토스트).

# v2.70 — 장비 표준형 (사용자: "기존 방치형 시스템과 다르게 생겼어", 2026-09-15)

도감 격자(v2.67)를 흔한 방치형 장비 화면으로 다시 짰다.
- **[무기][방어구][장신구] 탭** · 위에 낀 아이템 카드(레벨·효과·보유 효과 합) · **일괄 합성 (N)** ·
  **자동 장착** 버튼 · 아래 등급별 줄(전설→일반)에 **아이템 카드**(아이콘·Lv·×개수·착용·합성 가능
  배지, 안 얻은 건 회색 실루엣) · 카드를 누르면 **상세 칸**(장착 효과→다음, 보유 효과→다음,
  [장착][강화 비용][합성 3→1]).
- **아이템마다 레벨**(S.itemLv, 은자 강화, 등급별 상한 20/40/60/80/100, 레벨당 +3%). 장착 효과와
  보유 효과가 둘 다 레벨을 타서 **안 끼는 낮은 등급도 강화할 이유**가 있다(보유 효과 = 장착 효과
  ×0.15, 얻어 본 것 전부 영구). 자리 강화(v2.67 eqLv)는 폐지 — 옛 저장은 낀 아이템 레벨로 이월.
- **합성은 수동**(같은 것 3개 → 한 등급 위, 일괄 합성 버튼). 자동 장착은 장착 효과(레벨 반영)가
  가장 큰 것으로 — 그래서 많이 키운 영웅이 전설을 이길 수 있다. 첫 장비는 빈 자리에 바로 낀다.
- 등급명 범품/양품/진품/보물/신물 → **일반/고급/희귀/영웅/전설**(회/초록/파랑/보라/주황).
- 탭 알림점 = 합성할 것이 있거나 더 좋은 장비가 있을 때. sim spend는 일괄 합성·자동 장착 뒤 낀
  아이템부터 싼 강화 3회. 60분 sim: 동굴 3(v2.68 기준선 동굴 2와 같음), 잔고 5천.
- 검증 equiptest 24항목 + 전 회귀. 크로뮴 실기(무기 탭·상세).

# v2.69.10 — 패널 머리글 위로 아이콘이 겹치던 것 (2026-09-15)

- 수련 패널을 내리면 스텟 아이콘이 머리글 "수련" 위에 그려졌다(폰 스크린샷). 아이콘의
  drop-shadow filter가 쌓임 맥락을 만들어 z-index 없는 sticky 머리글 위로 올라온 것 → .zhd z-index 5.

# v2.69.9 — 수련 설명·수치 줄 나눔 (2026-09-15)

- 수련 타일: 설명("주먹이 매워진다")과 수치("+12% → +32% (10단계)")가 한 줄이라 읽기 어려웠다
  → 수치를 다음 줄(.trv, 파랑·굵게)로.

# v2.69.8 — 장비 패널 [장착][도감] 탭 (2026-09-15)

- "장비 아래 보는 게 불편" — 자리 카드 3장 아래로 도감 80칸이 이어져 스크롤이 길었다. 무공
  패널처럼 상단 고정 탭 [장착][도감(N/80)]으로 갈랐다. 시트 높이 92%.

# v2.69.7 — 초식 위력 표기 "정권 N배" → "공격력 N% (피해 ≈실수)" (2026-09-15)

- "기준이 정권인 게 이상해" — 초식 효과가 "정권 3배"로 적혀 있었다. 흔한 표기인 **공격력 N%**
  로 바꾸고, 지금 공격력으로 환산한 실제 피해(≈)를 곁들인다. 활인기공도 "최대 체력의 N% 회복
  (≈실수)". 스킬 툴팁·무공 타일·상세 칸이 같은 문구(artFxText)를 쓴다.

# v2.69.6 — 원경 땅 띠 자연스럽게 · 설산 대비 (2026-09-15)

- 덧댄 땅 띠가 줄마다 섞은 픽셀이라 가로 줄무늬로 보였다("부자연스럽다") → 땅 픽셀 풀을 2D로
  무작위 배치하고 3×3 흐림. 원본 마지막 줄과의 크로스페이드는 뺐다(나무 밑동이 섞여 어두운 선).
- 설산 원경은 시트가 흐려 나무가 잘 안 보였다("좀 더 보이게") → `--contrast=1.35`(평균 밝기
  기준 대비). 5구역 재추출.

# v2.69.5 — 설산 "여전히 똑같아": 덧대기가 절반만 들어가고 있었다 (2026-09-15)

- bg_extract의 `--pad`는 축소 전 원본에서 덧대는데, 설산·천산 시트는 2000px라 최종(1024)
  에선 절반이 됐다(120 → 61, 48 → 24). N을 최종 픽셀 기준으로 받아 원본 배율로 환산.
  설산 눈밭 띠 120px(에셋 422 높이), 천산 64px. 배포본을 다시 읽어 에셋 높이로 확인했다.

# v2.69.4 — 설산 원경 나무 줄 (2026-09-15)

- 설산 시트는 소나무 줄이 평평한 띠에 가려 밑동이 직선으로 끝난다(그림 자체). 눈밭 띠를
  120px로 넓게 덧대 나무 줄이 평평한 눈밭 위에 서고, 그 아래에서 바닥 텍스처로 녹게 했다.
  디졸브 계단 16→48(≈1px)로 넓은 띠의 줄무늬 제거.

# v2.69.3 — 설산 나무 잘림의 진짜 원인: 소품이 원경에 덮임 (2026-09-15)

- 원경 그림이 아니라 **소품 소나무**(sno_pine 102px)였다. 소품은 원경 뒤 층이라(v2.62 "원경이
  항상 오브젝트 위") 지평선 바로 아래 선 키 큰 나무의 머리가 원경에 덮여 잘려 보였다.
  propSprite: 꼭대기(y-gh)가 지평선(horizonY)에 닿으면 세우지 않는다. 죽림 대나무·천산 바위도
  같은 규칙.

# v2.69.2 — 원경 아래 잘림, 5구역 전부 땅 띠 덧댐 (2026-09-15)

- "설산도 나무가 잘려" — 설산 소나무·천산 산자락·동굴 바위도 그림 맨 아랫줄까지 닿아 있었다
  (bg_extract의 '평평한 띠 제거'가 그 아래 땅을 걷어내 버린 것도 한몫). 5구역 전부 `--pad`
  (죽림 24, 나머지 48)로 땅 띠를 덧대 디졸브가 그림 대신 띠를 녹인다. 실기 5구역 지평선 확인.

# v2.69.1 — 폐촌 원경 집 잘림 재수정 (2026-09-15)

- 폐촌 시트는 집 벽이 그림 맨 아랫줄까지 닿아 있어, 디졸브 띠를 14%로 줄여도 벽 아래가 녹았다
  ("여전히 집이 잘려"). bg_extract에 `--pad=N`: 맨 아래 6줄에서 땅 픽셀(줄 중앙값 근처)만 골라
  가로로 섞은 땅 띠 48줄을 덧댄다 — 디졸브가 집 대신 이 띠를 녹인다(벽 줄을 그대로 이으면
  물에 비친 듯 줄무늬가 됐다). 디졸브 계단 8→16(6px 띠 제거).

# v2.69 — 사냥터 여정 지도 일러스트 (사용자 그림, 2026-09-15)

- 사냥터 패널이 양피지 지도(assets/zone_map, 720² 디더 팔레트 392KB) 위에 노드를 얹는다.
  노드 자리는 ZONES[].map(360×360 좌표)에 손으로 맞춤 — 죽림 좌하 · 폐촌 우중 · 동굴 좌중 ·
  설산 우상 · 천산 좌상. 구역 6 성채 자리 [292,52]는 그림에 이미 있다. 점선 길은 그림의 길이
  대신한다(생략). 글자는 밝은 테(paint-order stroke)로 양피지 위에서 읽힌다. 그림이 없으면
  옛 지그재그 노드망으로 떨어진다.

# v2.68.1 — 시험 버튼 자리 · 도감 색 · 걷는데 발이 안 움직임 (2026-09-15)

- 시험 버튼을 화면 가운데 왼쪽으로(패널·스킬창과 겹침). 패널이 열리면 숨긴다(`:has`).
- 도감: 얻어 봤지만 지금 없는 칸은 작은 점만(전엔 품계색 테두리·바탕이라 "얻지 않은 것도
  색이 들어와 있네"). 색 칸 = 지금 가진 것.
- **걷는데 발이 안 움직임** — 맞는 동안(hitT) 이동은 되는데 동작은 피격 단일 컷이라 미끄러졌다.
  걷는 중엔 질주 컷을 우선(맞은 건 hitflash가 알린다). 경공 착지 경직 중 이동도 막았다(웅크린
  컷으로 미끄러짐).

# v2.68 — 난이도 재조정: 하루에 천산 초입 (사용자 확정, 2026-09-15)

"난이도가 너무 쉽다, 방치형은 길게 가야" — sim 4시간에 천산 끝(콘텐츠 소진)이었다.
탐침 sim(정권/적hp·적피해/내hp)으로 본 원인: 적 체력 성장(1.30/단계)이 주인공 승급 배율(1.31)과
같아 수련·무공·특성·장비가 겹치면 벽이 없고, 적 피해는 1.19라 회복(1.31/승급)에 묻혀 안 죽는다.
- 실험(각 240~480분): 처치 수련치 1.05·필요 수련치 1.36·적hp 1.34·조합(X1~4) — 미미(천산 3~5).
  적 피해 1.26~1.30(Y1~4) — 쓰러짐만 250~680회, 진행은 그대로(쓰러짐 벌칙이 약해 벽이 못 됨).
  적hp 1.38~1.46(Z1~7) — 효과적, 쓰러짐 10 안팎.
- **채택: DIFF.hpGrow 1.30 → 1.46.** 24h sim: 5분 죽림6 · 15분 폐촌2 · 60분 동굴2 · 4h 설산2 ·
  8h 설산6 · 16h 설산9 · **24h 천산 1단계**, 쓰러짐 12, 잔고 바닥(강화·수련이 계속 갈린다).
  1.48은 24h 설산9. REALM.seed(구역 진입 경지) [0,10,16,21,24]로 갱신.
- 보스: BOSS.hp 배수는 그대로(보스도 hpGrow를 타므로 같이 단단해진다). bosstest 폐촌 75초·
  동굴 124초·설산 69초·천산 26/41초(시드 기준 근사).

# v2.67 — 장비 도감형으로 (사용자 확정 "다른 방치형을 따라간다", 2026-09-15)

v2.66의 "자동 장착·아니면 판매"를 버리고 흔한 방치형의 **도감형**으로: 드랍은 전부 주머니
(S.inv[종류][품계])에 쌓이고, 같은 종류·품계 3개는 저절로 한 품계 위로 합쳐진다(연쇄).
자리엔 가진 것 중 최고 품계가 자동 장착(같은 품계면 유지). **보유 효과**: 얻어 본 칸마다
(품계 base × 0.12)%가 그 자리 주 효과에 영구 가산 — 16종×5품계 80칸 도감이 장기 목표.
종류마다 **부가 효과**(검 치명타·도 공격 속도·창 치명 피해·봉 회복·철구 체력·철선 은자 …
장착 효과의 40%)가 달라 도감 칸을 눌러 직접 골라 낄 수 있다. **강화는 자리(슬롯)에** 붙어
갈아껴도 남는다(S.eqLv, 상한 40). 판매 없음. 드랍 3%→5%(잡템이 재료).
- 저장: S.inv·S.codex(비트)·S.eqLv·S.equip{k,g}. v2.66 저장의 장비 lv는 자리 강화로 이월.
- 패널: 자리 카드 3장 + 도감 격자(종류 줄 × 품계 5칸: 얻어 봄 = 품계색 테두리, 가짐 = 개수,
  낌 = 착). heroSpd·heroAtkSpd·critMul에도 eBonus 합산(부가 효과용).
- 권갑→**철구**(아이콘이 쇠구슬이라 이름을 그림에 맞춤 — "권갑 이미지 없나").
- 검증 equiptest 25항목(합성 연쇄·자동 장착·보유·부가·자리 강화·이월·패널 80칸) + 전 회귀.

# v2.66 — 장비 성장축 · 저장 초기화 버그 (2026-09-15)

**장비** — 방치형답게 인벤토리 없이. 세 자리(무기·방어구·장신구), 처치 드랍(3%, 보스 100%,
품계는 구역별 가중 EQUIP.gradeW — 죽림은 범품·양품, 천산은 진품·보물·신물)이 지금 것보다
높은 품계면 **자동 장착(강화 레벨 전승)**, 아니면 **자동 판매**(처치 은자×3×(품계+1)).
강화는 은자 — 비용 = 현 단계 처치 은자 × 12 × 1.18^lv, 상한 품계별 20~40. 효과 전부 %:
무기 공격 / 방어구 체력 + 회복½ / 장신구 은자 획득 + 치명타½. 품계 base 8·14·22·32·45%,
강화 ×(1+0.04lv)(상한에서 범품 1.8배·신물 2.6배). 아이콘 eq_* 16종(v2.62 시트).
- 코드: EQUIP(00-data) · 68-equip.js(eqBonus·newItem·rollDrop·enhance·패널) · heroDmg 등에
  eBonus 합산 · 30-combat 처치 시 rollDrop · 저장 S.equip(검증 복원) · 탭 [장비](tab_equip) ·
  #epanel(자리 카드 3 + 최근 드랍 기록) · sim spend 강화(틱당 3회) · lint/build ORDER.
- 검증 equiptest(드랍·자동 장착·전승·판매·강화 효과·상한·저장·깨진 저장·패널) + 전 회귀.
- **sim**: 첫 안(상한 10~30·1.22^lv)은 60분에 상한이 차 잔고가 12만으로 쌓였다 → 상한 올리고
  곡선 완화. 60분: 장비 없음 설산2·잔고 4.9만 / 장비 설산3·잔고 11만(≈다음 강화 1회분).
  240분: 장비 천산7 g47 잔고 426만(다음 강화 ~940만) / 장비 없음 천산10 g50(끝) 잔고 137만 — 장비가 진행을 앞당기진 않는다(sim의 은자가 강화로도 갈린다).
  ※ 둘 다 4시간이면 천산 끝에 닿는다 — v2.0 기록(8시간 천산 중반)보다 빨라져 있다. 별도 점검 필요.
- **저장 초기화 버튼이 안 먹던 버그** — resetSave가 키를 지운 뒤 reload하면 pagehide·
  visibilitychange의 saveNow가 현재 상태를 도로 써 넣었다. 초기화 중엔 저장 금지(resetting),
  샌드박스가 reload를 막으면 같은 주소로 다시 연다.

# v2.65 — 표범·유령불 재작업 · 타이틀 화면 (사용자 시트, 2026-09-15)

- **표범(panther)** — 5행 시트(대기4·질주4·기어가기4·발톱 공격4·피격+죽음3). 칸 높이가
  균등하지 않아(3행 172px·4~5행 255px) 고정 격자가 칸을 걸쳤다 → foesheet가 **어두운 테두리
  줄을 검출해 격자를 잡는다**(_lines). 일어서서 할퀴는 컷이 몸 두 배 높이라 158×86, 몸 70×40.
  기어가기 4컷은 보관(미사용). 옛 판 raw/panther_old/.
- **유령불(wisp)** — 둘째 시트도 1행(불꽃 해골, 검사판 글자까지 베껴 옴)만 달랐다. 2~4행
  "두건 쓴 해골 귀신"으로 통일: 떠다님4(대기·이동)·공격4(손 들기·구체·파동·잔광)·피격·
  죽음2. 68×46, 몸 36×34. 옛 판(웃는 물방울) raw/wisp_old/.
- **타이틀 화면**(#title) — 사용자 배경 일러스트(assets/title_bg, 768² 디더 팔레트) + 주인공
  초상 + 「무협」 + 입산 버튼. 누르면 걷힌다. 게임은 뒤에서 이미 돈다(방치형). 복귀 카드는
  걷힌 뒤에(closeTitle) — 안 그러면 뒤에서 6초 만에 자동으로 닫혀 못 본다. savetest는
  closeTitle()을 호출.
- foesheet.pack: 캔버스 밖 픽셀을 버린다(음수 인덱스 랩으로 반대쪽에 조각이 붙던 것).
- 검증: spritetest·test·lint·animtest·hittest·bambootest·savetest·fxtest 통과, blackcheck 눈검사,
  크로뮴 실기(표범·유령불 양방향, 타이틀).

# v2.64 — 강도·주술사 재작업 (사용자 시트, 2026-09-15)

시트가 없어 재추출이 안 되던 옛 몹 둘을 사용자 제미나이 시트로 새로 뽑았다. 공용 추출기
`foesheet.py`(4×4 칸: 마젠타 배경·테두리 고리 제거·declutter·가장자리 3px 마젠타 성분 제거·
공통 배율·바닥 정렬·다리 무게중심 가로 중앙)를 만들고 `bandit.py`·`shaman.py`가 이를 쓴다.

- **강도(bandit)** — 붉은 두건·가죽 조끼·단도 산적. 대기4·걷기4·공격4(웅크림·찌르기·참격·
  갈무리)·피격1·죽음(피격→무릎). 60×49. 시체 컷은 돌바닥이 몸과 같은 색이라 못 떼어냄(미사용).
  옛 판(주인공과 같은 옷)은 raw/bandit_old/. **시트가 오른쪽을 봤는데 왼쪽으로 오판해
  반전했다가 실기에서 등을 돌렸다** — 실기 스크린샷으로 방향 확인(CLAUDE.md 규칙).
- **주술사(shaman)** — 시트 2장 합침: A(대기4·걷기4·주문4·피격1·죽음3) + B(지팡이 공격4).
  기본 공격=주문(cast, 기탄이 cast2에 그려짐·실제 탄은 atkAt 0.55에 발사), 스킬(큰 구체)=
  지팡이 들어 내리쳐 뻗기(staff, skillAt 0.55 찌르기 컷). 82×56, 몸 40×50. 구체 탄 그림
  shaman_m2는 옛것 유지. 옛 판 raw/shaman_old/.
- 유령불 시트는 **반려** — 행마다 디자인이 다르고(항아리 해골·상투 귀신 머리·불꽃 해골) 글자
  (氣·氣魂)가 들어 있어 못 쓴다(sheets/wisp.png 보관). 프롬프트를 죄어 다시 받는다.
- 검증: spritetest·test·lint·animtest·hittest·bambootest·shamantest·fxtest 통과, blackcheck
  6컷 눈검사, 크로뮴 실기(강도·주술사 양방향).

# v2.63.6 — 원경 아래 잘림·줄무늬 해소 · 시전 중 이동 금지 · 스킬 툴팁 · 초식명 외침 단장 (2026-09-15)

- **원경 집 잘림**("폐촌 원경 집이 다 짤려", "다른 원경도 아래쪽이") — 디졸브 띠가 64px
  고정이라 폰(VH≈280, 그림 84px)에선 그림의 3/4를 녹였다. 그림 높이의 14%(BACKDROP.fadeR,
  최소 8px)만 녹인다. horizonY도 같은 식.
- **원경 줄무늬** 3중 원인: ① bg_extract의 quantize(256, method=2)가 디더 없이 18~72색으로
  뭉개 안개 그라데이션이 띠가 됨 → 미디언컷 255색+플로이드 디더, 투명은 전용 인덱스 255
  (5구역 재추출, ✦ 워터마크 자동 탐지 패치 — 천산은 산봉우리를 오인해 패치 없이).
  ② 1024폭 시트를 화면 크기로 nearest 축소하며 모아레 → 표시 크기로 한 번 부드럽게 줄인
  캔버스 캐시(backdropScaled, jsdom은 원본 폴백). ③ 디졸브 띠가 ceil+1로 겹쳐 알파가 쌓임 →
  정수 경계로 겹침 없이. 빌드 11.85→12.5MB(디더 팔레트).
- **시전 중 이동 금지** — 초식 동작을 펼치며 적에게 미끄러져 갔다("움직이면서 스킬을 쓰네").
  40-step 이동 분기에 castT 조건. sim·bosstest 영향 없음.
- **스킬 툴팁**(#stip) — 스킬창 원판에 마우스 오버/누르는 동안 이름·한자·성·설명·효과.
- **초식명 외침** — 10.5px 가는 글자 → 피해 숫자와 같은 네온 결(FXD.artname: 굵은 13px,
  어두운 스트로크, 등장 팝 1.6, 문파색 잔광 가산).
- 사냥터 입장 카드 5종 프롬프트는 취소 — 사냥터 배너(zone k+1~3)가 이미 있어 그걸 쓴다.

# v2.63.5 — 무공 타일 메달·이름 간격 (2026-09-15)

- 무공 타일 56×50→56×60, 메달과 이름 사이 gap 2→5px("이미지랑 글씨가 위로 붙어 있네").

# v2.63.4 — 옥갑충 테두리 막대 세정 · 주인공 초상·휴식 카드·탭바 아이콘 (2026-09-15)

- **옥갑충 "오른쪽 검은 선"**(제보) — 시트 칸 테두리(어두운 자주 57,36,57, 일부 반투명)가
  캔버스 좌·우 끝 열에 세로 막대로 남아 있었다(atk0 28px·hit 34px·walk2 좌 32px 등, 10프레임
  156px). 몸과 떨어진 자주 세로줄만 지움(따뜻한 검정 외곽선 보존). spritetest에 **떠 있는
  세로줄** 검사 추가 — 기존 줄 잔재 검사는 85% 이상만 봐서 30~70% 막대를 놓쳤다.
- **주인공 초상**(hero_face 128²): 경지 사다리 머리글(.rhero — 얼굴+지금 경지+승급%).
  **휴식 카드**(rest_card 300×517): 오프라인 복귀 카드 위 그림(#oart, 기연 카드와 같은 틀).
- **탭바 아이콘 3종**(tab_arts/train/zone, 장비 tab_equip은 장비 탭 생기면) — 글자 위 26px,
  꺼진 탭은 흐리게. 알림점은 firstElementChild→.dot 조회로(아이콘이 앞에 와서).
- 사용자 시트: 주인공 초상+휴식 카드, 탭바 4종(jsonl 복원).

# v2.63 — 무공 아이콘·문파 엠블럼·보스 초상 (2026-09-15)

사용자 시트 3장. **UI류는 픽셀이 아니어도 된다**(사용자) — 원형 메달·회화풍 그대로 씀.

- **무공 아이콘 16종**(art_*, 96² 원형 알파): 스킬창 원판(.sk .gi, 한자 대신)과 무공 타일
  (.atile .gi)에. 시트가 18 중 16이라 백련심공·취권결은 한자 유지(있으면 자동 교체).
  안 배운 타일은 흑백. refreshArts가 .g 없을 때 터지던 것 가드.
- **문파 엠블럼 8종**(sch_*, 64²): 무공 타일 우상단 배지(.se). 화산은 설봉 위 매화 판.
- **보스 초상 5종**(boss_*, 160×240): 등장 문구 옆 컷인 — 문구와 같은 알파, 오른쪽에서
  슬라이드, 어두운 판+금테(FACECUT·BOSSFACE). 시트 6칸 중 석암거인 중복 1칸 제외.
- 추출: 장식 틀 시트는 마젠타 없이 셀 중심 원형 마스크(무공), 자홍 바탕 bbox→3×3(엠블럼).
- v2.62.x: 기연 카드 일러스트·수련 아이콘(재작업판, 테두리 링 배제)·장비 아이콘 16(대기),
  수련 예상값 배수 반영, 원경을 소품 위로.
- 검증 test·artstest·bosstest·lint 통과, 크로뮴 스킬창·무공탭·보스 컷인 눈검사.

---

# v2.61.7 — 원경 크기·지평선 디졸브 (2026-09-15)

"동굴 같은 건 원경 비율이 너무 크다 / 원경과 바닥 텍스처가 매끄럽지 않다" 피드백.

- **크기와 지평선 분리**: BACKDROP.hz(지평선 y 0.30)에 그림 바닥을 맞추고, 그림 높이는
  hDef 0.30·h[zone](동굴 0.21)로 따로. 그림 위 남는 하늘은 시트 윗줄 평균색(sky)으로
  채운다(마젠타 물든 가장자리 행 말고 완전 불투명 행에서 뽑을 것).
- **디졸브 페이드**: 평균색을 위에 칠하던 방식(평평한 띠 두 겹)을 버리고, 원경 그림의
  아래 fade(64px)를 알파 계단(16단, fadePow 1.6 ease)으로 빼서 **바닥 텍스처 위로
  녹인다**. drawImage 원본 자르기(9인자)로 띠마다 그린다.
- 시트 아래 **평평한 땅색 띠 자동 제거**(bg_extract): 옛 단색 페이드용이던 띠가
  디졸브에선 반투명 회색 띠로 남았다(설산). 중앙 열 std·바닥색 거리로 균일 행을 걷는다
  (죽림 44·폐촌 22·동굴 25·설산 71·천산 71px). 프롬프트의 "땅색 띠로 끝내라"는 이제 불필요.
- 검증 test·fxtest·lint 통과, 크로뮴 5구역 눈검사.

---

# v2.61.6 — 5구역 원경·바닥 완성 + 에셋 축소 (2026-09-15)

v2.61.2~5로 폐촌 원경·바닥, 동굴 원경·바닥을 넣은 데 이어 설산·천산 원경·바닥까지 —
**5구역 전부 원경(패럴럭스)+바닥 텍스처+소품+날씨** 세트가 됐다. 전부 사용자 시트.

- **빌드 16MB 한도 급제동**: 2000px 시트 그대로 넣자 15MB. bg_extract/ground_extract가
  폭 1024로 축소(LANCZOS, 게임은 ~290px 높이라 오버샘플)+팔레트 PNG-8로 저장 →
  원경·바닥 10장 합계 560KB, 빌드 11.2MB. 화질 차이 없음(눈검사).
- **지평선 회색 띠**(설산): 페이드가 땅색(#a4b3c0)으로 녹는데 눈 텍스처(#dbe6f0)가 훨씬
  밝았다 → GROUNDTEX.avg(추출 평균색)로 녹인다. GROUNDTEX.aZone으로 천산만 알파 0.7
  (밝은 얼룩이 강해 가독성).
- jsonl 복원 주의: 내가 Read한 스크린샷도 기록에 이미지로 남는다 — 마지막 N장이 아니라
  **크기·종횡비로 걸러** 복원해야 한다(천산 바닥이 390×844 스크린샷으로 덮였던 사고).
- docs/프롬프트-아이콘.md: 다음 그릴 것 — 장비 아이콘 16·기연 카드 4·수련 아이콘 8.
- 검증 test·fxtest·lint 통과, 크로뮴 5구역 눈검사.

---

# v2.61.1 — 죽림 바닥 텍스처 (2026-09-15)

"바닥은 굳이 타일이 아니어도, 원경과 이어지는 느낌이면" → **큰 무봉 텍스처 한 장을
카메라와 1:1로 2D 타일링**(32px 타일 아님). 사용자 제미나이 시트.

- 00-data GROUNDTEX(a 0.9·scale 1·keys), 50-render drawGround: 텍스처 있으면 땅색 위에
  알파로 타일링하고 이동감 격자는 생략(없는 구역은 종전 단색+격자). 70-main 조건 로드.
- ground_extract.py: 가장자리 48px를 반대편과 섞어 가로·세로 무봉화, ✦ 워터마크는
  --patch 왼쪽 띠 복사, 2×2 타일 검사판(review/ground_*.png).
- 위쪽은 원경 페이드(땅색 알파 계단)가 텍스처를 덮어 지평선으로 녹는다 → 원근 연결.
- 나머지 4구역 바닥·원경은 시트 대기. 검증 test·lint 통과, 크로뮴 죽림 눈검사.

---

# v2.61 — 네온 타격 연출 + 죽림 상단 원경 (2026-09-15)

"픽셀헌터 키우기 이펙트 확인해봐" → 도트 위에 **네온 발광 핵앤슬래시**. 두 갈래를 병렬로
(이펙트=서브에이전트 워크트리, 원경=본 트리) 진행해 병합했다.

**네온 타격 연출** (에이전트 구현, 순수 연출 — 밸런스 무변)
- **참격 호**(slash): 근접 명중마다 임팩트에 동심 호 3겹(여운·중간·흰 심), 방향·치명 배율.
- **피격 브라이튼**(hitflash): 맞은 적 스프라이트를 'lighter'로 n겹 덧그려 한 순간 흰
  실루엣 — source-atop·스크래치 캔버스 없이(사각 자국 사고 회피). `f.hitT`.
- **치명타**: 더 큰 금빛 파열 + 발밑 타원 링(critring) + shake 강화 + **미세 경직**
  (hitstop 0.04s, 70-main loop에서 step만 멈춤 → sim.js 무관, 배속과 충돌 없음).
- **탄 잔상**(drawTrail): 파공권·지풍·fly 탄(얼음·번개·해골·검기·음파)에 진행 반대로
  고스트 3개 'lighter' 감소 알파. 술병류(회전 탄) 제외. 왼쪽 거울 반전각 유지.
- **피해 숫자 팝**: 1.4→1 스케일 팝, 어두운 스트로크+밝은 채움, 치명 금색·더 큼·잔광.
- **시전 발광** castGlow 배율·네온 테. 수치 전부 FXD(slash/hitflash/critring/shake/
  hitstop/trail/dmgpop/castGlow). fxtest에 12검사 추가.

**죽림 상단 원경** (사용자 제미나이 시트 → bg_extract.py)
- 화면 위 34%에 안개 대숲 원경, 카메라 0.22배 패럴럭스, 아래 땅색으로 페이드, 지평선
  위 소품 컬링(v2.61a 인프라). 제미나이 ✦ 워터마크는 `--patch`로 왼쪽 띠 복사해 메움
  (평균색 칠하면 언덕·땅 경계에 연한 상자가 남았다). 좌우 이음새 24px 섞기.
- 나머지 4구역 원경 + 5구역 **바닥 무봉 텍스처**는 시트 대기(docs/프롬프트-배경.md).
- 검증: test·fxtest·hittest·animtest·spritetest·uitest·artstest·savetest·lint 통과,
  크로뮴 5구역 + 이펙트 확대컷 눈검사.

---

# v2.60 — 5구역 배경 소품 스프라이트 완성 (2026-09-15)

폐촌·동굴·설산·천산 소품 시트(사용자 제미나이)를 추출해 넣어 **5구역 전부** 배경
소품이 스프라이트로 채워졌다. 죽림(v2.59)과 같은 그림체로 통일.

- **추출 34종**(props_extract.py 일반화): 폐촌 10(항아리·깨진항아리·말뚝·울타리·
  그루터기×2·수레바퀴·기와·마른덤불·널빤지), 동굴 8(종유석×2·결정·바위·자갈·버섯·
  암첨·조각), 설산 8(침엽수×2·마른나무·눈바위·눈두둑·얼음결정·눈덤불·얼음그루터기),
  천산 8(돌탑·바람나무·바위·분재·비석·억새·자갈·깃발).
- 추출 로직: 어두운 보라 테두리로 격자 검출 → 칸 안 마젠타+보라그림자 제거 →
  **칸의 조각을 다 합쳐**(craggy 종유석·기와더미 분해 방지) tight bbox.
- **붙여넣기 이미지가 디스크에 저장 안 되는 문제** → 대화 기록(jsonl)의 base64
  인라인 이미지를 디코드해 복원(webp→png). CLAUDE.md 파이프라인에 절차 기록.
- 00-data PROPS에 4구역 sprite pick 추가(70-main 자동 로드). drawProps/propSprite
  그대로. 날씨 입자(AMB)는 계속 절차.
- 검증: test·uitest·spritetest·lint 통과, 크로뮴 5구역 눈검사(review/props_*.png).

---

# v2.59 — 죽림 배경 소품 스프라이트 + 절차 소품 폐기 (2026-09-15)

절차 드로잉은 한계 → 파이프라인 복귀. 사용자 제미나이 시트에서 죽림 소품 8종을
추출해 넣고, v2.57~58.2의 **절차 지형·소품은 전부 폐기**했다(사용자: "내가 그린
대나무 같은 건 쓰지 말고, 날씨(눈·재·낙엽)만 절차로").

- **죽림 8종**(bambooprops.py): 대숲 3대·2대·1대 / 죽순·이끼바위·고사리·쓰러진 대·풀.
  마젠타+보라 그림자 함께 배경 제거, 4px 인셋으로 테두리 링 스킵, tight bbox 추출.
- 00-data PROPS = 스프라이트만(kind:'sprite', pick:[키,높이,가중치] 가중 랜덤).
  village·cave·snow·heaven은 시트 오면 추가 — 지금은 빈 땅+날씨.
- 50-render: 옛 TERR 스캐터·픽셀 소품 함수 전부 삭제, drawProps는 propSprite로
  바닥 중앙·네이티브 종횡비 렌더. 70-main이 PROPS 스프라이트 자동 로드.
- **날씨 입자(AMB: 눈·재·낙엽·반딧불·구름)는 그대로** 절차로 유지.
- 검증: test·spritetest·lint 통과, 크로뮴 죽림 눈검사(대나무 스프라이트 확인).
- 시트 프롬프트: docs/프롬프트-지형.md (폐촌·동굴·설산·천산 남음).

---

# v2.58.2 — 소품 3톤 사실적 표현 (2026-09-15)

"좀 더 사실적으로" 요청. v2.58.1 픽셀 소품이 플랫 2톤·매끈한 실루엣이라 밋밋했다.
**3톤 음영(하이라이트·본색·그림자)으로 입체감**을 주고 실루엣을 사실적으로 다듬었다.

- pixRows에 hl(왼쪽 하이라이트) 추가 → 원통·볼륨감. pixLeaf(가운데 볼록 슬렌더
  잎) 헬퍼. PROPS.cols에 hi 톤 추가(00-data).
- **대나무**: 원통 마디(위로 가늘게)+마디 링(홈+밝은 띠)+잎가지 부채(대나무 잎).
- **침엽수**: 3층→4층, 가지 폭 지터로 들쭉날쭉, 눈이 윗 가지 왼쪽에 얹힘.
- **종유석**: 폭 지터로 울퉁불퉁, 3톤 바위, 청록 결정 2~3개 무리+발광.
- **항아리**: 둥근 프로파일+도자기 하이라이트+목 링+깨진 입 안 그림자+금.
- **말뚝**: 나뭇결 세로선+쪼개진 윗동. **그루터기**: 나이테 2겹+뿌리 두 갈래.
- **돌탑**: 돌마다 윗면 밝음+아래 그늘(둥근 돌). **관목**: 잎 클러스터+밑동.
- 검증: test·uitest·spritetest·lint 통과. 크로뮴 5구역 눈검사.

---

# v2.58.1 — 소품 그림체 픽셀화 (2026-09-15)

"그림이 별로다, 우리 그림체(픽셀 스프라이트)처럼 그려라" 피드백. v2.58 소품이
매끄러운 벡터(타원·곡선·안티앨리어싱)라 각진 픽셀 스프라이트와 이질감이 있었다.
**전부 픽셀 블록으로 다시 그렸다** — 곡선·스트로크 제거, 정수 정렬 fillRect만.

- **다크 아웃라인 + 플랫 셀 음영** — 스프라이트와 같은 그림체. pixRows(위→아래
  가로 막대 실루엣: line으로 1px 확장해 깔고 채워 1px 테두리만 남김) +
  오른쪽 그늘(shade) + pixShadow(픽셀 타원 그림자 2단).
- 대나무=마디 띠·계단 픽셀 잎, 침엽수=계단 삼각 3층+눈모자, 종유석=계단 삼각+
  왼쪽 하이라이트·결정, 항아리=폭 프로파일 행+깨진 입, 말뚝=정수 기울임 계단,
  그루터기=납작 원통+나이테, 돌탑=납작 돌 단, 관목=계단 픽셀 획.
- PROPS.cols에 line(아웃라인)·shade(음영) 톤 추가(00-data). 데이터·배치·레이어
  (drawProps 순서)는 v2.58 그대로.
- 검증: test·uitest·spritetest·lint 통과. 크로뮴 5구역 눈검사(픽셀 그림체 확인).

---

# v2.58 — 구역별 랜드마크 소품 (2026-09-15)

"좀 더 디테일한 요소들을 직접 넣어라, 타일은 꼭 안 써도 된다" 요청. v2.57 잔
스캐터 위에 **랜드마크급 절차 소품 층**을 얹었다 — 시트 없이, 구역마다 특징적인
큰 소품을 배경에 성기게 세운다. 잔 스캐터(풀·자갈) 뒤 층이라 깊이가 생긴다.

- **죽림** 대나무 줄기(휜 대·마디 띠·꼭대기 잎, 1~3대 무리) ·
  **폐촌** 깨진 항아리·기운 울타리 말뚝·그루터기(해시로 셋 중 택) ·
  **동굴** 종유석 원뿔(왼쪽 모서리 하이라이트, 가끔 청록 결정 가산 발광) ·
  **설산** 눈 쌓인 침엽수(3층 삼각+층마다 눈모자·줄기) ·
  **천산** 돌탑(납작 돌 3~4단) 또는 바람 맞은 관목.
- 데이터 PROPS(00-data: kind·dens·grid·h·cols). 넓은 격자(190~210px)에
  성기게(dens 0.4~0.5) 배치 → 화면당 2~3개 랜드마크.
- 무상태(thash 셀 해시)라 저장·sim 무관. 인물 뒤(먼저) 층이라 주인공이 앞을
  지난다(배경 플로라처럼). 50-render drawProps/drawProp/propStalk·Pine·Mite·
  Ruin·Cairn, render()가 drawGround→drawProps→drawScatter 순.
- 검증: test·uitest·spritetest·lint 통과. 크로뮴 5구역 눈검사(대나무·항아리·
  종유석·침엽수·돌탑 확인, 검은 막대·잘림·부유 조각 없음).

---

# v2.57 — 구역별 지형(절차 스캐터) (2026-09-15)

"사냥터 지형 타일" 요청. 시트 없이 갈 수 있는 길로 — AMB(구름 그림자·눈)와
같은 철학의 **무상태 절차 지형 스캐터**를 땅 층에 깔았다. 단색 배경 + 옅은
격자뿐이던 바닥에 구역별 특징물이 흩어져, 이동하면 뒤로 지나간다("보는 재미").

- **월드 좌표 고정**(격자 셀 104px, 셀 좌표 해시). AMB는 화면 고정이라
  흩날리지만, 이건 땅에 붙어 있어 카메라와 함께 스크롤한다 → 이동감이 산다.
- 구역별 그림: **죽림** 풀 무더기(3잎) · **폐촌** 낙엽 무더기(4점) ·
  **동굴** 자갈(그늘+윗면 하이라이트) · **설산** 눈 두둑(가끔 바위) ·
  **천산** 풀·자갈 섞임. 데이터 TERR(00-data: kind·cols·dens·sz).
- 무상태(thash 셀 해시)라 **저장·sim 무관**. 인물 뒤·구름 그림자/비네트 아래
  층. 50-render drawScatter/drawTerr/terrGrass/terrRock, render()가 drawGround
  직후 호출. jsdom 스텁 무해(순수 ctx 호출).
- 검증: test·uitest·spritetest·lint 통과. 크로뮴 5구역 눈검사(풀·낙엽·자갈·
  눈 두둑·천산 혼합 확인).

---

# v2.56 — 사냥터 = 여정 지도 (2026-09-15)

"사냥터 선택을 지도처럼 만들 수 있냐" 제보. 세로 목록을 여정 지도로 바꿨다.

- **5구역을 지그재그 여정 길**(SVG 점선 곡선)로 잇는다 — 죽림→폐촌→동굴→
  설산→천산. 각 구역은 **구역색(ZONES.ground) 원형 노드**. 진행 상태가
  한눈에: 클리어 ✓, 현재 구역 금테+맥동(▶ 수련 중), 잠긴 구역 🔒·흐린 길.
- 노드를 누르면 그 구역으로 이동(gotoZone). TEST는 아래 단계 이동 스트립
  (지금 구역의 1~10+보스)으로 단계까지 바로 점프.
- 60-ui buildZonePanel 재작성(SVG 인라인, viewBox 360×H). 데이터·이동 로직
  (gotoZone·S.unlocked·bossDone) 그대로. uitest를 지도 구조로 갱신.
- 검증: uitest(5구역·이동·단계)·test·savetest·screens 통과. 크로뮴 눈검사.

---

# v2.55.3 — 폴리시: 강도·주술사 공격 애니 타이밍 (2026-09-15)

알려진 자잘한 것 두 개 정리(애니 길이 vs 판정/시전 길이 불일치).

- **강도**: atk 3프레임이 7fps(0.43초)라 공격 시간(FOE.dur 0.55초)보다 짧아
  마지막 프레임이 0.12초 얼어붙었다. atk fps 7→5.4(3/5.4≈0.556초)로 꽉 채워
  멈춤 제거.
- **주술사**: skill 6프레임이 5.5fps(1.09초)라 시전 시간(skillDur 1.0초)보다
  길어 마지막 컷(m3 갈무리)이 안 나왔다. skill fps 5.5→6.2(6/6.2≈0.97초)로
  1.0초 안에 끝까지 보이게.
- 숫자는 00-data FOES. shamantest·bambootest·ronintest·cavetest·snowtest·
  animtest·test 통과.
- (남음: 마왕 swing3 오른쪽 돌기둥 칸경계 직선 잘림 — 시트 재추출+눈검사 필요.)

---

# v2.55.2 — 문파 무공도 접기 (심화 = 스킬 특성만) (2026-09-14)

"스킬을 은자로 배우고, 심화에 탭이 둘인데 다 필요하냐" 제보. 겹치는 건
[스킬 특성]과 [문파 무공도] — 둘 다 무공점을 쓰는 강화이고, 트리는 처음에
"노드는 방치형엔 번거롭다"고 접자던 그 거미줄이었다. 트리를 접었다.

- **심화창 = 스킬 특성 하나**. 상단 토글·문파 무공도 뷰 제거. 무공점은
  이제 전부 스킬 특성에 쓴다(무공 동선: 배우기→연마·돌파→심화 특성).
- **트리 환급**: 로드 시 S.tree를 안 되살린다 — 옛 저장의 트리 노드에 쓴
  무공점이 특성으로 풀린다. treeBonus는 빈 트리라 0(전투 수식 중립).
- 트리 엔진(66-tree·65b-treedata·트리 렌더)은 **잠자게 보존**(되돌릴 수
  있게) — 화면·저장·sim에서만 뺐다.
- 밸런스: 트리 패시브 %가 빠졌지만 무공점이 특성으로 몰려 상쇄 — sim 30분
  동굴 도달로 구역 진행 그대로. artstest·treetest·savetest·test·fatetest
  ·sim 통과. 크로뮴 눈검사(심화창 단일 뷰).

---

# v2.55.1 — 수호무사 초승달 검기 방향 교정 (2026-09-14)

"천산 검객 검기가 초승달 모양이 반대로 날아감" 제보. guard_shot의 볼록면이
왼쪽이라, fly 탄 회전 코드(오른쪽 기본 가정 — 해골·권기와 공용)를 태우면
오목한 쪽이 앞서 반대로 보였다. 에셋을 좌우로 뒤집어 볼록면을 오른쪽(진행
방향)으로 맞췄다(회전 코드는 그대로). heaventest·spritetest 통과.

---

# v2.55 — 스킬 심화 특성 (배운 무공에 특성 부여) (2026-09-14)

재구조 마지막 조각. 심화창이 두 갈래다: **[스킬 특성]**(배운 무공에 관통·
연타 대신 쿨감·위력·사거리·증폭 특성)과 **[문파 무공도]**(기존 노드망).
둘 다 경지 무공점을 쓴다. 사용자 확정: 탭=초식/심법·모델=혼합·자원=무공점.

- **데이터**(00-data TRAITS): 초식 5종에 쿨감·위력·사거리 특성, 심법 12종에
  증폭(심화 深化) 특성. eff: cdcut·power·reach·amp. 비용 2~4 무공점.
- **엔진**: traitDefs·hasTrait·traitMul·traitCdcut(00-data), traitBuy·
  skillPtsSpent에 특성 비용 합산(66-tree). 배운 무공(S.arts)에만 켠다.
- **전투 반영**(30-combat): 초식 쿨 ×(1−cdcut), 피해·회복 ×power, 사거리
  ×reach. 심법은 artMul에 amp 곱. 되돌리기 없음(연마·돌파와 같은 결).
- **UI**(67-treepanel): 심화창 상단 [스킬 특성][문파 무공도] 토글 + 남은
  무공점. 특성 뷰는 배운 무공 카드 + 특성 칩(이름·설명·비용, 익힌 건 ✓).
- **저장**(65-save): S.traits. **sim**(spend): 특성도 싼 것부터 산다 —
  30분 동굴·구역 진행 그대로(무공점 sink라 힘 폭주 없음).
- 검증: artstest(특성 구매·반영·뷰·저장)·treetest·test·savetest·fxtest·
  sim 통과. 크로뮴으로 특성 뷰·문파 무공도 전환 눈검사.

---

# v2.54.2 — 트리는 무공을 주지 않는다 ("배우기도 전에 쓰네") (2026-09-14)

새 구조에서 무공 습득은 '배우기' 버튼(은자) 몫인데, 심화 트리의 무공 마디
(eff.art 15개, 전부 습득 전용)를 켜면 여전히 S.arts를 세팅해 **안 배운 무공이
시전**됐다("배우기도 전에 쓰네").

- **트리는 무공을 습득시키지 않는다**: treeApplyArt 폐지, treeAlloc/treeDealloc/
  treeAvail이 무공 마디(treeArtNode)를 거부한다. 무공 마디는 **배운 무공을
  '익힘'으로 비추기만** 한다(treeGift = S.arts면 익힘 표시, 무공점 안 듦).
- 안 배운 마디는 흐리게 + "무공 탭에서 「…」을 먼저 배운다 — 배우면 열린다",
  배우면 초록 익힘으로 켜지고 뒤 마디(키스톤 등)가 열린다. 습득 버튼 없음.
- **옛 저장 정리**: 로드 시 treeReapply가 트리가 줬던 무공(S.treeArt)을 S.arts
  에서 걷어낸다 — 기연·구매로 배운 건 유지(fate·창 습득 보존 확인).
- treetest 새 규칙으로 갱신, artstest·test·savetest·fatetest·sim 통과.
  크로뮴으로 무공 마디 안내(안 배움/배움) 눈검사.

---

# v2.54.1 — 배우기 버튼 라벨 (2026-09-14)

"스킬 배우는 버튼이 없어, 강화도 해야지" 제보. 배우기 버튼(#abuy)이 가격+
엽전 아이콘만 있고 글자가 없어 그냥 가격표처럼 보였다 — 연마·돌파는 글자가
있는데 배우기만 빠져 "버튼이 없다"로 읽혔다.

- 배우기 버튼에 **"배우기"** 라벨을 붙였다(연마·돌파와 동일한 꼴). 이제
  배우기·연마·돌파가 모두 글자 붙은 버튼으로 보인다(강화 = 연마·돌파는
  익힌 무공에 이미 있다).
- artstest·test 통과. 크로뮴 눈검사(배우기·연마 버튼 확인).

---

# v2.54 — 무공 배우기 = 일반 스킬창, 트리 = 스킬 심화창 (2026-09-14)

"노드 시스템은 방치형엔 번거롭다. 노드 업그레이드는 스킬 심화창으로,
무공 배우기는 일반 스킬창에 탭으로." 피드백 — 무공 습득/성장 동선을 되돌리고
트리를 부가 시스템으로 강등했다. (1단계: 재구조. 특성 부여는 다음 단계)

- **무공 탭 = 일반 스킬창**(63-arts 부활): [초식][심법] 탭으로 나눠 배우기·
  연마·돌파를 그 자리에서 한다(은자+경지). v2.47에 트리로 덮였던 평면 타일표를
  다시 무공 탭에 붙였다 — 자동 전투 구경 중 한 손으로 훑기 좋게.
- **트리(문파 무공도) = 스킬 심화창**(#dpanel): 무공 탭 우상단 '스킬 심화'
  버튼으로 연다. 노드 업그레이드(경지 무공점)는 그대로 여기 산다. openArts→
  openDeepen/closeDeepen/deepenHud로 이관(67-treepanel), #abody→#dbody.
- 알림점: 무공 탭 점 = 배울/올릴 무공 있음 OR 심화에 쓸 무공점 있음. 심화
  버튼에도 무공점 알림점.
- 검증: test·artstest(무공 탭=배우기창, 심화 버튼=트리로 갱신)·treetest·
  savetest·fatetest·sim(진행 그대로) 통과. 헤드리스 크로뮴 눈검사.

**다음 단계(2)** — 심화창에서 **스킬에 특성 부여**: 초식/심법마다 관통·연타·
광역·쿨감 같은 특성을 무공점(경지)으로 켠다. 트리 핵심 노드를 특성으로 재편
(혼합) + sim 밸런스. 사용자 확정: 탭=초식/심법, 특성 모델=혼합, 자원=경지 무공점.

---

# v2.53 — 경공 컷 머리 크기 교정 (2026-09-14)

"경공 쓸 때 머리가 커진다" 제보. 원인은 sheets/hero_dash.png의 날기·착지
컷이 대기/질주 컷보다 **확대(줌인)로 그려져** 있어(머리 폭 idle 26px 대비
날기 38·착지 33px) 경공 순간 머리가 1.4~1.5배로 튀어 보였다.

- 렌더 규격만 줄여 대기 자세와 머리·몸 크기를 맞췄다: 날기 43×45→31×32,
  착지 37×54→30×44, 공중 lift 20→15(작아진 컷에 맞춤). 숫자는 00-data.js의
  DASH. 스프라이트 재추출이 아니라 그리는 배율만 바꾼 것(나머지 프레임과
  같은 인물 크기로 통일).
- 검증: 헤드리스 크로뮴으로 대기·날기·착지 인게임 컷 눈검사(머리 크기 일치
  확인), fxtest(날기·착지 컷 DASH 규격대로 렌더)·test 통과.

---

# v2.52 — 무공 트리 단장 · 익히기 완화 · 기연 전수 표시 (2026-09-13)

"트리창도 예쁘게, 안 배웠는데 쓰고 있다, 배우는 시간 길다, 주요노드 적다"
피드백. 세 갈래로 손봤다.

- **트리 단장 (예쁘게·이정표)**: 익힌 길은 두 겹(넓은 은은한 빛 + 가는 밝은
  선)으로 흐르고, 익힌 무공·비전(major·keystone) 마디는 **후광 + 유리 광택**
  으로 지도의 이정표처럼 빛난다(mixCol 헬퍼로 밝은 문파색). 랜드마크 반지름을
  키우고(비전 23·무공 20·입문 18) 이름표를 밝게 — "주요 노드가 안 보인다"의
  해법은 개수보다 **눈에 띄게** 하는 것. 프런티어 맥동 고리(v2.50)는 유지.
- **안 배웠는데 쓰는 불일치 해소**: 기연(은거기인)이 무공을 직접 전수하면
  S.arts엔 들어오지만 트리(S.tree)엔 없어 "트리엔 안 익힌 마디인데 실전에선
  씀"이 됐다. treeGift로 그 마디를 **'기연 전수'**로 쳐서 트리에도 익힘으로
  보이게 한다 — 금 점선 고리 + 정보칸 "기연 전수 · 무공점 없이 익혀 있다".
  무공점은 안 들고(skillPtsSpent는 S.tree만 셈) 되돌릴 수 없다.
- **익히기 완화**: 성급당 무공점 1→2(SKILLTREE.ptsPerStar). 같은 경지에서
  두 배로 익힌다 — sim 확인(30분 동굴 도달·무공 6종, 구역 진행 속도 그대로).
- 검증: treetest·artstest(쿨다운 원형 스윕으로 갱신)·spritetest·fxtest·test
  ·sim. 헤드리스 크로뮴으로 트리 화면 눈검사.

**주요 노드 개수 자체를 늘리는 것**은 밸런스·작명·효과 설계가 필요한 콘텐츠
작업이라 별도로 상의(docs/설계-스킬트리.md, 액티브 3종은 내공 도입 때).

---

# v2.51 — 스킬창(초식 슬롯) 단장 (2026-09-13)

"스킬창이 볼품없다, 예쁘게" 피드백. 우하단 초식 슬롯(#sbar)을 다듬었다.

- **쿨다운 = 시계 방향 원형 스윕**: 위→아래 사각 덮개 대신 conic-gradient
  부채꼴이 남은 시간만큼 줄어든다(60-ui가 각도 갱신). MOBA식.
- **겹침 제거**: 쿨 중엔 한자를 숨기고 굵은 숫자만(.cooling), 준비되면
  한자만 밝게 — 예전엔 흐린 한자 위에 숫자가 겹쳐 지저분했다.
- **입체 원판**: radial-gradient 바탕 + 안쪽 하이라이트 + 그림자. 준비된
  초식은 문파색 테두리 발광, 수동+준비는 맥동(reduced-motion 존중).
- **자동/수동 토글**: 둥근 알약 + 상태 점(dot). 수동은 호박색으로 강조.
- 슬롯 34→40px. 로직/저장 그대로(외형만). test·fxtest(경공 플레이크 제외) 통과.

---

# v2.50 — 무공 트리 배우기 편하게 (경로 일괄 습득·프런티어 강조) (2026-09-12)

"스킬창 제대로 만들고, 트리라서 배우기 힘들다" 피드백. 트리는 마디를 하나씩
선택→익히기 해야 하고 앞 마디(need는 all-of)를 다 갖춰야 해서, 깊은 무공을
배우려면 조상 마디를 일일이 켜야 했다.

- **경로 일괄 습득**: 노드를 고르면 "여기까지 익히기 · N마디 M점" — 필요한
  앞 마디(안 익힌 조상 전부)를 한 번에 켠다. need가 all-of라 선택지가 없어
  자동으로 다 켜도 안전(treeClosure/treeAllocPath). 무공점 모자라면 필요/보유
  점수를 알려주고 비활성, 다른 문파 교차 조건이 걸리면 "잠김"+조건 안내.
- **프런티어 강조**: 지금 익힐 수 있는 마디에 맥동 고리(.tpulse). "뭘 배울 수
  있는지" 한눈에 — 트리에서 가장 답답하던 부분.
- **선택 경로 미리보기**: 안 익힌 노드를 고르면 여기까지 켜질 마디·간선이
  트리에 밝게(pending) 표시된다.
- 단일 노드는 그대로 "익히기". 되돌리기 유지. 검증: 헤드리스 크로뮴에서
  b6 선택→"6마디 6점" 한 번에 b0~b6 습득 확인. 전 스위트 통과.

---

# v2.49.3 — 달리기 모션 검은 줄 제거 (2026-09-12)

"달리는 모션에서 검은줄" 제보. run.png(v2.44 추출)에 시트 셀 **테두리 선**이
프레임 머리 위·발 아래로 구워져 있었다. 머리·발이 셀 경계에 닿아 blob에 딸려
온 것 — declutter(1px 침식)로는 몸에 직접 붙은 가로선이 dilate로 되살아나 못 뗐다.
- **상단 캡**: 최상단 solid 행부터 선단의 역-테이퍼(아래 행보다 넓은 가로선,
  ≥12px)만 걷어냈다 — 몸통 최대폭(어깨)은 머리 아래라 정상 테이퍼여서 안 건드림.
  프레임6·7·8·9에서 제거.
- **하단 테두리**: 발(좁음) 아래 y≥45의 ≥30px 넓은 평평한 줄(셀 바닥선)을 제거.
  프레임0·1·3·4·5·6·9. 발끝은 y47 근처로 정렬돼 전 프레임 일관.
- blackcheck 눈검사로 머리·발 온전·검은 줄 0 확인. spritetest·test·fxtest 통과.
- (규칙 0의 blackcheck 눈검사가 이 결함을 잡았다 — 기하 검사는 스트립 전체폭
  기준이라 프레임 하나의 테두리선을 못 걸렀다.)

---

# v2.49.2 — 무공(스킬트리) 패널 클릭 먹통 수정 (2026-09-12)

"무공 화면 이상함, 클릭도 안 됨" 제보. artsHud()가 매 프레임(60-ui) 호출되는데
패널이 열려 있으면 renderTreePanel()을 **매 프레임 통째로** 다시 그렸다 —
SVG 54개·정보칸(익히기 버튼 포함)이 프레임마다 새로 생겨:
- 탭 도중 버튼이 detach돼 클릭이 안 먹었고(플레이라이트 재현: element detached 60회)
- 초당 60회 전체 재구성이라 화면이 깜빡이고 버벅였다.
수정: artsHud는 알림점만 갱신하고, **무공점이 실제로 바뀔 때(경지 상승)만**
다시 그린다. 사용자 조작(노드·탭·습득)은 원래 그 자리서 renderTreePanel을
직접 부르므로 반응은 그대로. renderTreePanel 끝에서 추적값 동기화.
검증: 헤드리스 크로뮴에서 익히기 클릭·노드 선택·탭 전환 정상, tree 갱신 확인.
전 스위트 통과.

---

# v2.49.1 — 발차기 검은 막대 제거 · 검은조각 눈검사 상시화 (2026-09-12)

"캐릭터에 검은선 보임" 제보. 옆차기 프레임 위 **머리 위 검은 가로 막대** —
옆칸(윗줄) 스프라이트의 발 조각이 얇은 다리로 머리카락에 붙어 딸려 온 것.

- **declutter** 추출 단계 추가: 1px 침식→최대 덩어리만→dilate로 얇게 붙은
  조각을 끊는다. 두 발차기 재추출, 머리 위 검은 막대 사라짐.
- **높은차기 캔버스 넘침 수정**: 든 다리가 높아 위 가장자리에 닿던 것(잘림
  위험) — 가장 큰 프레임 기준으로 배율을 줄여 위 여백 3px 확보. 폭 62→60.
- **spritetest에 발차기 2종 명시 검사 추가**: kickside·kickhigh가 ANIM에
  없어 여태 규격·가장자리·줄잔재 검사 밖이었다(사각지대). 이제 검사한다.
- **검은조각 눈검사 상시화**(사용자 확정 "매번 말해야 하니"): 이 결함은
  실루엣이 몸과 가늘게 이어져 기하 검사로 못 잡는다(규칙 0 그대로 "그림이
  이상한 것"). `blackcheck.py`로 흰 배경 확대판을 만들어 **비전으로 항상**
  확인하도록 CLAUDE.md 규칙 0에 못박음. 브리틀한 기하 탐지는 시도했다 폐기
  (katka 파란 권기에 오검출, 정작 막대는 놓침).
- spritetest·test·fxtest 통과. 빌드·아티팩트 갱신.

---

# v2.49 — 각도별 발차기 2종(옆차기·높은차기) (2026-09-11)

사용자 시트(c0f865d3, "발차기도 각도별로 있어")에서 쓸 만한 발차기를 추출해
기본공격 무브에 얹었다. 성급이 오를수록 발차기 각도가 는다.

- **옆차기(kickside, 성급 5)** — 수평으로 내지르는 찌르기 발차기(6컷 원본 →
  4컷 [0,2,4,5], CW 54). v2.48의 단일 `kick`을 대체한다.
- **높은차기(kickhigh, 성급 15)** — 머리 높이로 차올리는 발차기(5컷 원본 →
  4컷 [0,1,3,4], CW 62). ATKMOVES = [양주먹, 옆차기, 높은차기] 3종, cycle.
- 추출: 시트가 옆모습 좌→우 시퀀스라 다리가 옆칸으로 뻗어 붙는다 — 행별
  덩어리 라벨링으로 6·5컷을 분리하고, 높은차기 3·4컷이 붙은 곳은 열 밀도
  골(x466)에서 갈랐다. 배경 판정을 통일(초록이 적·청 둘보다 낮음 = 마젠타/
  자주 테두리)해 어두운 마젠타까지 걷고, 몸의 따뜻한 검은 외곽선은 지켰다.
  축소는 엣지 확장(투명부를 최근접 불투명색으로 채움) 후 리샘플 — 링잉·
  마젠타 번짐 차단. torso 중앙·발 바닥 정렬(51 캔버스, 좌우 대칭).
- HITFRAME=2 유지 위해 전 발차기 4컷으로 리샘플(마지막 컷=완전 뻗음, 유지).
- **옛 kick.png 제거**. flykick·firekick·cresckick·burstkick 에셋은 초식 후보로 보관.
- fxtest 공격 검사 갱신(무브셋 3종·옆차기/높은차기 제 폭 렌더·3종 cycle).
  spritetest(가장자리·잘림)·test·fxtest 통과.

---

# v2.48 — 기본공격 정리(권기 정권+발차기) · 걷기 글리치 수정 (2026-09-11)

사용자 정리: 양주먹은 예전 권기 정권(양손 파란빛)으로 고정, 발차기는 하나만,
화염 발차기류는 뺀다. 초승달·도약 발차기는 추후 스킬(초식) 후보로 보관.

- **양주먹 = 권기 정권**(katka/katkb, 양손 파란빛) 복귀·고정. drawHero atk가
  punch 무브를 katka/katkb 교대로 그린다(P.atkAlt). v2.45~47.1의 punch 스프라이트
  대신 예전 권기 판 사용.
- **발차기 1종**(kick, 성급 5부터 섞임). ATKMOVES = [양주먹, 발차기] 2종.
- **화염 발차기류(flykick·firekick) 제외**. cresckick·burstkick·flykick·firekick
  에셋·loadImg는 남겨 둠(초식 스킬 후보).
- **걷기 글리치 수정**: HERO.hold(7) 데드존 — 붙은 뒤 stopD+hold 안에선 걷지
  않는다. 쿨다운 중 경계에서 td가 stopD를 오르내리며 idle↔run이 깜빡이던
  "중간중간 걷는 현상" 제거. 검증: 정지 적 옆 180프레임 run 0회.
- fxtest 공격 검사 갱신(양주먹=권기 정권·발차기·무브셋 2종). 전 스위트 통과.

# v2.47.1 — 초승달 참격·도약 옆차기 (성장형 공격 5·6티어) (2026-09-11)

사용자 시트(초승달 참격 발차기·도약 옆차기)로 성장형 공격 상위 두 티어 추가.
- 초승달 참격 발차기(cresckick, 성급 17 초절정) · 도약 옆차기(burstkick, 성급 21 화경).
  ATKMOVES에 need와 함께 얹어 자동 확장(무브셋 6종). 별빛 작렬 fx는 엔진 타격
  이펙트가 대신하므로 깨끗한 발차기 프레임만 사용. 임팩트 index2, 폭 46, 발 기준 정렬.
- fxtest 두 동작 그리기·무브셋 6종 검사. 원본 sheets/hero_kick2.png. 전 스위트 통과.

# v0 — 기준선 (2026-09-02)

여기서부터 쌓아 올린다. 되돌릴 일이 생기면 이 지점으로 온다.

## 담긴 것

### 주인공
- 신규 캐릭터, 5동작: 대기 · 이동 · 정권 4프레임 · 피격 · 운기조식
- 규격 **35×51** (몸높이 48px) — 전 동작 공통
- 무기 없음. 맨손 정권 찌르기만
- 주먹 이펙트: `FIST` 좌표에 작은 원. 주인공과 같은 변환 안에서 그려 좌우 자동
  ```
  f0 (9,-22)  f1 (13,-28)  f2 (14,-30)  f3 (13,-30)
  ```

### 적
- **대나무숲 강도** 1종, 12프레임 → 5동작
  ```
  idle / walk(walk,run) / atk(atk0,atk1,atk2) / hit / death(death,death2)
  ```
- 규격 **56×51**
- 공격 중엔 피격으로 끊기지 않음 (양쪽 다). 넉백 0.09초 · 속도 90

### 구역 — 5개 × 10단계
| 구역 | 배율 | 바닥 |
|---|---|---|
| 죽림 | ×1.00 | `#6a7a52` |
| 폐촌 | ×1.90 | `#6b6350` |
| 동굴 | ×3.40 | `#5c5f5c` |
| 설산 | ×5.60 | `#b9c9d2` |
| 천산 | ×9.00 | `#7f9a86` |

- 진입 연출: 배경 3장(320×96)이 위에서 아래로 슬라이드. 2.2초, 간격 화면의 5.5%
- 죽림·천산은 새 배경, 나머지 셋은 이전 배경
- **테스트 모드** (`TEST = true`): 50단계 어디든 이동

### 소리
합성음 4종 — 슉(슬라이드) · 타격 · 처치 · 쓰러짐

### 화면
모바일·패드·PC 대응. 가로로 넓으면 세로 비율 0.52로 잘라 가운데.
픽셀 배율 최소 2배 보장. 전 기기 4Mpx 이하

## 밸런스
```
 1분  죽림 2단계    39처치  쓰러짐 0
 5분  죽림 7단계   258처치  쓰러짐 2
10분  죽림 10단계  552처치  쓰러짐 6
15분  폐촌 7단계   839처치  쓰러짐 8
30분  동굴 10단계 1722처치  쓰러짐16
```

## 크기
`wuxia.html` **344KB** · 에셋 32개

## 검증
```
python build.py      빌드
node sim.js          30분 진행
node test.js         브라우저 실행
node animtest.js     동작 끊김
node screens.js      기기별 레이아웃
node uitest.js       사냥터 패널
```

---

# v1 — 대나무 개구리 (2026-09-02)

## 더한 것
- **대나무 개구리** 9프레임 → 5동작
  ```
  idle(idle0~3) / walk(idle0,hop,idle2,hop) / atk(atk0~3,idle0) / hit / death
  ```
- 규격 **172×44** — 혀까지 담느라 넓다. 몸은 38px (`sw`)
- 혀로 때린다. 탄을 쏘지 않고 **그림이 닿는 자리에서 바로 판정**한다
  - 그림이 닿는 거리 84px · 공격 시작 거리 76px (항상 그림 안에서 맞는다)
- 능력치 `hp 0.70 · dmg 1.10 · spd 0.80` — 무르지만 멀리서 때린다

## 렌더러 변경
몸보다 캔버스가 넓은 적을 위해 두 값을 새로 본다.
- `sw` — 그림자 크기와 공격 기울임에 쓰는 실제 몸 폭
- `bh` — 체력바를 띄우는 높이

없으면 예전처럼 `w`·`h`를 쓴다. 기존 적은 영향 없다.

## 대기 프레임 보정
원본 4장의 높이가 168~188px로 제각각이라 그대로 쓰면 숨쉬기가 아니라
부풀었다 줄었다 한다. 편차를 40%로 눌러 ±1px만 남겼다.

## 밸런스
```
 1분  죽림 3단계    33처치
 5분  죽림 7단계   205처치
10분  죽림10단계   414처치
```
개구리가 들어오며 처치 속도가 약 7% 느려졌다. 성장 축이 들어올 때 다시 잡는다.

## 크기
`wuxia.html` **812KB** · 에셋 89개

---

# v1.1 — 잘린 곳 정리 (2026-09-02)

## 개구리 정수리
뛸 때 얼굴 윗부분이 납작하게 잘렸다. 배경을 뺄 때 '어둡고 채도 낮은 픽셀'을
전부 배경으로 봤는데, 개구리 머리 위의 두꺼운 검은 외곽선이 거기 걸렸다.
알파를 만든 뒤 검은 픽셀 쪽으로 2px 되살리고, 칸을 26px 넓게 보되
속 범위에 걸친 덩어리만 남기도록 고쳤다. 규격 172x44 → **164x43**.

## 샤먼 지팡이
공격·마법 동작에서 지팡이 양 끝이 잘려 있었다. 처음 뽑을 때 인물 덩어리로
칸을 재고 여백 8px만 줘서 지팡이가 그 밖에 있었다. 원본 시트가 없으니
`shamanstaff.py`로 잘린 단면의 기울기를 재고 같은 단면을 이어 붙인 뒤
끝을 둥글렸다. 색은 단면에서 가져와 나뭇결이 이어진다.
규격 68x56 → **92x70** (몸은 그대로, 여백만 넓힘 · `sw`/`bh`)
원본은 `raw/shaman/`에 남겨뒀다. 다시 뽑으려면 거기서 시작한다.
`m2`는 인물이 아니라 구체 그림이라 여백을 붙이지 않는다 (탄으로 통째로 늘려 쓴다).

## 검사 도구
`node spritetest.js` — 선언 규격과 실제 PNG가 같은지, 그림이 캔버스
가장자리에 닿는지(=거기서 잘린 것) 전 스프라이트를 한 번에 본다.
이제 '잘려 보인다'는 눈으로 찾지 않는다.

## 크기
`wuxia.html` **813KB** · 에셋 89개

---

# v1.2 — 샤먼 동작 정리 (2026-09-02)

두 동작이 같은 프레임을 섞어 써서 구분이 안 됐다. 갈랐다.

## 기본 공격
`idle → m0 → m0 → m0 → idle` — 대기 자세에서 손에 구체를 모아 쏘고 돌아온다.
발사는 세 번째 프레임(m0)에서.

## 스킬 시전
`m0 → m0 → m1 → m1 → m1 → m3` — 모으다가 자세를 낮추고(m1) 터뜨린다.
여기에 **구체 그림(m2)을 몸 앞에 덧그린다.** 스프라이트에 굽지 않고 렌더에서
얹는 방식이라, 캔버스를 안 키우고도 몸보다 큰 이펙트를 낼 수 있다.

```
castfx:{ img:'shaman_m2', from:0.30, out:0.58, gone:0.82, x:12, y:30, w:72, h:60 }
```
- `from`→`out` 동안 35%에서 100%로 자란다
- `out`(=skillAt 0.55 직후)에서 탄이 떠나고, `gone`까지 흐려진다 —
  몸에서 자란 기운이 그대로 떨어져 나가는 것처럼 읽힌다
- 몸과 같은 변환 안에서 그리므로 좌우 반전이 자동이다

## 검사
`node casttest.js` — 시전 중 몇 프레임에 이펙트가 실제로 그려졌는지 센다.
그리기 호출을 가로채 세므로 "코드는 있는데 안 그려진다"를 잡는다.
`python castshot.py` — 두 동작을 게임과 같은 식으로 계산해 그림으로 뽑는다.

## 크기
`wuxia.html` **814KB** · 에셋 89개

---

# v1.3 — 기운 덩어리 앞머리 (2026-09-02)

스킬 이펙트(m2)의 앞쪽이 세로로 잘려 있었다. 지팡이와 같은 사고 —
칸을 인물 기준으로 재서 앞으로 터져 나가는 부분이 밖에 있었다.

**잘렸는지 판별하는 법**: 마지막 열까지 불투명 픽셀이 49·49·50·51·49·47 로
꽉 찬 채 끝난다. 자연스러운 형태라면 끝으로 갈수록 줄어든다. 직선이면 절단이다.

`shamanorb.py` — 절단면 앞쪽 열들을 원 프로필 `sqrt(1-(k/N)^2)` 로 눌러
이어 붙여 둥근 앞머리를 만들었다. 안쪽 열을 되짚어 쓰므로 결이 이어지고,
대칭이 된 무늬가 오히려 문양처럼 읽힌다.
규격 68x56 → **75x60** (내용 52x52 → 67x52)

배치값도 같은 배율을 유지하도록 다시 잡았다.
```
castfx: x 12→16 · w 72→79 · h 60→64   (원본 부분의 화면 크기는 그대로)
```

## 크기
`wuxia.html` **816KB** · 에셋 89개

---

# v1.4 — 샤먼 마법 시트 재추출 (2026-09-02)

원본 시트를 받아 다시 뽑았다. v1.2~v1.3에서 추정으로 이어 붙인 것들을 버렸다.

## 배운 것 — 칸이 프레임 경계가 아니다
시트는 `Charging · Casting · Impact · Settling` 네 칸인데,
**가운데 두 칸은 한 그림이 칸 경계로 나뉜 것**이다. 기운이 지팡이 끝에서
나와 오른쪽 칸으로 이어진다. 칸마다 자르니 기운이 두 동강 났고, 그래서
앞머리가 뭉텅 날아간 채로 나왔다. 이걸 몰라 두 번 헛손질했다.

판별법: 마젠타가 아닌 영역을 라벨링하면 **이어진 칸은 한 덩어리로 나온다.**
칸 좌표를 손으로 재지 말고 이 덩어리를 쓴다.

```
덩어리 x14-244    → Charging
덩어리 x256-778   → Casting + Impact  (두 칸이 붙어 있다)
덩어리 x789-1019  → Settling
```

## 결과
| 프레임 | 내용 |
|---|---|
| m0 | Charging — 기본 공격에도 쓴다 |
| m1 | Casting + Impact **한 장** — 기운까지 들어 있다 |
| m2 | m1에서 기운만 떼어낸 것. 날아가는 탄 전용 |
| m3 | Settling |

규격 92x70 → **198x63** (몸 56x54 · `sw`/`bh`)
기존 5동작도 같은 캔버스로 다시 맞췄다. 정렬은 그림 전체가 아니라
**사람의 발바닥과 좌우 중심** 기준이다 (기운이 발밑보다 내려가는 프레임이 있다).

## 버린 것
`castfx` — 기운을 렌더에서 몸 앞에 덧그리던 방식. 이제 m1 한 장에
들어 있으니 필요 없다. `casttest.js`도 같이 지웠다.
`shamanorb.py`(추정 복원)는 남겨뒀지만 더는 쓰지 않는다.

## 크기
`wuxia.html` **832KB** · 에셋 89개

---

# v1.5 — 검사판 (2026-09-02)

**규칙이 하나 생겼다. 스프라이트를 자르면 게임에 넣기 전에 그림으로 먼저 보인다.**

`python review.py [종류]` — `/mnt/user-data/outputs/review-<종류>.png` 를 만든다.
프레임마다 캔버스 경계 · 발밑 기준선 · 가로 중앙선을 함께 그려서, 위치가
어긋났는지 잘렸는지 한눈에 보이게 했다. 배경은 죽림 바닥색이라 게임에서
보게 될 모습에 가깝다.

의심 신호는 빨간 테두리로 표시한다.
- 그림이 캔버스 가장자리에 닿음 (= 거기서 잘렸다)
- 끝 세 열/행의 픽셀 수가 줄지 않고 끝남 (= 직선 절단)

`spritetest.js`는 규격만 본다. '그림이 이상한 것'은 사람이 봐야 잡힌다.
이걸 안 해서 잘린 지팡이·구체·개구리 정수리를 세 번 놓쳤다.

## 돌려서 찾은 것
- **개구리 idle1 에 2픽셀 티끌** — 줄이는 과정에서 새로 생겼다. 원본 단계
  필터로는 안 잡혀서, 축소 후에도 5px 미만 조각을 터는 단계를 넣었다
- **마왕 swing1 · swing3 이 의심으로 걸림** — swing3 오른쪽에 마젠타 파편이
  남아 있고 몸 외곽에도 분홍 테두리가 돈다. 시트가 없어 아직 못 고쳤다

## 크기
`wuxia.html` **832KB** · 에셋 89개

---

# v1.6 — 개구리 이동 프레임 · 마왕 테두리 (2026-09-02)

## 개구리 얼굴이 잘려 있었다
이동(hop) 프레임을 혀 달린 그림에서 혀만 지워 만들었는데, 자르는 기준을
**'초록의 끝'**으로 잡은 게 화근이었다. 주둥이 끝은 누런빛이라 초록 판정에
안 들어오고, 그 자리에서 자르니 얼굴 앞쪽이 통째로 날아갔다. atk1·atk3도
같은 이유로 주둥이가 잘려 있었다.

**없는 동작을 합성하지 않는다.** 시트 1행 2칸이 혀 없이 몸을 세운 동작이라
그걸 그대로 `hop` 으로 쓴다. 공격의 첫 프레임도 같은 그림이다.

혀 길이를 조절할 때 기준도 바꿨다 — 초록이 아니라 **살구빛 덩어리(혀)의
구간**을 찾아 그 안에서만 자른다. 혀끝 8px 은 건드리지 않는다.

프레임 9 → 8 (`atk0` 이 `hop` 과 같은 그림이라 합쳤다)
```
walk: idle0 → hop → idle2 → hop
atk : hop → atk1 → atk2 → atk3 → idle0
```

## 마왕 발밑 검은 막대
칸의 검은 테두리가 실루엣 바깥에 한두 줄 붙어 나와 있었다. 23장 중 21장.
`demonclean.py` — 가장 바깥 줄이면서, 거의 다 검정이고, 10px 이상 곧게
이어지고, **한 가지 색으로 균일한** 줄만 지운다. 몸의 외곽선도 같은 검정이지만
명암이 섞여 균일하지 않아서 낫날 같은 진짜 그림은 살아남는다.

마젠타 잔털(배경이 가장자리에 번진 자국)도 같이 걷었다. 폭발 프레임처럼
자홍색이 그림의 일부인 경우를 피하려고, 분홍이 전체의 3% 미만인 프레임에만
적용한다. 1,174픽셀 정리.

## 남은 것
`swing3` 오른쪽 돌기둥이 칸 경계에서 직선으로 끊겨 있다. 시트가 있어야 고친다.

## 크기
`wuxia.html` **828KB** · 에셋 88개

---

# v1.7 — 개구리 앞다리 (2026-09-02)

혀를 줄이려고 **세로로 통째** 잘랐더니, 그 자리에 있던 앞발이 같이 잘렸다.
혀는 입에서 앞으로 뻗고 앞다리도 앞으로 뻗어서, 같은 열을 지난다.

**혀만 따로 떼어 줄이고 몸 위에 다시 얹는다.** 몸은 손대지 않는다.
```
rest = 그림 - 혀      (그대로 둔다)
tong = 혀만           (중간 마디를 뺀다)
결과 = rest 위에 tong 얹기
```

혀를 가려내는 판정도 고쳤다. 살구빛만 잡으면 혀의 검은 외곽선이 남아
당겨도 폭이 안 줄었다. **열마다 혀가 지나는 높이를 재서, 그 높이에 걸친
덩어리를 통째로** 혀로 본다.

재단 뒤에 딱 맞게 잘라내던 것도 없앴다. 프레임마다 잘리는 자리가 달라져
몸이 1px 씩 흔들렸다. 공통 캔버스에서 한 번에 정리한다.
이제 atk1·atk2·atk3 의 몸 픽셀이 완전히 같다 (왼쪽 100열 차이 0).

---

# v1.8 — 마왕 시트 재추출 (2026-09-02)

시트를 받아 24프레임을 다시 뽑았다. 포효가 4장이 아니라 **5장**이었다.

## 칸을 어떻게 찾았나
칸 테두리가 순검정이 아니라 **마젠타와 섞인 어두운 보라**(예: 115,11,113)라
검정으로 찾으면 한 줄도 안 잡힌다. 밝기 130 미만으로 찾고, 위·아래 테두리
줄을 짝지어 3행을 만들었다. 위 테두리는 촉수가 가로질러 끊겨 있지만
줄 자체는 남아 있어서 짝은 맞는다.

## 순서가 중요하다
1. 마젠타 + 글자띠(흰 속살·검은 테두리·회색 번짐·보라 번짐)를 배경으로
2. 칸 안쪽에 걸친 덩어리만 고른다 — 칸보다 34px 위까지 본다
3. **그 다음에** 칸 테두리를 지운다

3번을 먼저 하면 테두리를 가로지르는 촉수가 두 동강 나서 위쪽이 버려진다.
테두리 판정은 색으로 한다 — 검정(밝기<50)이거나 보라(r,b 가 g보다 20 이상
높고 밝기 170 이하). 초록 촉수와 폭발의 밝은 분홍은 남는다.

## 촉수가 분홍이던 문제
촉수가 1~2px 이라 전체가 마젠타와 섞여 분홍이 됐다. 잔털이라고 지우면
촉수가 통째로 사라진다. **지우지 않고 마젠타 성분만 걷어낸다** —
r·b 를 g+12 이하로 끌어내린다. 자주색이 진짜 그림인 프레임(포효 후반·폭발)은
분홍 비율 35%를 기준으로 갈라 건드리지 않는다.

## 결과
규격 92x111 → **114x96** · 프레임 23 → **24** (roar4 추가)
스킬 동작이 포효5 + 폭발5 = 10프레임이 됐다 (fps 7.4 / 1.35초)

## 크기
`wuxia.html` **909KB** · 에셋 89개

---

# v1.9 — 저장·오프라인 진행 (2026-09-03)

리텐션 장치 0개이던 것을 해결했다. 껐다 켜면 이어지고, 자리 비운 만큼 진행된다.

## 저장 (`src/65-save.js`)
- localStorage 키 `wuxia1`, 10초마다 + 화면이 가려질 때(`visibilitychange`/`pagehide`) 저장
- 저장하는 것: 구역·단계·처치·최고기록·해금·누적처치·쓰러짐 횟수
- 깨진 저장·버전 불일치는 버리고 새로 시작. 저장 실패(시크릿 모드)해도 게임은 돈다
- 테스트 모드의 사냥터 패널에 '저장 초기화' 버튼

## 오프라인 진행
설계(조사로 정한 것): 온라인의 70~80%, 상한 8시간, 1분 미만은 무시.

실제 step을 8시간 돌릴 수 없어 근사 모델을 쓴다:
`처치당 시간 = ceil(적체력/정권피해) × 공격간격 ÷ 동시타격 + 접근시간`, 보스는 고정 초.
`sim.js`(실제 step 60분) 실측과 대조해 보정했다 — aoe 2.3 · walk 0.5 · boss 60일 때
모델이 실측의 약 87%(평균 오차 14%)이고, 여기에 rate 0.8을 곱해 온라인의 약 70%.
정권이 범위 안 모든 적을 때리는 광역이라(heroHitCheck) 동시타격 계수가 필요했다.
숫자는 전부 `00-data.js`의 `SAVE`·`OFFLINE`.

보스 단계에서 남은 시간이 60초 미만이면 거기서 멈춘다. 마지막 구역은 step과
똑같이 1단계부터 돈다. 돌아오면 가운데 카드로 요약을 보여준다
(수련 시간·처치·단계 전진·구역 돌파). 닫기: 버튼·바깥 클릭·ESC.

## 검증 (`node savetest.js`)
jsdom 실제 DOM으로: 자동 저장 → 3시간 오프라인 적용 → 패널 표시·닫기 3종 →
8시간 상한(24시간 = 8시간) → 깨진 저장 복구. 전부 통과, 런타임 오류 0.

## 크기
`dist/wuxia.html` **915KB** · 에셋 89개

---

# v1.10 — 은자·체력바·배속 (2026-09-03)

기획 상의로 확정: 1순위 은자+체력바, 무공 발동은 자동/반자동/수동 3모드,
은자 소스는 처치 드랍·보스 첫 격파·오프라인 정산 (단계 클리어 보너스 없음).

## 은자 (첫 재화)
- 처치 드랍: `(2 + 단계) × 구역배율`. 보스는 10배. 수치는 임시 — 쓸 곳(심법)이
  들어오면 sim으로 다시 잡는다. 숫자는 `00-data.js`의 `SILVER`
- 보스 **첫** 격파: `80 × 구역배율` 보너스 + 토스트. 구역별 1회(`S.bossDone`)
- 오프라인 정산: 근사 모델이 단계를 걸으며 그 단계 드랍으로 계산.
  첫 격파 보너스는 직접 잡을 때만 준다
- HUD 카드에 은자 줄 추가(금색), 돌아온 패널에 '은자 +N' 줄
- 저장에 silver·bossDone 포함. 예전 저장은 0으로 시작 (버전 유지)

## 체력바
주인공 머리 위 초록 체력바 — 다쳤을 때만 뜬다. 적(빨강)과 색으로 구분.
적 체력바·보스 상단 바는 기존 그대로.

## [테스트 전용] 배속 x1·2·3·5·10·100
좌하단 버튼 줄. 같은 dt로 step을 여러 번 밟는 방식이라 물리가 안 깨진다.
`TESTSPEED`는 `70-main.js`에만 있다. 테스트 전용 요소(단계 이동·강제 쓰러짐·
저장 초기화·배속)는 전부 `TEST` 게이트 + `[테스트 전용]` 주석으로 묶어 뒀다.

## 크기
`dist/wuxia.html` **918KB** · 에셋 89개

---

# v1.11 — 수련(스텟)·하단 탭바 (2026-09-03)

기획 상의 확정: 스텟 용어는 쉬운 말로(무협 한자어는 어렵다는 피드백),
상한은 경지 연동, UI는 하단 탭바 시작.

## 수련 (`src/62-train.js`)
- 5종: 공격(+2/Lv) · 체력(+25/Lv) · 회복(+0.5/Lv) · 이동 속도 · 치명타
- 이동·치명타는 점근식 `max×n/(n+half)` — 이속 최대 +60%, 치명 최대 50%.
  아무리 올려도 상한을 못 넘으니 %스텟이 밸런스를 못 깨뜨린다
- 상한 = 최고 경지(S.best) × 10. 경지가 오르면 수련 그릇이 커진다
- 비용 = 12 × 1.16^Lv (임시 — 심법 때 sim으로 재조정)
- 버튼 꾹 누르면 연속 구매(pointer 이벤트, 130ms 간격). 열려 있는 동안은
  줄을 다시 만들지 않고 값만 갱신한다 — 누르던 버튼이 안 끊긴다
- 치명타는 1.5배 + 노란 숫자 fx
- 저장에 stats 포함. 옛 저장은 전부 0에서 시작

## 하단 탭바
[수련][사냥터] 두 탭. 재클릭이면 닫힘, 다른 패널은 접힘, 살 수 있으면
수련 탭에 금색 알림점. 우하단 사냥터 버튼은 탭바로 흡수. 시험 버튼은
탭바 위로 올렸다.

## 크기
`dist/wuxia.html` **925KB** · 에셋 89개

---

# v1.12 — 경지 (2026-09-03)

기획 확정: 경지 재료는 처치 수련치(추후 재료 추가 여지), 사다리는 무협 기반으로
길게, 무한 성장.

## 구성
- 9경지(삼류·이류·일류·절정·초절정·화경·현경·생사경·자연경) × 4구간
  (초입·소성·대성·극성) = 36구간, 이후 신화경 1중·2중·… 무한
- 수련치: 잡몹 = 구역배율×1, 보스 = 구역배율×25. 승급 필요량 = 20×1.18^k
  (sim 실측으로 보정 — 60분에 생사경 소성, 죽림 안에서도 몇 분마다 승급)
- **자동 성장을 단계→경지로 이관**: 승급당 공격+5·체력+70·회복+1.3.
  단계(lv)는 이제 구역 진행도일 뿐이다. 수련 상한도 경지 기준((경지+1)×10)
- 승급 순간: 토스트("절정 초입에 올랐다") + 터짐 fx + 진동. HUD 좌하단에
  경지 이름·진행% 상시 표시
- 오프라인 정산에도 수련치가 쌓인다. 저장에 rexp 포함
- 테스트 단계 이동(gotoZone)은 seedExp(구역,단계)로 걸맞은 수련치를 자동 지급
  — 경지 없이 천산에 가면 못 버티기 때문. bosstest도 이 경로로 통과

## 검증
sim 60분: 죽림(삼류→이류)→…→천산(화경→생사경 소성), 쓰러짐 23(과하지 않음).
bosstest 5구역 47~89초. npm run check + 회귀 8종 전부 통과.

## 크기
`dist/wuxia.html` **927KB** · 에셋 89개

---

# v1.13 — 경지 표기 개선 (2026-09-08)

"급수가 어렵게 표현된 건 아닐까"라는 고민에서. 결론: 경지 이름(삼류~신화경)은
장르 표준이라 유지하고, 어려운 건 두 가지였다 — 순서가 안 읽히는 세부 단계
(초입·소성·대성·극성)와 신화경 N중 표기.

- 세부 단계 → 숫자 성: "절정 2성". 신화경은 1성부터 무한 (N중 폐지)
- HUD의 경지 표시를 누르면 **전체 사다리 패널** — 지나온 경지·현재 위치(N성·
  진행%)·다음 경지가 한눈에. 용어의 해법은 쉬운 말이 아니라 위치가 보이는 것
- 닫기 4종(✕·바깥·ESC), 다른 패널과 배타 열림

## 크기
`dist/wuxia.html` **929KB** · 에셋 89개

---

# v1.14 — 무공 (2026-09-08)

"무공 테이블을 만들자. 경지별로 습득 가능하고, 표로 뭐가 있는지 보이게,
습득 조건은 알아서, 기연 자리도" — 그대로 구현.

## 표 (00-data.js ARTS — 12종)
- **초식 5** (자동 시전, 쿨다운, 절차 이펙트 — 새 그림 없음):
  파공권(이류1성·200, 근거리 3배 기파) → 선풍퇴(일류2성·1,200, 광역 1.5배+넉백)
  → 백보신권(절정2성·5,000, 최원거리 5배) → 활인기공(초절정4성·25,000,
  체력 40% 이하일 때 30% 회복) → 붕산장(화경3성·60,000, 전 화면 4배)
- **심법 5** (% 증폭 — 수련의 고정치와 역할 분리):
  삼재심법(회복+25%) → 철우공(체력+20%) → 유운심법(이동·회복+15%)
  → 혼원일기공(공격+15%) → 태허진경(전능력+10%)
- **기연 전용 2** (표에 보이지만 못 얻음 — 기연 판에서 연다):
  구양신결(장무기 모티프) · 건곤이형
- 습득 = 경지 도달 + 은자. 익히면 되돌리지 않는다. 표에는 미도달 무공도
  조건("절정 2성에 열린다")과 함께 보인다 — 다음 경지를 오를 이유가 된다

## 시전·연출
쿨다운 자동 시전(stepArts), 대상 없으면 쿨 아낌. 이펙트는 절차식:
기파 줄기(streak)·기의 고리(ring)·활인 고리(heal)·무공 이름 부상(artname).
시전마다 이름이 파랗게 떠서 "무공이 보인다"(조사: 팬 갈증 1위).

## 밸런스 (sim)
전 무공 무료 보유 상한 케이스: 60분 처치 +16%(2,644→3,076), 쓰러짐 23→0.
폭주 없이 생존·체감만 좋아진다. 비용·배수는 임시.

## 검증
artstest.js 신설(표 노출 12종·습득 조건·심법 곱연산·초식 시전·저장 왕복),
npm run check에 포함. 전 회귀 통과.

## 크기
`dist/wuxia.html` **939KB** · 에셋 89개

---

# v1.15 — 무공 문파 계보·개명 (2026-09-08)

무공 작명 조사(docs/조사-무공작명.md) 반영. 김용 고유명·소림 72절기
실명을 피하고, 구역 모티프로 오리지널화. 문파 계보(SCHOOLS) 도입 —
무공 표의 각 줄에 문파 칩이 붙는다(색깔로 구분).

- 문파: 청죽문(죽림)·야우문(폐촌)·암향각(동굴)·설매궁(설산)·천운관(천산)
  + 독학·실전 비급(기연). 아직은 계보 표시일 뿐 — 나중에 명성·인연으로
  문파별 무공 개방을 얹는다
- 개명(키 유지, 저장 호환): 철우공→청죽공, 유운심법→야행심법,
  백보신권→암향지(72절기 실명 회피), 붕산장→설붕장
- 외침·표·저장 전부 새 이름 자동 반영. npm run check 통과

## 크기
`dist/wuxia.html` **940KB** · 에셋 89개

---

# v1.16 — 타격 숫자·외침 정리·무공 그리드 (2026-09-08)

- **타격 숫자**: 매 타격마다 조그만 흰 숫자, 회심(치명타)은 크고 노랗게
  튄다(순간 확대). 초식·평타·보스전 전부 hurtFoe 한 곳에서 나간다
- 초식 외침에서 낫표 제거 — "파공권!" 만
- **무공 패널 개편**: 문파별 섹션 + 아이콘 타일 그리드(한자 머리글자 +
  이름)로 12종이 한눈에. 타일 선택 → 상단 상세 칸에 설명·조건·구매.
  상태는 색으로: 익힘(문파색 테두리)·잠김(흐림)·기연(금색 점선)
- artstest를 그리드 구조로 갱신, 전 검증 통과

## 크기
`dist/wuxia.html` **942KB** · 에셋 89개

---

# v1.17 — 문파를 유명 문파 기반으로 재편 (2026-09-08)

방향 정정(사용자): 지역 연고 창작 문파가 아니라 기존 무협의 유명 문파
기반 + 우리 창작 문파. 문파명은 실존 지명·일반명사라 무공명과 달리
자유(화산귀환 선례).

- SCHOOLS 재편: 독학 · **청죽문(우리 문파, 죽림 뿌리)** · 소림 · 무당 ·
  화산(무공 예정) · 아미 · 개방 · 당문 · 마교 · 실전 비급
- 계보 재배정: 파공권→소림, 선풍퇴·야행심법→개방, 암향지→당문,
  활인기공→아미, 혼원일기공·태허진경→무당, 청죽공→청죽문,
  붕산장(설붕장에서 원복 — 마교 패도 장법이라 원래 이름이 맞다)→마교
- 야우문·암향각·설매궁·천운관 폐기. 무공 키는 전부 유지(저장 호환)

## 크기
`dist/wuxia.html` **942KB** · 에셋 89개

---

# v1.18 — 기연 (2026-09-08)

조사 결론 1순위. 공짜 랜덤 금지 — 누적된 인연의 정산(장무기 공식).

## 인연 → 기연
- 인연(S.karma): 처치 = 구역배율×1, 보스 +120, **쓰러짐 +60** (고난이
  기연의 씨앗 — 장무기 서사 그대로)
- 필요량 400×1.4^회차. 차면 단계 진입(enterStage) 순간 기연 카드 —
  전투를 안 끊는다. 게이지는 일부러 숨김(놀라움 보존)
- 종류: 낡은 비급(은자 = 처치 드랍×250) · 천년 영약(완전 회복 + 다음 승급
  수련치 60%) · 은거기인(경지 닿은 미습득 무공 무료 전수) · 실전 비급 조각
  (4회차부터 가중 35%, 3조각 = 구양신결/건곤이형 해금)
- 오프라인에도 인연이 쌓이고, 복귀 카드에 "✦ 기연 기다리고 있다" 예고 →
  닫으면 기연 카드가 이어진다
- 카드: 금테 "기연 奇緣", 받아들인다 버튼 하나 (거절 없음)

## 검증
fatetest.js 신설(축적·카드·보상·조각 해금·저장 왕복), npm run check 포함.
부분 로드 도구(sim 등)를 위해 enterStage의 maybeFate 호출에 가드.
전 회귀 통과, sim 60분 진행 변화 없음.

## 크기
`dist/wuxia.html` **948KB** · 에셋 89개

---

# v1.19 — 무공 패널 압축 (2026-09-08)

"무공 보기 불편함" — 문파별 섹션 9개가 한 줄에 타일 한두 개뿐이라
세로로 길었다. 섹션을 초식/심법 둘로 줄이고 문파는 타일 위 색띠로:
상세 카드 + 초식 한 줄 + 심법 한 줄 = 12종이 스크롤 없이 한 화면.
줄 안 정렬은 습득 조건(경지) 순 — 진행 순서가 왼쪽에서 오른쪽으로 읽힌다.

---

# v1.20 — 오프라인 정산 개편·무공 숙련도·몹 상향 (2026-09-08)

## 오프라인: 단계를 넘지 않는다 (사용자 확정)
제자리 사냥 정산만: 은자 전부(방침: 확실한 보상) · 수련치는 시간 기준
(8시간 = 승급 3회 분량 — 약한 적 3만 컷으로 경지 폭주하던 부작용 차단) ·
인연은 필요량 1.5배 상한 · 심법 숙련도 처치 수만큼. 복귀 카드에서
단계 전진/구역 돌파 줄 제거.

## 무공 숙련도 (사용자 확정)
- 초식 = 시전 횟수, 심법 = 지닌 채 처치 수 (오프라인 포함)
- 게이지 40×3^성이 차면 **돌파**: 은자 습득비×3^성, 성당 효과 +25%, 4성 상한
- 돌파하면 숙련 0부터 (이월되면 성이 연달아 뚫려 벽이 안 됨 — 검증에서 잡음)
- 돌파 재료·기연 조건은 재료 시스템 도입 때 (사용자: "재료나 기연 등")
- 타일에 "파공권 2성" 표기, 상세에 게이지·돌파 버튼, 탭 알림점에 돌파 포함

## 몹 상향 ("성장 대비 약하다")
체력 20+13i(구 16+9i), 피해 3.0+0.85i(구 2.8+0.7i). sim: 60분 천산 9단계·
쓰러짐 53(기준선 — 실제는 수련·무공 투자로 완화). 구역 도달 시점이 기존
REALM.seed와 일치해 재보정 불필요. 첫 안(피해 3.4+1.0i)은 쓰러짐 61로 과해 완화.

## 크기
`dist/wuxia.html` **950KB** · 에셋 89개

---

# v2.0 — 지수 난이도 재설계: 콘텐츠 수명 (2026-09-08)

"몬스터가 약하다 = 진행이 빨라 콘텐츠가 금방 소모된다"는 지적(사용자).
선형 몹으론 벽이 없어 60분에 천산까지 갔다. 원 확정(단계당 1.30배,
처치 목표 24+단계×7)을 복원하고 전면 지수로 재설계.

## 구조
- 전역 단계 g(1~50) = 난이도 축. 적 체력 22×1.30^(g-1) · 피해 3×1.19^(g-1)
  · 처치 목표 24+7g. ZONES.mul 폐지, 은자·수련치·인연도 전부 g 지수
- 주인공: 승급당 피해·체력·회복 ×1.31 (적과 등가 — 승급 하나 = 한 단계).
  수련 스텟 고정치 → %(+2%/Lv). 보스 배수 62→30
- 수련치 수입 1.07^g ≪ 승급 필요 25×1.30^k → 깊어질수록 벽이 서고,
  벽에서 은자·숙련·기연으로 뚫는다

## sim v2 (소비 전략 포함 24시간)
30초마다 수련 구매(최대 10회)·무공 습득·돌파를 흉내. 실측:
5분 죽림6 / 30분 폐촌10 / 1시간 동굴9 / 2시간 설산7 / 8시간 천산7,
완주(천산 보스)는 며칠~1주+. 쓰러짐 전선에서만(초반 0, 8시간 107).
보스 격파 23초(폐촌)→57초(동굴)→113초(설산)→7분(천산 최종 — 결전답게).
튜닝 이력: killGrow 1.20→1.12→1.10→1.07, GROW.dmg 1.40→1.34→1.31,
dmgGrow 1.14→1.22→1.19 (각 단계 sim으로 판단).

## 이주
- REALM.seed [0,11,17,24,29]로 재보정, bosstest 관찰 시간 12분으로,
  artstest 체력 검사를 artMul 기준으로. 기존 저장 호환(수치만 바뀜 —
  단, 경지 대비 체감 위치는 달라질 수 있다)

## 크기
`dist/wuxia.html` **951KB** · 에셋 89개

---

# v2.1 — 수련 8종·구매 배수·무공 표시 (2026-09-08)

"x10 x100 max 한 번에, 스탯 더 다양하게(공격속도 등), 가치 따라 가격·
상승곡선 다르게" + "성이 올라도 안 좋아져 보임" + "업그레이드 가능한
스킬 표시, 배움/안 배움 구분" (전부 사용자).

## 수련
- 3종 → 8종: 공격·체력·회복(+2%/Lv, cb 8~12·cg 1.14~1.16) /
  공격 속도(50%·cb40·cg1.22) · 치명타(50%·cb25) · 치명 피해(+100%·cb30) ·
  이동(60%·cb15) · 은자 획득(+100%·cb20) — 점근식은 asym:[최대,절반점]
- 공격 속도는 정권 애니 재생(40-step)·공격 주기·쿨다운(30-combat)을 함께
  당긴다. 치명 피해는 critMul()=1.5+cdmg%, 은자 획득은 killSilver에 곱
- 구매 배수 x1·x10·x100·MAX (trainPlan/buyStatN) — 버튼에 총비용 ×개수

## 무공 표시
- 효과 요약이 숙련 성 실효값(×1+0.25(성-1))으로 나오고 "돌파하면 →" 예고 추가
- 타일: 익힌 무공 = 문파색 바탕(22 알파)·테두리, 안 익힌 = 흑백 글리프,
  ▲ 돌파 가능(금색 점멸 배지) · + 배울 수 있음(청록 배지)

## 밸런스 (sim, 에이전트 검증)
SIM_MIN=240: 5분 죽림6 / 30분 동굴1 / 1h 동굴9 / 2h 설산8 / 4h 천산4 —
진행 속도 v2.0과 동일. 240분 잔고 46만 < 다음 구매 ~70만으로 "다음 구매
대기 자금" 수준 — v2.0의 후반 은자 적체가 8스텟으로 해소. 쓰러짐 21(전부
2h 이후 전선, 균등 구매 시뮬 특성상 상한 추정). 스텟별 한계비용이 68만~82만
으로 수렴 — 비싼 스텟(공격속도·치명피해)도 방치되지 않는다. cb/cg 유지.
점검 절차는 docs/QA-밸런스.md로 문서화 — 새 성장 축은 이 표와 sim spend()에
반드시 추가.

## 크기
`dist/wuxia.html` **954KB** · 에셋 89개

---

# v2.2 — 무공 연마 + 발동 3모드 보류 (2026-09-08)

## 연마 (무공 레벨업, 백로그 확정분)
- 익힌 무공을 은자로 레벨업 — 레벨당 효과 +2%(수련과 같은 결)
- **상한 = 성×10(최대 40)** — 돌파(숙련 게이지)가 상한을 여는 벽 구조.
  경지↔수련 상한과 같은 문법
- 비용 = 습득비×0.25×1.2^(Lv-1). artEff = (1+0.02(Lv-1))×(1+0.25(성-1))
- 상세 칸에 연마 Lv·버튼, 타일 배지 ↑(파랑) — ▲(돌파)·+(습득)과 우선순위
- 기연 무공(cost 없음)은 연마·돌파 불가 — 재화 설계 때 다시 본다
- 저장(artLv)·sim spend()(틱당 5회)·QA-밸런스 표에 반영, artstest 검사 추가

## 발동 3모드 보류 (사용자 확정)
내공(마나)이 없어 소비 없이 전부 자동으로 쏘는 지금은 반자동/수동이
"공짜 스킬을 손으로 누르는 것"일 뿐 — 내공 재화가 들어올 때 함께 설계.

## 문서
- docs/QA-밸런스.md 신설(밸런스 점검 축 총람 — 새 축은 표+spend() 필수)
- docs/프롬프트-무공이펙트.md — 무공 12종 제미나이 이펙트 시트 프롬프트

밸런스 시뮬(240분) 검증은 에이전트로 진행 중 — 판정·튜닝은 후속 항목에.

## 크기
`dist/wuxia.html` **956KB** · 에셋 89개

---

# v2.3 — 눈에 보이는 성장: 기운·권기·초식 시전 (2026-09-08)

사용자가 제미나이로 뽑은 시트 6장(주인공 기운+권격 / 파공권 / 선풍퇴 /
암향지 / 붕산장 / 활인기공)을 전부 반영. "최대한 활용해서 자연스럽게"
+ "반영하면 구동 검증 로직을 항상 추가" (사용자 확정).

## 들어간 것
- **경지 기운** — 대기·이동 중 몸 뒤 안개. 절정 흰 / 화경 금 / 생사경 청
  / 신화경 자주 (HFX.auras). 심법 시트의 안개만 분리해 4색으로 구움 —
  심법별 중첩 기운은 난장판이라 폐기(사용자 동의), 색은 경지가 말한다
- **권기 정권** — 절정(k12)부터 정권 스트립이 푸른 권기 판(katk 64px)으로
- **초식 시전** — 액티브 5종 전용 cast 동작(P.castT·castK, 0.5초, ANIM.cast).
  파공권 권기 주먹 탄(pashot)·암향지 지풍 빔(bshot)이 절차 선 대신 날아간다.
  선풍퇴 발차기 회오리·붕산장 흑적 장풍 구체·활인기공 연꽃 치유
- 밸런스 여파: 연마 lvPer 0.02→0.015 (쓰러짐 21→1로 소멸해 기연의 고난
  씨앗이 말랐다 — 0.015에서 쓰러짐 4, 진행 곡선 동일. sim 240분 재검증)

## 추출 (겪은 것 → 파이프라인 갱신)
- 행마다 칸 폭이 다른 시트: 가로 테두리로 행을 가르고 행 안에서 세로 탐지
- 암향지 빔(어두운 보라)이 마젠타 제거에 먹힘 → 순마젠타 판정에 r 하한
- 활인기공은 칸 배경이 회색 — flood-fill로 가장자리 연결 배경만 제거
- 기운 프레임의 유령 외곽선(몸 반투명 테두리) — 몸 3px 이내 반투명 전부 제거
- 잘린 프레임(권격 4·8번)·마젠타 잔재 프레임은 버림. 건곤이형 시트 2장 보관

## 검증
- **fxtest.js 신설** (check에 포함) — 시전이 초식별 스트립·폭으로 그려지나,
  탄이 날아가나, 권기 정권이 경지 문턱대로 바뀌나, 기운 4색이 켜지나를
  jsdom drawImage 후킹으로 구동 검증. 규칙 0에 "반영 후 구동 검증" 명문화
- 실기(Playwright): 시전 컷·경지별 기운 5컷 확인. 기운은 idle 한정이면
  사냥 중 안 보여서 run에도 그리게 수정

## 크기
`dist/wuxia.html` **1159KB** · 에셋 101개

---

# v2.3.1 — 시전 프레임 확장 6~9장 (2026-09-08)

"4장이면 이펙트가 빈약하다, 무공은 재빠른 느낌이 중요하다" + "활인기공
잘못 잘랐다"(사용자). 시트 칸을 최대한 써서 시전을 늘렸다.

- 파공권 7 · 선풍퇴 9(회오리 피날레) · 암향지 6 · 붕산장 6(장풍 파열)
  · 활인기공 8(손모음→기운→연꽃 만개, 순서대로 다시 자름)
- HFX.cast가 [에셋, 폭, 프레임 수]가 되고 castFps 16으로 재생 —
  시전 길이 = 프레임 수/16 (0.38~0.56초). beginCast() 헬퍼로 통일
- 암향지 7번째 칸(원본에서 몸이 잘린 것)은 검수에서 걸러 제외
- spritetest·fxtest가 프레임 수·폭을 HFX에서 읽는다 (하드코딩 제거)

## 크기
`dist/wuxia.html` **1283KB**
---

# v2.4 — 성 연동 시전 컷·양손 정권·탄 기울기 (2026-09-08)

사용자 셋: "성급이 오르면 신컷을 더 써서" / "일반 공격 양손으로" /
"투사체가 나가는 방향으로 기울어야".

- **시전 컷 = 숙련 성 비례** (castStar [0.55/0.7/0.85/1.0]) — 1성은 처음·
  끝을 지킨 성긴 판, 돌파할수록 중간 컷이 채워져 동작이 유려해진다.
  성장 보상이 눈에 보이는 축 하나 추가
- **양손 교대 정권** — 권기 시트의 두 줄이 오른손/왼손이라 8프레임
  (katk 66px, katkN 8)으로 묶고 공격마다 P.atkAlt 토글
- **탄 기울기** — pashot·bshot이 비행 방향으로 회전. 왼쪽 발사는
  거울(scale -1) 뒤 반전각이라 위아래가 안 뒤집힌다
- fxtest 추가 검증: 양손 앞/뒷절반 칸 슬라이스, castN 1성<4성,
  성긴 판의 처음·끝 컷 보존. spritetest에 katk 규격 검사

## 크기
`dist/wuxia.html` **1301KB** · 에셋 101개

---

# v2.4.1 — 이미지 기준 재단: 지풍 통짜 빔·활인기공 재추출 (2026-09-08)

"이미지 기준으로 잘라라, 암향지 길쭉한 두 칸은 붙여서" + "활인기공
3·4컷 오른쪽 잘림, 8프레임 기본" (사용자).

- **암향지 지풍** — 원본에서 빔 한 줄기가 칸 두 개를 관통하는 연결된
  그림이었다("칸은 프레임 경계가 아니다" 재현). 두 칸을 통짜로 잘라
  간극 열을 접합, 140×49 한 프레임의 긴 빔으로 (bshot). 어두운 빔이
  흰 안개용 알파 공식에 씻기던 것도 마젠타성(min(r,b)−g) 공식으로 교체
- **활인기공** — 추출 때 안쪽 5px 일괄 절삭이 경계까지 그린 기운을
  깎았다. 안쪽 2px + 겉 고리의 어두운 테두리 선만 제거하는 내용 보존
  방식으로 재추출, 8프레임 유지
- 렌더: 지풍은 통짜 1프레임(소멸은 알파), 파공권은 비행 2+소멸 2 유지

## 크기
`dist/wuxia.html` **1302KB** · 에셋 101개

---

# v2.4.2 — 앱 전환 복귀 정산·활인기공 가장자리·시전 속도 (2026-09-08)

"오프라인 왜 안 됨" + "활인기공 여전히 오른쪽 잘림" + "씬컷이 너무 빨라
시전이 빨리 끝남" (사용자).

- **오프라인의 진범은 모바일 앱 전환** — 탭을 안 닫으면 페이지가 다시
  로드되지 않아 로드 시 정산이 영영 안 돌았다. visibilitychange로
  hidden 시각을 기억했다가 **다시 보이는 순간 정산·패널 표시**.
  처치 0이어도 패널을 띄우고(수련치는 시간으로 쌓이므로) 수련치 줄 추가.
  savetest에 복귀 정산 회귀 검사. 로드 경로 정산은 원래 정상이었다
  (Playwright로 시각 조작 검증 — pagehide 저장이 실험을 오염시켰던 것)
- **활인기공** — 남은 잘림은 원본 칸이 경계에서 뚝 끊긴 절단 프레임
  (연꽃 만개·전신 광휘, 끝 열 픽셀 50개+). 복원은 불가라 가장자리 5px
  알파 페더로 녹여 절단감 제거
- **castFps 16→10** — 컷이 씹혀 보이던 문제. 4성 시전 0.6~0.9초

## 크기
`dist/wuxia.html` **1302KB** · 에셋 101개

---

# v2.5 — 절차 발광·팝업 자동 진행·수련창 정리 (2026-09-08)

"암향지 손끝 원형이 안 보임, 무공별 이펙트를 직접 넣어라" + "팝업
눌러주기 귀찮다" + "스탯창 불편, 배수 고정·금액 이상함" (사용자).

- **절차 발광** (HFX.glow) — 무공 색 동심원 3겹을 가산 합성으로:
  시전 손끝(암향지 보라·파공권 금·붕산장 적·활인기공 초록·선풍퇴 모래)
  + 탄(파공권 몸통, 지풍 촉끝). 그라디언트 없이 픽셀풍 원으로
- **팝업 자동 진행** — 기연 카드 5초(버튼에 카운트다운 표기), 복귀
  카드 6초 뒤 스스로 진행. 수동 클릭도 그대로 된다
- **수련창** — 배수 토글 줄 상단 고정(sticky). x10·x100은 정확히
  N개 총액을 표기하고 모자라면 비활성(금액 흔들림 제거), MAX만 유동
- 실기 검증: 카운트다운·자동 수락·정액 표기·발광 스크린샷

## 크기
`dist/wuxia.html` **1305KB** · 에셋 101개

---

# v2.5.1 — 무공창 클릭 증발 수정 (2026-09-08)

"무공창 내부 클릭이 안 됨" (사용자). 전투 중엔 처치마다 은자·숙련이
변해 artsHud→refreshArts가 상세 칸 innerHTML을 매번 갈아엎었고,
손가락이 버튼을 누르는 도중(다운→업 사이) DOM이 교체되면 클릭이
증발했다 — 모바일의 느린 탭일수록 잘 터진다.

- 상세 칸에 **구조 서명**(선택 무공·성·연마 Lv·구매 가능 여부 등)을
  도입 — 서명이 같으면 DOM을 유지하고 숙련 숫자(span#axp)만 제자리 갱신
- 서명이 바뀌는 건 실제 문턱을 넘을 때뿐이라 재구성이 드물어진다
- Playwright로 재현·검증: 60ms마다 값이 변하는 중 320ms 느린 탭 →
  배우기·타일 선택 모두 정상

## 크기
`dist/wuxia.html` **1306KB** · 에셋 101개

---

# v2.6 — 건곤이형·구양신결 실효 (2026-09-08)

"건곤이형은 왜 안 했냐"(사용자). 발동 방식이 없어 시트만 보관 중이었다
— 반격형으로 설계해서 넣었다.

- **건곤이형** — 반격형 초식: 맞는 순간 받은 피해 50%를 흘리고(guard)
  3배×숙련계수로 가장 가까운 적에게 되돌린다(ref). 14초 쿨, 자동 시전
  없음(stepArts는 쿨만 돌리고 hurtHero에서 발동). 태극 원반 연출
  (gshield 5프레임, 원형 마스크) + 시전 동작(castg 5프레임) — 보관하던
  sheets/geongon_fx1·2.png에서 추출
- **구양신결** — 공+20% 체+20% 회복+30% (전설 심법답게 최대 폭)
- 두 기연 무공에 cost 20만 부여 — 연마·돌파가 열린다 (습득은 기연만)
- artstest 반격 4검사·fxtest 원반 렌더·spritetest 규격 추가.
  발견: 70-main 이미지 로드 누락을 fxtest가 잡았다 (구동 검증 규칙의 값)

## 크기
`dist/wuxia.html` **1376KB** · 에셋 103개

---

# v2.6.1 — 태극 원반 재추출: 그림 모양 그대로 (2026-09-08)

"원형이 잘리면 어떻게 해, 그림에 맞게 컷해야지"(사용자). 칸 격자 추출이
어긋난 걸 원형 마스크로 덮었던 게 잘못 — 바깥 고리가 깎였다.

- 격자를 버리고 **마젠타 아닌 덩어리 라벨링**으로 재추출 — 원반 하나가
  컴포넌트 하나라 bbox가 곧 그림의 모양이다 (테두리 격자는 긴 직선만 제거)
- 7프레임으로 확장: 흐린 태극 → 금테 → 진한 태극 → 고리 → 금 소용돌이
  → 되쏘기 → 파열 (76×80, 마스크 없음)
- CLAUDE.md 사고 목록에 "그림을 도형으로 가정하고 깎지 마라" 추가

## 크기
`dist/wuxia.html` **1421KB** · 에셋 103개

---

# v2.7 — 돌파 조건 강화: 연마 상한 + 숙련 만충 (2026-09-08)

"스킬 레벨이랑 횟수 다 차야 업글 가능하게"(사용자).

- canBreak에 **연마 상한 도달** 조건 추가 — 돌파 = 연마 Lv 상한 + 숙련
  게이지 만충 + 은자. 갈고 → 익히고 → 넘는 3단 벽
- 숙련만 찼을 땐 "연마를 상한까지 채우면 돌파가 열린다" 안내, 둘 다
  차면 돌파 버튼 + ▲ 배지 (sig에 lv가 있어 자동 갱신)
- sim(SIM_MIN=120): 진행 곡선 기준선과 동일(2h 설산8·쓰러짐 1),
  은자 잔고는 더 낮아짐 — 연마+돌파 소비 증가로 소비처가 두터워졌다
- artstest: "숙련만 차선 못 뚫는다" 포함 기대값 재조정

## 크기
`dist/wuxia.html` **1421KB** · 에셋 103개

---

# v2.7.1 — 은자 아이콘 (2026-09-08)

"은자 이런 건 글 말고 아이콘으로"(사용자).

- 은원보(元寶) 픽셀 아이콘 14×9를 직접 그려 assets/silver.png로 —
  HUD 우하단, 수련 헤더·버튼 단위, 무공 상세(습득·연마·돌파·잠금 문구),
  복귀 카드, 기연 낡은 비급 문구까지 coin() 헬퍼로 통일
- HUD 은자 숫자는 span#silvern으로 분리 (textContent 갱신이 아이콘을
  지우지 않게)

## 크기
`dist/wuxia.html` **1422KB** · 에셋 104개

---

# v2.7.2 — 은자 아이콘 엽전으로 (2026-09-08)

"동전 모양으로 하자, (은원보는) 진짜 못 그렸다"(사용자). 은원보 →
**엽전**(둥근 은전 + 네모 구멍, 12×12)으로 다시 그렸다. coin() 경로
그대로라 UI 전체에 일괄 반영.

## 크기
`dist/wuxia.html` **1422KB** · 에셋 104개

---

# v2.8 — 구역별 분위기 연출 (2026-09-11)

"구경하는 게 코어"인데 구역이 바닥색만 달랐다. 시트 없이 절차 연출로:

- 죽림 흩날리는 댓잎 · 폐촌 잿빛 낙엽 · 동굴 어둑함(부드러운 비네트)
  + 깜빡이는 반딧불 · 설산 내리는 눈 · 천산 흐르는 구름 그림자
- **무상태 입자** — 해시(i)+시간으로 좌표를 만들어 저장할 상태가 없다.
  화면 좌표라 카메라와 무관, 눈·낙엽은 인물 앞층, 구름·어둑함은 땅층
- 숫자는 AMB(00-data). fxtest에 5구역 렌더 스모크 추가
- 몬스터 시트 생성 프롬프트 문서(docs/프롬프트-몬스터.md) 별도 커밋 —
  구역별 2몹+보스, 스킬형 포함 (사용자가 제미나이로 생성 예정)

## 크기
`dist/wuxia.html` **1425KB** · 에셋 104개

---

# v2.9 — 폐촌 낭인 (2026-09-11)

사용자 제미나이 시트 1호가 게임에 들어갔다. 채팅에 붙인 그림도
대화 기록에서 원본을 복원해 쓸 수 있게 됐다(파일 첨부 불필요).

- **낭인** — 대기4·이동8·베기3·던지기2·피격1·죽음2 (몸높이 48, 52×51).
  근접 베기 기본 + 거리가 뜨면(80~200px) 술병 투척(6.5초 쿨)
- **근접형+투척 혼합 AI 틀** (FOES.throwCd/throwDur/throwAt/throwMin/Max)
  — 그림 탄(shotImg, 빙글 돌며 난다) + 명중 먼지(shotDust, imgburst fx).
  다음 몬스터(수호무사 검기 등)도 이 틀을 쓴다
- 폐촌 등장표: 낭인 3 : 강도 1 : 유령불 1
- ronintest.js 신설(에셋 존재·근접·투척·명중 먼지) — check에 포함
- 추출: 덩어리 라벨링 + 마젠타 성분 제거(보랏빛 외곽 교정), 검토판
  확인 후 반영(규칙 0)

## 크기
`dist/wuxia.html` **1531KB** · 에셋 128개

---

# v2.9.1 — 낭인 방향 교정 (2026-09-11)

"공격 방향을 보면서 때려야지, 반전해서 쓰면 될 듯"(사용자). 시트의
atk1(휘두르기)·cast1(던지기) 두 컷이 오른쪽을 보고 있었다 — 좌우 반전.

- 반전 재굽기 과정에서 atk0의 치켜든 칼끝이 51px 캔버스에 잘려 있던 것도
  발견(spritetest 가장자리 접촉) — 캔버스를 52×55로 키우고 bh:48로
  몸높이를 따로 선언(체력바·그림자 위치 유지)
- 검증: spritetest 접촉 해소, ronintest 통과, 실기에서 오른쪽에 선 낭인이
  왼쪽(주인공)을 보며 베고 던지는 것 확인

## 크기
`dist/wuxia.html` **1531KB** · 에셋 128개

---

# v2.10 — 폐촌 들개 (2026-09-11)

사용자 시트 2호. 이번 시트는 전 컷이 오른쪽을 봐서 추출 때 통째로 반전
(낭인 교훈 반영 — 이제 방향을 추출 단계에서 검사한다).

- **들개** — 대기4·이동4·물기4(웅크림→도약→물기→반동)·피격·죽음2.
  몸높이 34(짐승), 캔버스 78×42(물기 컷의 먼지·섬광 포함) — sw58·bh34
- 빠른 근접형: hp 0.68 · dmg 0.9 · spd 1.5 (표범보다 조금 빠르게 붙는다)
- 폐촌 등장표: 낭인 2 : 들개 2 : 강도 1 : 유령불 1
- ronintest 확장(들개 에셋·등장표) + 병 명중 검사의 레이스 제거
  (주인공이 딴 몹을 쫓아가 거리·명중이 흔들리던 것 — 5회 연속 통과)

## 크기
`dist/wuxia.html` **1644KB** · 에셋 143개

---

# v2.10.1 — 몬스터별 공격 사거리·낭인 방향 완치 (2026-09-11)

"공격 범위를 위험도별로, 보스는 모든 범위" + "낭인이 왼쪽을 공격할 때
오른쪽으로 칼질"(사용자).

- **근접 사거리 위험도별** (FOES.range): 유령불 38(바짝) < 강도 44 <
  들개 52 < 표범 60 < 낭인 64(장검). **보스 999 — 모든 범위**(BOSS.range,
  어디서든 공격이 닿는다). 적들이 서로 다른 반경에 포진해 화면에 층이
  생기고 주인공도 더 움직인다. 실측: 낭인 62~64 · 유령불 46에서 정지
- **낭인 방향 완치** — 시트가 컷마다 방향이 섞여 있었다(걷기·atk1·cast1은
  오른쪽-자연, 나머지는 왼쪽-자연). 걷기(엔진 기준)에 맞춰 atk0·atk2·
  cast0·hit·죽음 2컷을 반전, v2.9.1의 잘못된 반전은 원복. 좌/우 양측
  실기 검증 — 이제 어느 쪽에서든 주인공 쪽으로 벤다
- sim 60분: 곡선 동일(1h 동굴9·쓰러짐 0) — 사거리 차등의 밸런스 영향 없음

## 크기
`dist/wuxia.html` **1644KB** · 에셋 143개

---

# v2.11 — 폐촌 보스 원혼·들개 방향 원복 (2026-09-11)

사용자 시트 3호(폐촌 보스). 죽림에 이어 두 번째 전용 보스.

- **폐촌의 원혼** — 백발을 늘어뜨리고 기어 다니는 귀신 보스.
  대기4·이동4·공격3(기 모으기→발톱 궤적→낮은 휘두르기)·스킬5·피격·
  죽음3(웅크림→무너짐→스러짐). 발톱 궤적이 앞으로 길어 캔버스 190×96,
  몸은 sw130·bh88. ZONEBOSS.village='ghost', 구역 보스명도 '폐촌의
  원혼'으로 통일(등장 문구와 같음)
- **탄 보스의 틀** — FOES에 shotImg가 있는 보스는 스킬(BOSSKILL)이
  범위 폭발 대신 **거리 불문 해골 귀화 탄**이 된다. 위력은 그대로
  보스 스킬 배수(×2.6). fly 탄: 빙글 돌지 않고 나는 방향을 보며
  (꼬리가 뒤로), 명중하면 초록 폭발+진동
- **들개 방향 원복** — v2.10에서 전 프레임을 왼쪽으로 뒤집었던 것이
  거꾸로였다(엔진 기본 방향은 오른쪽 — dir<0일 때만 반전). 전 프레임
  재반전, 좌/우 실기 검증. 렌더러 규약에 기본 방향 문서화
- ronintest → 폐촌 종합 검증으로 확장: 원혼 에셋·보스 지정·해골 귀화
  발사와 명중까지. bosstest 폐촌 보스 23초 격파 · sim 곡선 동일

## 크기
`dist/wuxia.html` **2158KB** · 에셋 157개

---

# v2.12 — 동굴 독충 (2026-09-11)

사용자 시트 4호. 동굴 전용 몹 1호.

- **동굴 독충** — 검자줏빛 갑각의 지네·딱정벌레 잡종. 대기4·이동4·
  물기4(몸 세움→내리찍기→독액→갈무리)·피격·죽음3(뒤틀림→무너짐→부서진
  갑각). 몸높이 36, 캔버스 90×47 — sw70·bh36
- 단단한 탱커형: hp 1.25 · dmg 1.0 · spd 0.75 · range 46 — 느리게
  붙어서 질기게 버틴다. 동굴 등장표: 독충 2 : 강도 1 : 유령불 1
- **자주색 갑각 보존** — 분홍 걷기를 가장자리 2px(투명 이웃)에만 적용.
  전면 적용하면 보라 무늬가 죽는다 (첫 판에서 확인, 재추출)
- 프롬프트 문서 facing LEFT → **RIGHT** 전면 교체 (엔진 기본 방향 확정)
- sim 70분: 1시간 동굴 7 (독충이 질겨 반 단계쯤 느려짐 — 의도한 질감)

## 크기
`dist/wuxia.html` **2288KB** · 에셋 173개

---

# v2.13 — 독충 독기 교체판·동굴 보스 석암거인 (2026-09-11)

사용자 시트 5호(독충 재생성)·6호·7호(석암거인 2판 — 2판 채택).

- **독충 교체판** — 등껍질에 독기 발광 무늬, 공격 3컷째가 독무 분사로
  화려해졌다. 넝쿨이 칸 경계를 넘는 시트라 **경계에 붙은 작은 침입
  조각만 걷어내는** 규칙 추가 (라벨링으론 전체가 한 덩어리였다).
  캔버스 84×45 · sw66·bh36
- **석암거인** — 동굴 보스. 이끼 낀 바위 거인, 가슴에 호박색 핵.
  대기4·이동4·공격4(치켜들기→내리찍기→먼지→갈무리)·피격·죽음3
  (금 감→엎어짐→스러짐). 스킬은 내리찍기 재활용 + 기본 범위 폭발
  (BOSSKILL — 탄 없음, 원혼과 다른 근접형 문법). 캔버스 180×96 ·
  sw99·bh92. ZONEBOSS.cave, 구역 보스명 '석암거인'
- 골렘 팔레트엔 보라가 없어 분홍 전면 제거, 독충은 가장자리 한정 유지
- 1판(불규칙 격자·idle 5컷)은 2판(정규 4×4·죽음 3컷)으로 대체
- bosstest 동굴 보스 57초 격파 · sim 곡선 동일(1h 동굴7)

## 크기
`dist/wuxia.html` **2754KB** · 에셋 189개

---

# v2.14 — 설산 설랑 (2026-09-11)

사용자 시트 8호. 설산 전용 몹 1호.

- **설산 설랑** — 서리 맺힌 흰 늑대, 입김이 얼어 있다. 대기4·이동4·
  물기4(빙주 웅크림→도약 설참→낮은 돌진→갈무리)·피격·죽음2.
  몸높이 38, 캔버스 92×47(공격 컷 빙기 포함) — sw63·bh38
- 빠른 사냥꾼형: hp 0.9 · dmg 1.15 · spd 1.45 · range 56 — 들개보다
  아프고 표범만큼 빠르다. 설산 등장표: 설랑 2 : 강도 1 : 유령불 1
- 흰·옅은 청 팔레트라 분홍 전면 제거 (골렘과 같은 판단)
- sim 130분: 2시간 설산 7 — 곡선 유지(2h 설산 목표대로)

## 크기
`dist/wuxia.html` **2846KB** · 에셋 204개

---

# v2.15 — 초식 동시 시전 금지·스킬창 (2026-09-11)

"스킬은 동시 시전하지 않을 것, 스킬창을 두어 쿨타임이 도는지 볼 것"(사용자).

- **동시 시전 금지** — stepArts가 쿨 찬 초식을 같은 프레임에 전부
  펼치던 것(시전 동작이 서로 덮어써 그림도 겹쳤다)을 직렬화:
  시전 중(P.castT)이거나 숨 고르기(P.castGapT) 중엔 기다린다.
  시전이 끝나면 CASTQ.gap(0.35초) 쉬고 다음 초식이 나간다.
  **쿨다운은 기다리는 동안에도 돈다** — 잃는 건 타이밍뿐이라
  sim 곡선 동일(5분 죽림6 · 30분 폐촌10 · 1h 동굴7)
- **스킬창** (#sbar, 우하단·탭바 위) — 익힌 초식이 슬롯으로 늘어선다:
  문파색 테두리 · 이름 두 글자 · 쿨다운은 어두운 덮개가 위에서부터
  걷히고 남은 초가 아래 숫자로. 준비되면 슬롯이 빛난다.
  건곤이형도 표시(피격 발동이라 쿨만 보임). 익힌 목록이 바뀔 때만
  DOM을 다시 만든다(수련 패널과 같은 시그니처 방식)
- artstest +5: 한 프레임 하나만 · 대기 중 쿨 유지 · 순차 시전 ·
  슬롯 수 일치 · 덮개 표시

## 크기
`dist/wuxia.html` **2849KB** · 에셋 204개

---

# v2.16 — 설산 빙백령 (2026-09-11)

사용자 시트 9호. 설산 원거리 몹.

- **빙백령** — 반투명 얼음으로 빚은 여인, 떠다닌다. 대기8(부유)·이동4
  (활공)·시전5(모으기→응축→내뻗기→갈무리)·피격·죽음3(금 감→흩어짐→
  얼음 무더기). 몸높이 46, 캔버스 58×63 — sw22·bh46.
  시트가 8×4 격자로 와서(요청은 5줄) 컷을 골라 썼다 — 소매 설참 컷은
  원거리형이라 미사용
- **원거리 + 그림 탄**: 주술사 계열(ranged)이지만 절차 구체 대신
  **얼음 조각 그림 탄**(spirit_shot 35×12)을 쏜다 — shotFly 필드 추가:
  보스가 아니어도 fly 탄(돌지 않고 나는 방향)을 쓸 수 있다
- hp 0.58 · dmg 0.9 · spd 0.9 · range 104. 설산 등장표
  설랑2 : 빙백령2 : 강도1 : 유령불1 (폐촌과 같은 2:2:1:1)
- 눈송이가 마젠타에 물들어(얇은 것 사례) 2차 분홍 걷기(pink>8)
- **snowtest.js 신설** + npm check 편입 (설랑 물기·빙백령 발사·명중 —
  회복이 피해를 메꿔서 hurtHero 계수로 검증)
- sim 130분: 2시간 설산 5 — 원거리 몹이 섞여 반 단계 느려짐(의도 범위)

## 크기
`dist/wuxia.html` **2971KB** · 에셋 226개

---

# v2.17 — 설산 보스 설산백호 · 보스 탄 동결 버그 수정 (2026-09-11)

사용자 시트 10호. 설산이 완성됐다(설랑·빙백령·설산백호).

- **설산백호** — 서리 갑주가 돋은 백호 보스. 대기4·이동4·설참3
  (치켜들기→발톱 설참→갈무리)·스킬5(일어서 포효 2컷→눈보라 숨결)·
  피격·죽음2. 몸높이 80, 캔버스 226×94 — sw159·bh80.
  스킬은 원혼과 같은 탄 보스 틀 — 냉기 원뿔 탄(tiger_shot 75×70,
  shotSpd 250). ZONEBOSS.snow, 보스명·등장 문구 '설산백호' 통일
- 시트 규격 불일치 컷 2장 제외: 웅크림(atk2 — 절반 크기로 그려짐),
  일어서기 3컷째(sk2 — 칸 경계에서 잘린 냉기 조각이 붙어 있음).
  줄마다 칸 폭이 달라 **키 큰 열 구간 중심**으로 몸을 정렬(문턱 0.62 —
  낮게 깔린 숨결 원뿔이 중심을 끌지 않게)
- **보스 단계 탄 동결 버그 수정** — step()의 isBoss 분기가 탄 루프보다
  먼저 return 해서 보스 단계에선 모든 탄이 허공에 얼어 있었다
  (원혼 귀화도 실전에서 안 날아갔다). 탄 루프를 분기 앞으로 이동.
  기존 검증이 가짜 통과였던 원인: 체력 감소가 보스 전천후 근접타와
  구분이 안 됐다 → ronintest·snowtest 모두 **fly 탄 폭발(burst) 계수**로
  교체 (탄이 언 채면 0회)
- bosstest 설산 보스 113초 격파 (곡선 유지)

## 크기
`dist/wuxia.html` **3453KB** · 에셋 244개

---

# v2.18 — 양손 정권 복원 · 제패 중 방향 고정 (2026-09-11)

"공격 모션이 양손이었는데 뭐야" + "스테이지가 끝나면 이쪽 저쪽 바라봄"
(사용자).

- **양손 정권 복원** — 맨손 정권 4컷은 원래 왼손 잽→오른손 지르기
  양손 콤보다. 절정부터 katk 스트립(한 팔 정권 8컷)으로 통째 교체하던
  것이 원인 — 두 절반이 같은 팔이라 "양손 교대"가 안 보였다.
  이제 **맨손 콤보를 그대로 두고 주먹 끝에 권기 빛무리**(HFX.kfist,
  동심원 3겹 가산 합성·맥동)를 얹는다. FIST 좌표가 콤보를 따라가므로
  빛이 왼손·오른손을 번갈아 탄다. katk 스트립은 raw/로 보관,
  P.atkAlt 제거
- **제패 연출 중 방향 고정** — 제패(sweepT) 중에도 주인공 이동·공격
  로직이 돌아서, 사방으로 밀려나는 적을 번갈아 조준하며 P.dir이
  매 프레임 뒤집혔다. 제패 중엔 쫓지도 치지도 않는다
- fxtest 개편: katk 검사 → 맨손 유지+빛무리 유무(경지별), 제패 중
  60프레임 방향 뒤집힘 0회 검사 추가 (ctx.arc 계수)

## 크기
`dist/wuxia.html` **3412KB** · 에셋 243개

---

# v2.19 — 독충 정밀판 교체 (2026-09-11)

사용자 시트 11호. 독충 3판째 — 이번 판을 최종 채택.

- 훨씬 정밀한 렌더링: 등껍질 발광 무늬가 살아 있고, 공격이
  독기 소용돌이 모으기 → 독액 분사 → **큰 독운 폭발(자주+초록)** →
  연기 갈무리 4컷으로 화려해졌다. 죽음 3컷 + 시체 독기·반짝임
- **자주색 독운 보존이 관건** — 마젠타 마스크를 엄격하게
  (R>150 & B>150 & **G<70**): 배경 마젠타는 G≈0이고 독운 자주는
  G가 높다. 분홍 걷기도 가장자리 2px 한정(문턱 60) — 전면 걷기면
  독운이 죽는다
- 캔버스 88×45 · sw69·bh36 (규격만 갱신, 스탯·동작 동일)

## 크기
`dist/wuxia.html` **3419KB** · 에셋 243개

---

# v2.20 — 천산수리 (2026-09-11)

사용자 시트 12호. 천산 전용 몹 1호.

- **천산수리** — 금갈색 산독수리, 늘 떠 있다. 부유4·활공4·급강하4
  (제동→날개 접고 강하→발톱→복귀)·피격·죽음2(추락→떨어짐).
  몸높이 46, 캔버스 106×67 — sw94·bh46
- **비행 표현**: 캔버스에 띄움 12px을 구워 바닥 기준 렌더러에서도
  그림자 위로 몸이 뜬다 (프레임별 좌표 규약 변경 없음)
- **줄별 방향 혼합** — 대기·활공 줄만 왼쪽 보기였다(공격·피격·죽음은
  오른쪽). 거울쌍 검사(반전 유사도)로 줄 안 일관성을 확인한 뒤
  실기 좌/우 배치로 판별, 8컷만 반전
- 급강하 사냥꾼형: hp 0.8 · dmg 1.2 · spd 1.6 · range 58.
  천산 등장표: 수리 2 : 강도 1 : 유령불 1
- **heaventest.js 신설** + npm check 편입

## 크기
`dist/wuxia.html` **3556KB** · 에셋 258개

---

# v2.21 — 천산 수호무사 (2026-09-11)

사용자 시트 13호. 천산 정예 몹.

- **수호무사** — 백금 도포·상투의 정예 검객. 대기8(발검 변형 포함)·
  이동8·검격4(치켜들기→회전 베기→장거리 찌르기→갈무리)·
  시전3(납도→검기 모으기→발도)·피격·죽음2. 몸높이 48(인간형),
  찌르기 검이 길어 캔버스 194×55 — sw30·bh48
- **근접+투척 혼합** (낭인 틀): 거리가 뜨면(90~240px) 발도로
  **초승달 검기**(guard_shot 29×33, fly 탄·shotSpd 240)를 날린다.
  시트의 초승달이 볼록면 왼쪽이라 오른쪽 비행 기준으로 반전
- 정예 스탯: hp 1.7 · dmg 1.3 · spd 0.95 · range 66.
  천산 등장표: 수리2 : 무사2 : 강도1 : 유령불1
- 미사용 컷: 장거리 찌르기 2판(thrust2)·발검 자세(stance2)
- heaventest 확장 (무사 에셋·초승달 발사·fly 폭발 명중)

## 크기
`dist/wuxia.html` **3734KB** · 에셋 285개

---

# v2.22 — 수호무사 돌격 (2026-09-11)

"수호무사는 돌격형 공격이 있어도 좋겠네, 좀 멀 때"(사용자).

- **돌격** (FOES.dashCd 계열 — 세 번째 행동 틀): 중거리(70~170px)에서
  쿨이 차면 미사용이던 장거리 찌르기 컷(guard_dash)으로 자세를 잡고
  **330px/s로 미끄러져 들어와** 사거리 안에 닿는 순간 1.4배 피해로
  꿰뚫는다. 돌격 5.5초 쿨, 끝나면 일반 베기 쿨로 이어진다
- 투척(초승달 검기)은 더 먼 거리(180~270)로 밀어 역할을 갈랐다:
  근접 베기 → 중거리 돌격 → 원거리 검기
- heaventest 확장: 돌격 명중·실제 접근 검사 (+ 투척 검사를 투척
  구간으로 이전)

## 크기
`dist/wuxia.html` **3743KB** · 에셋 286개

---

# v2.23 — 양손 권기 정권 복원 (2026-09-11)

"여전히 한손 정권" + hero_fx 시트 재전달(사용자 — 두 정권 줄이
오른손·왼손이라는 확인).

- v2.18에서 없앤 스트립 교대를 복원하되, 이번엔 **2000px 고해상 원본**
  에서 두 줄을 각각 4컷 스트립으로 재컷 (katka 오른손 · katkb 왼손,
  프레임 70×51 · 몸높이 48). 공격마다 P.atkAlt로 교대 —
  왼손은 가슴 앞 구슬 와인드업, 오른손은 어깨선 지르기라 이제
  게임 크기에서도 두 판이 구분된다
- v2.18의 주먹 빛무리(HFX.kfist)는 폐기 — 스트립에 권기가 구워져 있다.
  절정 미만은 맨손 스트립 + 주먹 끝 흰 점 유지
- fxtest 교체(오른손/왼손 교대·경지 분기), spritetest에 두 스트립 규격
  검사 추가. lint가 주석의 "이름(...)"을 호출로 오인하는 것도 확인

## 크기
`dist/wuxia.html` **3797KB** · 에셋 288개

---

# v2.24 — 석굴 박쥐 · 죽림 확장 프롬프트 7종 (2026-09-11)

사용자 시트 14호(박쥐) + 카탈로그 시트(raw/catalog-bamboo.png).

- **석굴 박쥐** — 노란 눈 동굴 박쥐, 늘 떠 있다(띄움 10px). 부유4·
  비행4·급습 물기4·시전3(절규 — 음파 고리 발사)·피격·죽음2.
  캔버스 132×66 — sw90·bh36. 근접+투척 틀: 거리 80~210이면
  **음파 고리 탄**(bat_shot 87×51, fly)을 쏜다. hp 0.55 물몸 견제형.
  동굴 등장표 독충2 : 박쥐2 : 강도1 : 유령불1 — **동굴 완성**
- **cavetest.js 신설** + check 편입 (독충 물기·박쥐 고리 발사·fly 폭발)
- **죽림 티어 확장 준비** — 카탈로그 시트에서 미보유 7종의 대표 컷
  (ref1~7)과 규격 프롬프트를 정리 (엘리트 정령병·추적자, 레어 방울뱀,
  일반 무뢰배·파수꾼·등딱지벌레·말벌). 카탈로그 자체는 컷이 작고
  라벨이 박혀 있어 추출 금지 — 참고 그림 + 재생성용
- 이제 몬스터 잔여는 천산 최종 보스 뇌운신장 하나
- sim 60분: 1시간 동굴 8 — 곡선 유지

## 크기
`dist/wuxia.html` **3984KB** · 에셋 308개

---

# v2.25 — 죽림 티어 확장 3종 (2026-09-11)

사용자 시트 15~17호 (카탈로그 대표 컷 기반 재생성 1~3번).

- **대나무 정령병 (엘리트)** — 살아 있는 대나무 병사, 쌍죽검.
  대기6·이동8·검격6(십자 섬광 포함)·피격2·죽음3(**마디가 흩어져
  대나무 무더기**). 캔버스 122×54 · sw38·bh48. hp 1.6 · dmg 1.2
- **죽림 추적자 (엘리트)** — 붉은 눈 넝마 추적자, 장창 + **회전 잎날
  투척**(낭인 틀, 빙글 도는 탄 — stalker_shot 37×38). 대기4·이동6·
  창격4·시전2·피격·죽음2. 캔버스 72×58 · sw34·bh48.
  hp 1.4 · spd 1.15 · range 60. 장막 컷 2장은 은신 연출 후보로 보관
- **대나무 방울뱀 (레어)** — 옥빛 방울뱀, 또아리에서 튀어나와 문다.
  독니 dmg 1.35. 캔버스 86×45 · sw56·bh40. 이 시트는 테두리가
  어두운 청회색이라 슬레이트 배경 마스크를 추가
- 죽림 등장표 12칸: 기존 9 + 정령병·추적자·방울뱀 각 1 (엘리트가
  가끔 섞이는 리듬). sim 5분 죽림6 — 초반 곡선 유지
- **bambootest.js 신설** + check 편입 (3종 에셋·근접·잎날 투척/명중)

## 크기
`dist/wuxia.html` **4398KB** · 에셋 371개

---

# v2.26 — 대나무 무뢰배 (2026-09-11)

사용자 시트 18호 (카탈로그 재생성 4번).

- **대나무 무뢰배** — 몽둥이 무뢰배. 대기4·이동4·내리찍기4·피격·
  죽음3(무릎→엎어짐→반짝). 캔버스 80×56 · sw44·bh48.
  hp 1.1 · dmg 1.1 · range 48 — 강도의 한 급 위 일반 몹
- 죽림 등장표: 강도 2칸 중 1칸을 무뢰배로 교대 (총 12칸 유지)
- bambootest에 무뢰배 편입. sim 5분 죽림6 — 곡선 유지

## 크기
`dist/wuxia.html` **4520KB** · 에셋 387개

---

# v2.27 — 등딱지벌레·말벌·옥갑충 (2026-09-11)

사용자 시트 19~21호 (카탈로그 재생성 5~6번 + 천산판 명명).

- **등딱지벌레** (죽림) — 갈색 뿔 딱정벌레 탱커. hp 1.25 · spd 0.8.
  캔버스 76×56 · sw58·bh34, 죽음 3컷
- **대나무 말벌** (죽림·비행) — 띄움 10px, 침 박기. hp 0.5 · spd 1.6
  물몸 견제. 캔버스 84×54 · sw44·bh30. 옆 칸에서 넘어온 침 조각 제거
- **옥갑충(玉甲蟲)** (천산 — 명명은 위임받아 결정) — 옥·금 갑주
  딱정벌레, 등에 武 자, 뿔에 기를 모아 소용돌이 박치기. hp 1.5.
  천산 등장표의 강도 재탕을 대체. 시트의 한자 라벨(損·死) 제거,
  자주 불꽃 죽음 컷은 그림 자체가 마젠타라 시퀀스에서 제외
- **테두리 잔재 처치법 추가**: 이 배치 시트들은 테두리가 어두운
  자주(마왕 사례)라 경사 픽셀이 유령 사각으로 남았다 — 색 마스크
  (G 낮은 탁한 자주) + 리사이즈 뒤 알파 바닥(80 미만 제거)으로 해결
- 죽림 등장표 13칸(말벌이 유령불 1칸 교대), 천산 6칸.
  bambootest 6종·heaventest 옥갑충 확장. sim 5분 죽림6 유지

## 크기
`dist/wuxia.html` **4741KB** · 에셋 441개

---

# v2.28 — 은자 오브젝트 보관·은화 아이콘 교체 (2026-09-11)

사용자 시트 22호 (은자 오브젝트 모음).

- **raw/objects/ 신설** — 은화·동전 더미·은괴·보물상자·장신구·트로피 등
  52종을 덩어리 라벨링으로 낱개 재단해 보관 (빌드엔 안 들어감 —
  assets/는 전부 dist에 박히므로, 쓸 때 골라 옮긴다). 동전 회전
  프레임(정면→기울기→모서리)도 있어 드랍 연출 후보
- **은자 아이콘 교체** — 손그림 엽전(12×12) → 시트의 민짜 은화
  (silver02, 24×24로 리샘플). HUD·수련·무공·기연 카드 전부 공통
  (coin() 헬퍼라 한 곳 교체). 엽전은 raw/silver_yeopjeon.png 보관

## 크기
`dist/wuxia.html` **4741KB** · 에셋 441개

---

# v2.29 — 옆모습 대치 (2026-09-11)

스프라이트가 죄다 옆모습인데 전투는 전방향이라 안 어울린다는 사용자
고민 → 3안(등장 편향 + 보스 수평 대치) 확정. "위아래가 비어 보이지
않을까"라는 걱정은 **고정이 아니라 편향**으로 푼다.

- **잡몹 등장 타원** — 등장원의 세로 성분을 0.45로 눌렀다(SPAWN.flat).
  좌우가 주 방향이 되고, 위아래에서도 오되 사선으로 온다 — 세로 화면
  위아래가 비지 않는다. 반경 190~300도 SPAWN으로 옮김(숫자 규칙)
- **보스 옆 등장** — 소환문이 주인공 위(-118)가 아니라 **옆 ±150,
  같은 땅 높이**에 선다(SUMMON.side·gate). 보스는 사거리 999라 안
  걸으므로 소환점이 곧 대치 구도다 — 옆모습끼리 마주 보는 보스전.
  소환·등장 카메라는 주인공과 문 사이를 비춰 시점이 안 튄다
- **높이 맞추기** — 주인공이 위아래로 돌면 보스가 높이만 슬며시 맞춰
  나란히 선다(BOSS.alignY 26 · alignSpd 0.6)
- **몸 가장자리 거리** — 같은 높이 대치에서 주인공이 보스 중심까지
  파고들어 스프라이트에 파묻혔다 → 보스는 거리를 몸 폭 절반(sw×
  BOSS.edge 0.5)만큼 가장자리에서 잰다(foeRad — 접근·공격 시작·정권
  판정 공통). 잡몹은 그대로
- bosstest 격파 시간 변화 없음(23초~), sim 30분 동굴1 유지, 전 스위트
  10/10

## 크기
`dist/wuxia.html` **4790KB** · 에셋 441개

---

# v2.30 — 검은 테두리 세정·보스 밀착·엽전 은자 (2026-09-11)

사용자 제보 두 건("케릭터랑 새로 추가된 몬스터들 테두리가 검은 현상,
보스몹을 공격안함") + 엽전 아이콘 요청.

- **검은 테두리 세정** — katka·katkb(주인공 정권 두 판)와 방울뱀·추적자
  전 프레임에 시트 칸 테두리 잔재가 남아 있었다: 어두운 자주 줄
  (katka 4~7·48~50행 전폭, 방울뱀은 사각 상자, 추적자는 발밑 줄) +
  옅은 칸 배경 워시. 색만으로 지우면 주인공 머리카락(어두운 자주갈색)이
  같이 먹혀서, **가장자리 8px 안의 줄(그 줄의 55% 이상이 자주) +
  최외곽 3px 띠 + 알파 60 이하 어두운 워시**만 정밀 제거. 머리·옷·
  잎날 무사, 38장 세정
- **spritetest 줄 잔재 검사 추가** — 가장자리 8px 안에서 한 줄의 85%가
  채워졌는데 안쪽 3줄에서 절반 밑으로 뚝 끊기면 잔재로 판정(누운 시체·
  붕산장 기운처럼 몸이 넓은 그림은 완만히 이어져 안 걸린다). katka류는
  noEdge·바닥 허용이라 기존 검사를 다 통과했었다
- **보스 밀착** — BOSS.edge 0.5는 주먹이 보스 몸에 안 닿아 허공질로
  보였다("공격 안 함") → 0.35로: 주먹 끝이 몸 가장자리에 닿는다.
  heroAttack의 거리도 조준·판정과 같은 눌린 척도(atkFlat)로 통일 —
  위아래로 어긋났을 때 조준만 되고 공격이 안 나가는 틈 제거
- **엽전 은자** — 은화 가운데 6×6 네모 구멍(사용자 요청). 좌·상 둘레는
  그늘, 우·하 둘레는 밝게 — 파인 면 표현. HUD·패널 공통(coin())
- bosstest 격파 시간 동일, sim 30분 동굴1 유지, 전 스위트 10/10

## 크기
`dist/wuxia.html` **4804KB** · 에셋 441개

---

# v2.31 — 이펙트 강화 1차·맨손 양손 정권 (2026-09-11)

사용자 제보·요청 두 건: "이런 화려한 이펙트 가능해? 지금은 조잡하다"
(상용 방치형 스크린샷 4장) + "공격 모션이 또 주먹 한 개로 돌아감".

- **맨손 양손 정권** — 주먹 한 개의 정체: 절정(경지12) 미만은 옛 35px
  한손 스트립이었다(교대는 절정+ 전용). 권기 스트립(katka·katkb)에서
  권기 발광만 걷어내 맨손 오른손·왼손 판(atka·atkb)을 만들고, **전
  경지에서 교대**하게 했다. 절정부터는 권기 판으로 바뀌는 건 그대로.
  주먹 끝 흰 점(FIST)은 은퇴 — 양손 판엔 주먹이 그림에 다 있다.
  권기 제거는 색 마스크 + 주변 잔광 + 고립점 덩어리 제거 3단.
- **이펙트 팩(FXD)** — 전부 가산 합성(lighter), 그라디언트 없이
  동심원·픽셀 파편이라 jsdom 무해. 상태 없이 시간으로 위치를 만든다:
  · wave 충격파 고리(밝은 심+넓은 여운) · rays 방사 속도선
  · sparks 포물선 파편 · flash 화면 섬광 · burst는 심광+파편으로 강화
  · ring(광역 초식)은 가산+무공 색 이중 고리
  fxBlast() 묶음 헬퍼, fxPush() 상한(FXD.max 200)
- **연출이 붙은 순간들** — 평타 임팩트(작게)·치명타(금빛 대파열+속도선)·
  광역 초식(무공 색 섬광)·파공권/암향지 탄 명중 파열·보스 등장 파열·
  보스 스킬 충격파. **보스 스킬 'shock'은 렌더러가 아예 없어 여태 안
  보였다** — 조잡함의 숨은 원인
- fxtest: 파열 4종 생성 + 맨손 양손 교대 검사. spritetest: atka·atkb.
  bosstest·sim 변화 없음, 전 스위트 10/10

## 크기
`dist/wuxia.html` **4861KB** · 에셋 443개

---

# v2.31.1 — 보스전 구도 고정 (2026-09-11)

사용자 확정: "캐릭터가 중앙이 아니라 조금 왼편, 보스는 오른편".

- 소환문 방향의 랜덤(±150)을 없애고 **오른쪽 +150 고정**. 보스전
  카메라(주인공:보스 = 2:1)가 자연히 주인공을 중앙 조금 왼편,
  보스를 오른편에 잡는다. 주인공은 왼쪽에서 접근하므로 대치 내내
  구도가 유지된다
- bosstest·전 스위트 변화 없음

## 크기
`dist/wuxia.html` **4861KB** · 에셋 443개

---

# v2.32 — 천산 최종 보스 뇌운신장 (2026-09-11)

사용자 제미나이 시트 (텍스트 프롬프트 기반, 대표 이미지 없음).

- **뇌운신장(雷雲神將)** — 먹구름 몸에 금 갑주를 두른 뇌신 장수. 천산
  ZONEBOSS. 이로써 **5구역 보스 전부 완성**(마왕·원혼·석암거인·백호·뇌운신장)
- 기본 공격 = 번개 주먹 스매시 4컷(시트가 왼쪽을 봐서 반전), 스킬 =
  두 팔 들어 갈래 번개 탄 투척(원혼·백호와 같은 shotImg 탄 보스 틀,
  thunder_shot·shotSpd 280). 캔버스 156×126 · 몸 sw94·bh92
- 시트 하단 시전·죽음 행은 그림 배율이 달라 상체 행(92/103)과 별도로
  92/139 정규화. 병합 칸 이음매(구름이 옆칸으로 이어짐)는 얇은 골 접기 +
  가로 이음매 줄을 위 구름 색으로 메꿔 처리
- BOSSCRY.heaven "천산의 검객이 하강한다" → "뇌운이 하늘을 덮는다",
  ZONES 보스명 "천산 검객" → "뇌운신장"
- heaventest에 뇌운신장 에셋·번개 탄 폭발 검증 추가. bosstest 천산 최종
  보스전 397초(기존 백호 틀 유지), sim 곡선 변화 없음, 전 스위트 10/10

## 크기
`dist/wuxia.html` **5351KB** · 에셋 462개

---

# v2.32.1 — 뇌운신장 고해상 시트 교체 (2026-09-11)

사용자가 더 정교한 뇌운신장 시트를 다시 보내 교체.

- 새 시트는 프레임이 균일하고 마젠타 배경도 깨끗해 단일 배율(92/105)로
  정규화. 캔버스 196×98 · 몸 sw114·bh92 (이전 156×126에서 갱신)
- **방향이 칸마다 섞여 있었다**(낭인 사건 유형): idle·walk·atk0·atk2는
  왼쪽 봄, atk1·atk3·skill·hit·death는 오른쪽 봄. 프레임별로 반전을
  다르게 적용해 전부 오른쪽 봄으로 통일(적 규약)
- 번개 주먹 스매시·두 손 번개 모으기·갈래 번개 탄 모두 선명. 실기에서
  좌우 반전·스매시 확인
- heaventest·bosstest·전 스위트 그대로 통과(격파 시간 유지)

## 크기
`dist/wuxia.html` **5476KB** · 에셋 462개

---

# v2.33 — UI 정리 A: 스킬창·숫자·수련·무공 패널 (2026-09-11)

실기 피드백 12+2건 중 A묶음 (가장 거슬리는 것).

- **스킬창이 패널을 가리던 문제**(1) — #sbar·시험 버튼이 무공/수련/사냥터
  패널 위로 떠서 스탯 줄·구양신결 칸·동굴 21~30단계를 덮었다. 패널이
  열리면 둘을 숨긴다(opacity+pointer-events). 이걸로 "수련에 x10/x100/MAX가
  없다"(4)도 해소 — 배수 버튼은 있었는데 스킬창이 가리고 있었다
- **스킬창 재설계**(2,3) — 자동 시전인데 버튼처럼 보이던 것: 사각 버튼 →
  둥근 "기운 원판", 위에 "자동 시전" 라벨. 이름+초 숫자 겹침 → 쿨 도는
  동안엔 초 숫자만 크게, 다 차면 한자 한 글자가 밝아지며 문파색 테두리가
  빛난다(둘이 절대 안 겹친다)
- **숫자 만·억 축약**(5) — fmt() 헬퍼(10-engine): 1만 미만은 쉼표,
  이상은 만·억·조·경(240만·8.3억·7428만). HP·은자·피해 숫자·수련 비용·
  무공 비용·보스 격파·기연·오프라인 정산 전부 적용
- **무공 상세칸 위치**(14) — 그리드 위 → 아래로. 타일 누르면 상세칸으로
  부드럽게 스크롤해 바로 보인다(시선 왕복 제거)
- 전 스위트 10/10

## 크기
`dist/wuxia.html` **5478KB** · 에셋 462개

---

# v2.34 — UI 정리 B: 연출 흐름 (2026-09-11)

- **단계마다 3초 인트로 제거**(6) — 구역 이름+배경 3장+"N단계" 연출이
  50단계 내내 끊고 들어왔다. 이제 **구역이 바뀌거나 보스 단계일 때만**
  연출한다. 같은 구역 안 일반 단계 이동은 바로 진행(단계는 상단 표시로
  충분). enterStage(intro) 인자로 제어
- **기연 팝업 → 상단 배너**(7) — 자동 수락(5초)인데 화면 가운데를 막던
  것: 배경 어둠 없이 위에서 내려오는 띠로 흘려보낸다. 전투가 계속 보인다
- **빈 화면 감소**(8) — 연출 없이 진입할 땐 적 하나를 바로 스폰, 필드가
  비면(개체<2) 스폰 속도를 7/초로 올려 빨리 채운다(상한 st.max 유지)
- sim 30분 곡선 유지(죽림6·동굴1), 전 스위트 통과

## 크기
`dist/wuxia.html` **5479KB** · 에셋 462개

---

# v2.35 — UI 정리 C: 비주얼 (2026-09-11)

- **동굴·설산이 허옇고 밋밋**(9) — 근본 해결(지형 타일)은 시트 대기 중이라
  톤·비네트로 우선 개선: 동굴 바닥 #5c5f5c→#474d54(어둑·푸른끼), 어둠
  비네트 0.16→0.32로 가운데에 시선을 모은다. 설산 #b9c9d2→#a4b3c0
  (톤다운, 흰 백호 대비) + 옅은 한기 비네트. 반딧불·눈 입자도 조금 늘림
- **보스 이름이 너무 작다**(12) — 체력바 위 이름 7px→12px 굵게, 검은
  4방향 외곽 + 금빛. 체력바도 h5→6. 만난 무게감을 준다
- 나머지(10 네모 테두리·11 겹침·15 옷 묻힘)는 재현이 약하거나 근본이
  배경 대비/지형 타일이라, 이번 톤 조정으로 부분 완화. 지형 타일이
  들어오면 근본 해결 예정
- sim·bosstest·전 스위트 통과

## 크기
`dist/wuxia.html` **5480KB** · 에셋 462개

---

# v2.36 — 정권 옷 뚫림 수정 (2026-09-11)

사용자: "옷이 이상해" — 삼류(절정 미만) 공격 시 배 앞에 살색 덩어리가
떠 옷이 뚫린 듯 보였다.

- 원인: 정권 시트(hero_fx)는 권기가 그림에 구워진 "권기 정권". v2.31에서
  맨손 판(atka·atkb)을 만들려고 파란 권기를 지웠더니, 원래 권기 구체 안에
  있던 주먹이 배 앞 살덩이로 남았다(atka1·atka2)
- 해결: **전 경지에서 권기 판(katka·katkb)을 쓴다**. 맨손 판은 폐기
  (raw/hero_bare/ 보관). 양손 교대는 그대로, 옷은 온전. 경지 성장 표현은
  몸 뒤 경지 기운(절정부터)이 담당
- drawHero의 katkRealm 분기 제거, 70-main/spritetest/fxtest 정리
- 전 스위트 10/10

## 크기
`dist/wuxia.html` **5428KB** · 에셋 460개

---

# v2.37 — 조작 편의·이펙트 지속 (2026-09-11)

실기 피드백.

- **수련 x100·MAX가 "안 눌림"** — x10·x100이 "정확히 N개"라 배수를 다 못
  사면 버튼이 비활성이었다. 초·중반엔 100개를 못 채워 x100이 늘 죽어
  있었다. 이제 **살 수 있는 만큼(최대 배수)까지 사고 ≥1이면 활성**,
  실제 개수를 ×N으로 보여준다(MAX와 같은 규칙). x100도 초반부터 쓸모
- **무공 연마 꾹 누르기** — 수련처럼 연마 버튼을 꾹 누르면 연속 레벨업
  (0.14초 간격). 누르는 중엔 상세 칸 DOM을 안 갈아엎어 pointer가 안 끊긴다
  (artHold). "하나씩 누르기 불편" 해소
- **이펙트가 언젠가 사라지던 것** — 타격 숫자(dmg)가 fx 상한을 무시하고
  쌓여 상한(200)을 넘기면, 새 타격 파열이 영영 안 떴다. 이제 모든 fx가
  fxPush를 거치고, 상한(320) 초과 시 **가장 오래된 것부터 밀어낸다** —
  최신 이펙트는 항상 뜬다
- **평타 이펙트 색** — 노란끼(255,236,190) → 손끝 권기와 같은 옅은 청백
  (206,230,255). 예전 주먹 끝 흰 점 색감
- 전 스위트 10/10, sim 곡선 유지

## 크기
`dist/wuxia.html` **5430KB** · 에셋 460개

---

# v2.37.1 — 팝업 이후 버튼 먹통 수정 (2026-09-11)

사용자: "팝업 이후 버튼이 안 눌리는 느낌 — 수련도, 무공 선택도 안 됨".

- 원인: v2.34에서 기연 배너를 만들 때 `#fpanel .ocard`를 **항상**
  pointer-events:auto로 둬서, 팝업이 자동 수락으로 사라진 뒤에도(opacity:0)
  투명한 카드가 상단에 남아 그 아래 무공 타일·수련 버튼의 탭을 삼켰다
- 해결: `.show`일 때만 카드가 탭을 받도록 게이트(#fpanel.show .ocard).
  닫히면 완전 통과. 팝업 중 auto / 닫힘 후 none 확인, 타일 선택 복구
- 전 스위트 10/10

## 크기
`dist/wuxia.html` **5430KB** · 에셋 460개

---

# v2.38 — 공격 근접·헛손질 제거 (2026-09-11)

사용자: "멀리서 못 때리고 손만 허우적 — 설산 빙백령 같은 애. 더 근접하자".

- 원인: 이동을 멈추는 거리(atkRange×closeIn ≈ 18~32px)와 공격을 시작하는
  거리(atkRange+atkReach = 54px)가 달라, 바짝 붙기 전에 54px에서 정권을
  질렀다. 그 54는 판정 거리(44) 밖이라 헛쳤다 — 특히 원거리 적(빙백령)엔
  안 물러나도 멀리서 헛손질
- 해결: **공격 시작 거리 = 이동을 멈추는 거리(stopD)**로 통일 — 붙은 뒤에만
  친다. 원거리 적엔 18.5px, 근접엔 31.7px까지 파고들어 친다("더 근접")
- 판정 반경도 atkReach만큼 넉넉히 — 붙어서 친 뒤 적이 밀리거나 움직여도
  놓치지 않는다. 계측: 설산 헛손질 17%→0%
- sim 5분 죽림7·30분 동굴2 (헛손질 제거로 소폭 빨라짐, 건강 범위 유지),
  보스전·전 스위트 그대로 통과

## 크기
`dist/wuxia.html` **5431KB** · 에셋 460개

---

# v2.39 — 투사체 진행 방향 회전 (2026-09-11)

사용자: "모든 투사체는 날아오는 방향에 맞춰 돌려야지 — 얼음이 항상 같은
방향을 봐서 이상함".

- fly 탄(얼음 조각·해골 귀화·초승달 검기·번개·박쥐 음파)이 좌우 반전만
  하고 각도 회전이 없어 대각선으로 날아도 늘 수평을 봤다. 이제 **진행
  방향(atan2 vx,vy)으로 기울여** 난다. 왼쪽은 거울 뒤 반전각으로 돌려
  뒤집힘 방지(파공권 탄과 같은 방식). drawShots의 fly 분기
- 술병류(fly 아님)는 그대로 빙글빙글. 전 스위트 10/10

## 크기
`dist/wuxia.html` **5431KB** · 에셋 460개

---

# v2.40 — 무협 폰트·보스 등장 구도·문 페이드 (2026-09-11)

사용자 3건: 글씨가 작다+무협 폰트, 보스전이 등장 뒤 카메라가 튀며 주인공이
가운데로 감, 문이 등장 뒤 그대로 남음.

- **무협 폰트**(글씨 키움) — Gugi(붓글씨 제목체 --fw) + Do Hyeon(굵고 또렷한
  본문 --fb) 웹폰트. 제목·보스 이름·인트로·초식명은 Gugi, 본문·수치는
  Do Hyeon. 온라인 로드·오프라인 시스템 대체(단일 HTML 유지). HUD·패널
  글씨 전반 상향, 캔버스 텍스트도 Gugi
- **보스 등장 구도** — SUMMON.side 150→92. 등장 거리가 전투 대치 거리와
  거의 같아 주인공이 안 움직이고 등장 구도 그대로 싸운다(계측: 주인공
  이동 0~15px, 카메라 요동 제거). "왜 화면 변하며 주인공이 가운데로 가냐" 해소
- **문 페이드** — 보스가 걸어 나온 뒤 문이 SUMMON.fade(0.7초) 동안 아래로
  가라앉으며 옅어지고, 밑동을 타원 그림자가 삼켜 어둠에 스러진다
  (S.gateT·drawGateFade). source-atop 사각 자국 없이 알파로만
- sim·bosstest·전 스위트 10/10

## 크기
`dist/wuxia.html` **5433KB** · 에셋 460개

---

# v2.47 — 문파 무공도(스킬트리) 게임 이식 + 질주 비율 보정 (2026-09-11)

스킬트리 3종 작업을 끝냈다(에이전트 8기 병렬 활용).

- **7문파 트리 편입** — 에이전트가 초안 7개를 src/65b-treedata.js(TREEDATA,
  8문파 182노드)로 통합. eff 어휘 검증, id·need 정합, **교차 노드 7쌍 상대
  키스톤에 상호 연결**(강유상제=소림×무당 등). 통합 중 엔진 버그도 잡음
  (cross/crossNode가 eff 안에 있어 게이트가 무시되던 것 → 최상위로).
- **신규 심법 6개** ARTS.list 편입(항마진경·매향심결·백련심공·취권결·
  만독불침공·탈혼공). 매향심결의 aspd/crit이 먹히게 heroAtkSpd·critCh에
  artMul 경로 확장. 액티브 신규 3종(금강파산권·태극환장·표매권)은 그림
  병목이라 보류 — 트리 노드는 임시 {artPower:12}(docs/트리안/신규무공.md).
- **트리 패널 게임 이식**(src/67-treepanel.js) — 무공 탭이 문파 탭+거미줄
  노드망을 연다. 탭 전환·노드 익히기(경지 포인트)·되돌리기·정보 칸. openArts/
  closeArts/artsHud 재정의로 옛 평면 표(63-arts) 대체. treetest·artstest 갱신.
- **밸런스**(규칙 4): sim spend()에 트리 할당(무공 마디 우선·싼 것부터) +
  QA표에 트리·성장형공격 축 추가. 2h 시뮬 = 5분 죽림7 → 30분 동굴3 →
  120분 천산1. 트리가 새 성장축이라 **기존보다 ~1구역 빨라짐**(가속) — 튜닝
  노브는 SKILLTREE.ptsPerStar/노드 %. 아직 게임 파괴적이진 않음.
- **질주 비율 보정** — 몸이 작아 얼굴이 커 보이던 것 해결(몸 43→44, 캔버스
  꽉 채움). 재추출 중 어두운 보라 브래킷 잔재(is_bg r>25가 놓친 r19)를
  is_bg 문턱 r>12로 세정. HFX.aw.run 46.

# v2.46.1 — 화염 발차기 두 종 추가 (성장형 공격 상위 티어) (2026-09-11)

사용자 시트(불꽃·흙먼지 발차기)로 성장형 공격의 상위 티어를 채움.

- **도약 화염 발차기**(flykick, 성급 9 일류): 도약→화염→흙먼지 임팩트.
- **화염 옆차기**(firekick, 성급 13 절정): 뻗어 차며 불꽃·흙먼지.
- ATKMOVES에 need 9·13으로 얹기만 해서 성급대로 자동 추가 — 시스템 설계 그대로.
- 정규화: punch/kick와 동일 배율(크기 일치), **발(어두운 부츠) 기준 정렬**로
  흙먼지에 발이 안 튀게. 폭 44(HFX.aw.flykick·firekick). 임팩트 index2.
- fxtest 상위 티어 해금·그리기 검사 추가. 원본 sheets/hero_firekick.png. 전 스위트 통과.

# v2.46 — 성장형 기본공격 (성급 오르면 동작이 는다) (2026-09-11)

사용자 설계: 기본공격이 경지 성급에 따라 성장한다. 처음엔 양주먹만,
발차기는 일정 성급부터 해금, 성급이 오를수록 공격 동작이 더 는다.

- **ATKMOVES**(00-data): 무브셋을 `{key, need(성급 realmLv 문턱)}` 목록으로.
  양주먹 need 0, 발차기 need 5(이류). `atkPool()`=지금 열린 동작. 앞으로
  동작 스프라이트가 생기면 목록에 need와 함께 얹기만 하면 는다.
- heroAttack이 열린 동작을 순서대로 돌려 쓴다(P.atkMove 인덱스·P.atkKey).
  삼류(성급<5)면 양주먹만, 이류부터 양주먹·발차기 교대.
- drawHero atk는 P.atkKey로 스트립 선택. 스프라이트는 v2.45의 punch/kick 재사용
  (사용자: 방금 준 시트 말고 그전 걸 양주먹으로). 새 공격 시트는 sheets/
  hero_strike2.png로 보관(추후 동작 확장 후보).
- fxtest: 낮은 성급 양주먹만 / 성급 오르면 발차기 해금 / 연속공격 교대 검사.
  전 스위트 통과.

# v2.45 — 기본공격 양주먹·발차기 교대 + 질주 폭 보정 (2026-09-11)

사용자 시트(공격 10칸: 위 5=발차기, 아래 5=양주먹)로 기본공격 개편.
"기본공격에 발차기와 양주먹 같이 쓰자".

- **기본공격 = 양주먹(punch)·발차기(kick) 4프레임 교대** — P.atkAlt가 매 타
  토글하므로 한 대는 양주먹, 다음은 발차기. 각 동작 임팩트 프레임을 index2
  (HITFRAME)에 맞춰 재샘플(발차기 1·3·4·5 / 양주먹 6·7·8·10). 폭 43 공통,
  동일 배율·몸통 정렬. HFX.aw.punch·kick. 기존 권기 정권(katka/katkb)은
  기본공격에서 물러남(에셋 보존) — 성장 표현은 경지 기운·타격 이펙트가 담당.
- **질주 폭 보정** — v2.43~44 질주가 35칸에서 보폭·옷자락이 좌우로 닿아
  spritetest 가장자리 경보가 있었다(내 검증 grep이 "문제"를 못 잡아 딸려 나감).
  폭 44·상단 여백으로 재추출(HFX.aw.run 44). 검증 grep도 ★/문제까지 잡게 교정.
- fxtest 기본공격 검사(양주먹·발차기 교대)로 갱신. 원본 sheets/hero_strike.png.

# v2.44 — 질주 재작업 10프레임 (2026-09-11)

v2.43 이동이 "뛰는 게 아쉽다"는 피드백 → docs/프롬프트-주인공.md 질주 프롬프트로
사용자가 새 시트(10칸, 역동 질주)를 뽑아 줌. 이걸로 달리기 교체.

- **달리기 → 10프레임** (ANIM.run [8,13] → [10,15]): 전방 기울임·긴 보폭·
  옷자락 날림이 살아난 진짜 질주. v2.43과 같은 정규화(8→10컷 공통 배율로
  크기 안정 + 몸통 무게중심 가로 정렬 → 좌우 흔들림 없음, 바닥 y49, 35×51)
- 대기(14)·경공 착지(15)는 v2.43 그대로. fxtest 프레임 수 검사 10으로 갱신,
  전 스위트 통과

# v2.43 — 주인공 이동 사이클 8프레임·대기 자세·경공 착지 (2026-09-11)

사용자 시트("8-FRAME MOVEMENT CYCLE" 16칸)에서 주인공 이동 애니를 교체.
검사판을 보이고 프레임 선택을 확정받은 뒤 반영(규칙 0).

- **달리기 → 8프레임** (시트 1~8): 기존 4프레임을 상단 걷기/달리기 사이클로
  교체. ANIM.run [4,10] → [8,13]. 배율을 **8컷 공통**으로 줘 크기가 안 튀고,
  **몸통 띠(위 30~62%) 무게중심**으로 가로 정렬해 좌우 흔들림을 없앴다
  (bbox 중앙은 뒷자락에 흔들린다). 바닥 정렬 y49, 공통 캔버스 35×51
- **대기 → 정면 전투 자세** (시트 14): ANIM.idle [3,4] → [1,4] 단일 컷.
  옆모습 달리기와 달리 정면 파이팅 스탠스라 멈추면 기수식으로 선다
- **경공 착지 → 웅크림** (시트 15): dashland을 착지 웅크림 컷으로 교체
  (37×54 유지). 날기(dashfly)는 v2.41 그대로
- 추출은 마젠타+보라 프레임 제거 후 덩어리 라벨링 — 별(★)·브래킷 자동 제외.
  fxtest에 8프레임 달리기·단일 대기 구동 검증 추가. 전 스위트 통과

# v2.42 — 주아 폰트 · 보스 등장 흐림·회복 · 스킬 오토/수동 (2026-09-11)

사용자 피드백 넷: 폰트가 별로다(메이플체) · 보스 나오기 전에 죽는다 ·
등장 때 캐릭터가 흐려진다 · 스킬 오토/수동 모드.

- **폰트 → 주아(Jua)** — 붓글씨 Gugi(v2.40)가 "구리다"는 피드백. 메이플
  폰트는 라이선스·용량 문제로 이 환경에서 임베드 불가라, 둥글고 친근한
  구글 폰트 **주아**로 제목·본문·캔버스 텍스트를 통일(사용자 확정 대체안).
  Do Hyeon은 폴백. 온라인 로드, 오프라인은 시스템 대체
- **등장 때 캐릭터 흐려짐 → 경지 기운 억제** — 보스 소환·문 페이드
  (summonT·gateT) 동안엔 몸 뒤 경지 기운 안개를 끈다. 3.6초간 가만히
  서면 흰 안개가 짙게 깔려 캐릭터가 흐릿해 보였다(drawHero)
- **보스 나오기 전에 죽음 → 등장 시 만회복** — beginSummon에서 P.hp를
  가득 채운다. 단계 진입은 이미 enterStage가 회복시키지만, 제자리 사냥 중
  보스 단계에 든 경우 등 안전장치. (등장 중엔 전투가 없으니 공정하다.
  등장 직후에도 죽는다면 그건 수련·체력 성장 부족 = 밸런스)
- **스킬 오토/수동 모드** (사용자 요청) — S.skillManual. 자동은 기존대로
  알아서 시전, 수동은 쿨만 돌고 **스킬창을 눌러 시전**(castByHand). 반격형
  건곤이형은 피격 발동이라 모드와 무관. 스킬창 왼쪽에 [자동/수동] 토글,
  수동+준비 초식은 맥동(tap)해 누를 수 있음을 알린다. 저장됨(65-save).
  내공 재화가 없어 "공짜 스킬을 손으로"라는 설계 보류(v2.2)와 상충하나
  사용자 명시 요청으로 넣음 — 내공이 들어오면 소비·수동 보너스와 재설계
- artstest에 수동 모드 검사 추가(자동 미시전·castByHand·토글·저장).
  전 스위트 통과

# v2.41.1 — 경공 스프라이트 칸 테두리 세정 (2026-09-11)

v2.41 반영 뒤 인게임 스크린샷에서 경공 스프라이트에 **검은 사각 테두리**가
보였다("테두리가 검은 현상" 재발). 원인은 시트 칸의 프레임선·모서리 브래킷이
**어두운 보라(g≈0, r≈b, ~110)** 라 마젠타 배경 검출을 통과해 몸 덩어리에
붙어 나온 것 — CLAUDE.md "칸 테두리는 순검정이 아니다" 사례.

- 배경 검출에 **어두운 보라 프레임**을 추가(g < r-22 && g < b-22 && |r-b|<34)
  하고, 마젠타·프레임 제거 뒤 **최대 덩어리(주인공)만** 남겨 재추출
- 가장자리 불투명 픽셀: dashfly 위40·좌45 → 위0·좌4, dashland도 정상화
- 규격(43×45·37×54)·자세·먼지 잔상 그대로. 코드·데이터 변화 없음
- fxtest 포함 전 스위트 통과, 인게임 확인 — 테두리 사라짐

# v2.41 — 경공(輕功) (2026-09-11)

사용자 아이디어: 일정 레벨부터 경공을 써서 원거리·후방 먼 적에게 날아가
패는 것. 사용자 시트(뛰기 3버전 + 경공 2버전) 중 경공 img4 채택.

- **경공 발동** — 절정(DASH.realm 12)부터, 가장 가까운 적이 DASH.min(170)
  이상 멀면 쿨다운(5초)마다 그 적 코앞으로 훌쩍 날아간다(DASH.spd 680).
  원거리 몹(빙백령·주술사)이 물러나며 쏘는 견제를 무력화한다
- **스프라이트** — 시트의 날기(칸2)·착지(칸4) 두 컷만 몸으로 추출
  (dashfly 43×45·dashland 37×54). 회오리·나선 궤적은 몸과 붙어 정규화가
  안 되므로 버리고, 날아가는 흙먼지는 코드 잔상(burst)으로 대체
- **동작** — P.dashT 비행 중엔 dashfly로 공중에 떠(DASH.lift) 목표로 보간
  이동, 도착하면 dashland 경직(DASH.hold 0.16) 후 바로 공격. 비행 중
  step은 카메라만 갱신하고 return
- fxtest에 경공 발동·접근·컷 검사 추가. 거리 고정 zone 테스트(snow·heaven)는
  DASH.min=1e9로 경공을 꺼서 측정. sim 30분 동굴2 유지(견제 제거일 뿐
  DPS 급증 아님), 전 스위트 통과

## 크기
`dist/wuxia.html` **5448KB** · 에셋 462개
